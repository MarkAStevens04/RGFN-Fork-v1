# ablations

Ordering floor and ceiling, filter ablation, greedy-oracle ceiling, tau/gate surfaces, parallel
groups.

**The core method is filters 1 + 2** (reward gate + Tanimoto diversity). The parallel-group filter is
a labelled METHOD VARIATION, never folded into the headline: it exists to show a hard selection rule
still has room for extra requirements, and must not be confused with the main result.
