# committed results

Small CSV/JSON per exhibit. Each exhibit directory carries a `PROVENANCE.md` naming: the script that
produced it, the artifact path it lands in inside `AC-MedChem-SDL/RGFN-LSD`, and the `reproduce/`
script that consumes it there.

Figures are build products and are never committed.
