# stage 3 — retrosynthesis (COMPETITORS ONLY)

MultiAiZ for the route-less generators (FragGFN, S3-GFN, REINVENT, Saturn) at ~16 s/molecule.

Native routes for the two that ship them: SynFormer (`routes.jsonl`) and TANGO (**ARM 2 only** —
re-run syntheseus over the emitted pool, ~3.5 h/cell; ARM 1's coverage correlates with the generator's
own mode collapse and covered 7 of 218 molecules on the pruned sEH pool).

Our side pays zero here — recipes already exist. That asymmetry is a result, reported rather than
equalised.
