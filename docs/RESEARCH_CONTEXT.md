# RGFN Molecular Glue Research — Project Context

This file exists so that AI agents working on this project can orient themselves quickly. Read it before writing any experiment log. Update it as the project evolves.

> **Ground your claims in the real work.** The method (what RGFN/GFlowNets are) and the
> biology (why these systems and metrics) come from a short shelf of papers in
> `Logs/references/` — read its `README.md` before asserting how the model or the oracle
> works. Cite by key (e.g. `[koziarski2024rgfn]`); don't reconstruct a citation from memory.

---

## What this project is

We're building an end-to-end computational pipeline for generating novel **molecular glue degraders** — small molecules that force two proteins to interact, triggering degradation of a disease-causing protein. The generative model is **RGFN** (`[koziarski2024rgfn]`), a **GFlowNet** (`[bengio2021gflownet]`). A GFlowNet is *not* a reward-maximizing RL agent: it learns to **sample** candidates with probability **proportional to their reward**, so it returns many diverse high-scoring molecules instead of collapsing onto a single optimum. RGFN specifically builds each molecule by composing **chemical reactions over a building-block library**, so everything it proposes is synthesizable by construction. RGFN needs a *reward signal* to know whether a molecule is a good glue candidate — we're building and validating that signal (the **oracle**).

The oracle uses **molecular docking**: simulate how well a candidate molecule sits in the complex and — crucially — whether its *arm* helps recruit the second protein the glue must bring in. A good glue should score well not just because its warhead is anchored in a pocket, but because its arm earns an **extra** binding bonus once the recruited partner is present. We capture that bonus as a two-tier differential on the **same pose**: Tier 1 scores the molecule against only the protein its warhead anchors into; Tier 2 adds the recruited partner; the **differential (Tier 2 − Tier 1)** isolates the **arm's contribution once the recruited partner is present** — our working proxy for glue-specific cooperativity. (This is the project's own methodological contribution, not something inherited from a paper.)

What the differential *does* capture is the ligand arm's direct contacts with the recruited partner in the docked pose. What it *cannot* capture is protein–protein interface stabilization that doesn't run through the ligand — which is the most likely explanation for the CRBN ceiling below, where neosubstrate recognition is heavily PPI-driven.

Which protein is *anchored into* versus *recruited* **flips between our two systems** — they are mirror images, which is exactly why the same differential works for both:

- **CRBN / 5HXB** (`[matyskiela2016cc885]`): warhead (glutarimide) anchors in the **E3** (CRBN); the arm recruits the **neosubstrate** (GSPT1). Differential = GSPT1 bonus.
- **CDK12–DDB1 / 6TD3** (`[slabicki2020cr8]`): warhead (purine) anchors in the **target kinase** (CDK12); the arm recruits the **E3 adaptor** (DDB1). Differential = DDB1 bonus.

We call this metric the **neosubstrate differential** for historical reasons — it was first defined on CRBN, where the recruited partner *is* the neosubstrate. For 6TD3 the recruited partner is the E3, so there the same metric is the DDB1 bonus. (For in-silico glue-design context and why 6TD3 is a sound testbed, see `[bengeoffrey2025molde]`.)

### How the model learns: the active-learning loop

RGFN never reads the expensive scoring function directly. We train it the way `[bengio2021gflownet]` runs its multi-round molecule experiments (§4.3; **Algorithm 1** in A.5): a fast **proxy** `M` is the in-loop reward, and the expensive **oracle** `O` is queried only on small batches to keep that proxy honest.

**Before the loop (initialization).** Build a seed dataset `D_0` of `(x, O(x))` pairs labeled by the *true* oracle, then **warm-start** the proxy `M` by fitting it on `D_0`. Skip this and round 1 trains RGFN against an untrained proxy (noise). Also fix `N` (rounds), `β` (inverse temperature), and `K` (how many top molecules you want out at the end). One more constraint: a GFlowNet needs a **positive** reward, so `O(x)` — and hence `M(x)` — must be scaled/shifted to be `> 0` before use (docking is negative-is-better, so this matters).

**One round** (higher β peaks the sampling distribution harder toward high reward):

1. Fit the proxy `M` on everything labeled so far, `D_{i-1}`.
2. Train RGFN to sample proportional to `M(x)^β`.
3. Sample a query batch `B = {x_1,…,x_b}`, each `x_j ~ π_θ`, from the trained policy.
4. Score `B` with the expensive oracle `O`, giving `D̂_i = {(x_j, O(x_j)) : x_j ∈ B}`.
5. Accumulate the **full history**: `D_i = D̂_i ∪ D_{i-1}` (the proxy is refit on everything, not just the latest batch).
6. Repeat for `N` rounds; the deliverable is the top-`K` molecules in `D_N`.

Verbatim algorithm (`[bengio2021gflownet]`, Alg. 1, A.5; their generic oracle `O` = our docking/MD scorer, their proxy `M` = our fast learned reward):

```
Input:  D_0 = {(x_i, y_i)}   (y_i = true oracle reward);  K;  N;  β
Result: TopK(D_N)
Init:   proxy M;  policy π_θ;  oracle O;  i ← 1
while i ≤ N:
    fit M on dataset D_{i-1}
    train π_θ with unnormalized target reward  r(x) = M(x)^β
    sample query batch  B = {x_1,…,x_b},  x_j ~ π_θ
    evaluate B with O:  D̂_i = {(x_1, O(x_1)), …, (x_b, O(x_b))}
    update dataset  D_i = D̂_i ∪ D_{i-1}
    i ← i + 1
return TopK(D_N)
```

**The one gotcha to respect:** oracle scores enter the loop *only* by retraining `M`, never as a direct RGFN reward — the chain is `oracle → improves proxy → proxy scores everything → RGFN learns`, not `oracle → RGFN`.

Why this needs a *diverse* sampler — i.e. why a GFlowNet rather than reward-maximizing RL — is the paper's own argument: the proxy is only trained on what the generator proposes, so a mode-collapsed generator gives the proxy no signal outside the modes it already found (`[bengio2021gflownet]`, A.5). RGFN's diversity keeps the proxy's training distribution broad, which is what makes the loop work at all.

**Mapping the paper's vocabulary onto our systems.** The expensive oracle `O` is our **docking** score today (and **MD stability** later — see open experiments); the **proxy** `M` is a fast learned model fit on those scores. In their molecule experiment they instantiate exactly this — an MPNN proxy trained to predict AutoDock scores, refit on ~200 freshly docked molecules per round (`[bengio2021gflownet]`, A.5.2). Where docking is cheap enough to call in-loop, it can stand in for `M` directly in early single-system runs; the proxy/refresh machinery is what keeps the full run tractable once the per-molecule score (MD, co-folding) is far too slow to call millions of times.

The core argument of the paper: RGFN + a properly designed docking oracle, trained through this active-learning loop, can generate novel, drug-like, **synthesizable** molecular glue candidates. Each experiment is a piece of evidence for this argument.

---

## How library cost is measured: the MultiAiZ → SPARROW pipeline

> **⛔ READOUT CONVENTION (decided 2026-08-17): a fixed budget of 100 REACTIONS is primary.**
> Ask "how many distinct high-reward molecules do 100 reactions buy?", **not** "what do 100 modes
> cost?". Both come off the same ordering at read time, so this is a reporting convention rather than
> a different experiment — but do not mix the two axes in one table. Full reasoning, and the
> re-slicing trap that is exact for greedy but not for SPARROW, are in **CLAUDE.md** under
> "THE BENCHMARK'S PRIMARY READOUT". Everything below this line, and every number in Logs/056 through
> Logs/063, is written at the older 100-*mode* readout — that is the secondary axis now, not wrong,
> but re-read it on the reaction axis before quoting.


> **Naming (adopted 2026-08-04).** SPARROW does **two different jobs** in this project and conflating
> them is the single easiest way to misread a result. We now name them explicitly and use these terms
> in all new logs and figures: **SPARROW-Verifier (SV)** and **SPARROW-Batching (SB)**, defined below.
> Older entries predate the convention — read them with it in mind.

### The question this machinery answers

A chemist wants **100 different kinds of molecule likely to bind a target**, and has to physically
make them. Every reaction costs time and money. So: *which pipeline delivers those 100 for the fewest
reactions?* Our claim is that building molecules **out of reactions in the first place** — so the
software always knows how each one was made — beats generating molecules first and working out how to
make them afterwards.

### Three stages, swappable

The benchmark is three independent stages joined by two contracts
(`validation/lsdflow/eval/base.py`):

```
Generator ──pool──▶ Selection strategy ──ordered library──▶ Evaluator ──▶ (reactions, timing)
```

- **Generator.** Ours (SCENT, RGFN, RxnFlow) build from reactions and carry recipes. The non-reaction
  baseline (S3-GFN) emits molecule strings with **no recipe at all** (`has_route=0`).
- **Selection strategy** (`glue/samplers/lsdflow/campaign.py`) — which molecules to actually make.
- **Evaluator** — what the chosen set costs.

**A structural constraint that drives every design choice here:** our own cost model
(`count_once`) needs a reaction DAG, so route-less libraries carry `count_once_reactions=None`. It
**literally cannot score S3-GFN.** Any comparison against a non-reaction generator therefore *has* to
go through SPARROW. That is forced by the architecture, not a preference.

### SPARROW's two jobs

**SPARROW-Verifier (SV) — the independent auditor.** We hand it a library we have *already chosen*
plus its recipes and ask: what is the cheapest way to make **all** of it? In code:
`constrain_all_targets=True`, reward weight **zero**. SV decides nothing; it checks our arithmetic.
This is what produces statements like "our estimate is within 3.1% of a provably optimal solution".
We *expect* SV to agree with us — that is the point.

**SPARROW-Batching (SB) — a genuine competitor.** We hand it a large pool with recipes and scores plus
a reaction budget, and it chooses **which** molecules to make. In code:
`constrain_all_targets=False`, reward-bearing objective, hard `--max-rxns`. This is SPARROW used the
way it was designed, and it is a real rival to hub-batching, not a measuring device.

SB has **no diversity term**. It maximizes score under a budget, and cheap means shared recipes, which
means structurally similar molecules. So whenever SB selects, the fraction of its picks that are
genuinely distinct is something we **measure**, never assume.

*SB is a selector, so always name the pool with it* — `S3GFN + SB`, `BC-SB`, `BC-Enum-SB` — otherwise
"SPARROW-Batching" reads as one fixed experiment when it is a slot in a matrix.

### ⛔ The hit-gate standard: 5% FPR against property-matched decoys (settled 2026-08-21)

**Every target's gate is the score at which 5% of its property-matched decoys pass.** Not a published
convention, not a round number, not another target's value. `experiments/lsd_hubs/matrix16/targets.py`
is the source of truth and `experiments/oracle_validation/calibrate_gates.py` re-derives it.

| target | gate | TPR | FPR | enrichment | AUROC | was |
|---|---|---|---|---|---|---|
| sEH | **5.68** | 13% | 5% | 2.5× | 0.676 | 5.0 → 16% FPR |
| DRD2 | **0.345** | 74% | 5% | 14.9× | 0.949 | 0.5 → 4% FPR |
| ClpP | **−9.1** | 47% | 5% | 9.5× | 0.895 | −8.0 → **23% FPR** |
| 6TD3-B | **6.718** | 78% | 5% | 15.6× | 0.917 | *(signal changed to CNN_VS — see 072)* |

**Why a common criterion rather than a common number.** The four rewards are on incompatible scales —
an arbitrary proxy value, a probability, kcal/mol, a pK estimate — so a shared bar is meaningless.
What can be equalised is the bar's *effect*. Before this rule the decoy pass-rate ran from 4% to 23%:
a "mode" on ClpP admitted realistic non-binders **six times** more often than a mode on DRD2, which
quietly made mode counts non-comparable across targets — the one thing a four-target matrix has to
get right.

**Why FPR and not TPR or Youden.** The gate answers *does this molecule count*, and the failure that
corrupts the benchmark is decoys inflating a count. Fixing FPR bounds that contamination identically
everywhere. Fixing TPR equalises something we never report and lets contamination float — at TPR 75%
the sEH gate admits **49%** of decoys, which is barely a gate. Youden's J weights a false positive and
a false negative equally, which is wrong when the headline number is a **count**: admitting junk costs
more than missing a hit.

**Why 5% and not 1%.** At 1% the weaker oracles starve — sEH would retain **3%** of known actives, and
a gate discarding 97% of actives makes most cells pool-limited, which is a different failure rather
than a fix.

**Do not round the bars.** 5.68 / 0.345 / −9.1 / 7.97 are empirical grid points; rounding breaks the
exact-FPR property that defines the standard. Re-run the calibrator if a decoy set changes.

**One consequence to state, not bury.** sEH cannot reach a good operating point at *any* threshold —
AUROC 0.676, and enrichment stays in 2.4–2.7× across the whole range. That does not invalidate the
benchmark (every arm is compared at the same gate, so a weak gate affects all arms equally), but
"sEH modes" is a weaker claim than "DRD2 modes" and should not be presented as equivalent.

### ⛔ How to report diversity whenever SPARROW selects (settled 2026-08-19)

**Build to this. It is settled, and the reasons below are the ones a reviewer will probe.**

**1. Use SPARROW's OWN diversity mechanism, never a rule of our own.** `[fromer2025diversity]` (same
group as SPARROW) adds a diversity weight — objective slot 3, which triggers its
`add_diversity_objective`. Our clone already ships it; switching it on is *configuration*, not an
upgrade. Pass it with `--lambda-div`. Competing against a diversity-blind selector when the authors
published a diversity-aware one is the single easiest way to have the whole comparison dismissed.

**2. Clusters are caller-supplied, so supply OUR τ-modes.** The paper defines the partition as an
input, so handing it our own mode definition is legitimate *and* makes SPARROW compete on our metric
rather than a second, incompatible one.

**3. Sweep λ; never report a single value.** `{0, 0.1, 1, 10}` is the standing ladder. λ=0 and 0.1 are
cheap and act as redundancy. Measured on sEH: BC-Enum-SB goes **2 → 24 distinct** from λ=0 → 1 (12×
on their own knob) and gains **nothing** at λ=10 while pushing solve time past 7,000 s. Reporting only
λ=0 understates the competitor; reporting only the best λ hides that it saturates.

**4. Report BOTH the unpruned and the pruned set — we deliberately have not picked a headline.**
- *unpruned* (everything SPARROW selected): `n_selected`, `rxn_per_selected`
- *pruned* (survivors after τ-mode sphere exclusion), then **re-priced**: `n_modes_kept`,
  `cost_kept_rxns`, `rxn_per_mode_kept`

**5. The five metrics to carry on every SB row.** All are already emitted by
`sparrow_select_frontier.py`: `clusters_touched` (their metric), `n_modes_kept` (ours),
`scaffolds_all` / `scaffolds_kept` (Bemis-Murcko), `rxn_per_selected` and `rxn_per_mode_kept`,
`mean_pairwise_sim` / `mean_pairwise_sim_kept`. Report their metric alongside ours — we expect to
lose on theirs, and saying so first is worth more than the point costs.

**6. Prune-then-reprice, and say why it is fair.** The obvious objection is "you cut on diversity
first, so of course synthesis looks worse." Two answers: a chemist does not pay for compounds they
discard, and the step is **symmetric** — applied to hub-batching's own library it is a **no-op** (100
in, 100 out), because that strategy only ever accepts a molecule τ-diverse from everything already
taken. Generous to the competitor, free for us.

**7. Do NOT add a hard "touch ≥K clusters" constraint.** Tried and **rejected on evidence**: sphere
exclusion guarantees the *representatives* differ, not arbitrary members. Measured — 75 families gave
75 distinct via representatives but only **54** via arbitrary members, and a 25-family constraint
yielded **14** distinct. Cluster coverage ≠ modes formed, and one low-reward molecule can belong to
many never-before-seen families (the multi-coverage problem).

**8. Never quote a `TimeLimit` row as a competitor result without saying so.** CBC reports `Optimal`
when it has merely run out of time (PuLP 3.3.2's `sol_status` does not distinguish), so
`sparrow_select_frontier.py` detects it by wall-clock. A capped row is a **lower bound on the
competitor**, i.e. it **flatters us**. This is not hypothetical: our sEH headline read 3.4× off a
capped row and dropped to **~2.5×** once two replicate seeds converged.

**9. Optimizing for information gain is out of scope for this paper — put it in Limitations.**

**Where to look:** `Logs/062` (the diversity sweep + the R=100 readout),
`experiments/lsd_hubs/campaign/sparrow_select_frontier.py` (emits all of the above),
`results/paper_pair_gallery/` (what "distinct" looks like structurally).

### The two pools and the two numbers (decided 2026-08-19)

Everything the competitor pipeline reports is a **2×2**: two ways of building the 500-molecule pool we
hand to MultiAiZ, and two ways of counting what SPARROW-Batching then buys with **100 reactions**
(the primary budget — see CLAUDE.md, "THE BENCHMARK'S PRIMARY READOUT"). Both pools go through the
identical downstream: MultiAiZ retrosynthesis → SB selects under the budget → count.

**The two pools.**

- **Naive pool** — the top 500 by reward, no diversity filter of ours. Whatever diversity it has is
  whatever the generator's *own* machinery produced (REINVENT's `IdenticalMurckoScaffold` filter,
  Saturn's filter, S3-GFN's soft-synthesizability training). This is the pipeline as a chemist would
  actually run it if they trusted the generator, and it is the setting in which each program is judged
  on the tools it ships with.
- **Pruned pool** — 500 molecules that are *guaranteed* mutually distinct: greedy sphere exclusion in
  reward order (Morgan r=3/2048, Tanimoto `< τ`), i.e. the top 500 **modes** rather than the top 500
  molecules. Scanning deeper is free — the filter is CPU-seconds — and we still route only 500, so
  this costs no extra MultiAiZ. It reaches further down the reward ranking to find them, and that
  price is measured, not waved away.

**The two numbers, reported for BOTH pools.**

- **reactions / candidate** — `used_rxns / n_selected`. High-reward molecules, *no* diversity filter
  on the selection. **This is the metric the competitor wins**, and it is reported first precisely
  for that reason: a cost-only optimizer is extremely good at buying many molecules cheaply when
  near-duplicates share intermediates (Logs/059: 89 candidates for 100 reactions ≈ 1.12).
- **reactions / mode** — the same selection pruned to genuinely distinct molecules and re-priced.
  This is the metric the paper's claim is about, and the gap between the two numbers *is* SPARROW's
  diversity blind spot, stated as a measurement rather than an argument.
- **mean pairwise Tanimoto** over the selection, as the continuous companion to the mode count — no
  partition-based metric can fake it (see the edge-effect finding in Logs/062 Method 3). Emitted on
  both output sets (`mean_pairwise_sim`, `mean_pairwise_sim_kept`), alongside Bemis-Murcko scaffold
  counts and SPARROW's own `clusters_touched`, so the reader can see where the diversity proxies
  disagree instead of trusting our choice of one.

**What each cell of the 2×2 is for.** On the naive pool the two numbers *diverge*, and that divergence
is the competitor's blind spot. On the pruned pool every selected molecule is distinct by
construction, so **reactions/candidate and reactions/mode coincide wherever the budget binds** —
which doubles as a correctness check with real teeth. The two numbers come from *different code
paths*: `rxn_per_selected` divides the MILP's own `total_reactions`, while `rxn_per_mode_kept`
re-prices the surviving set from scratch through `_price_kept_set`. On a pruned pool the mode filter
keeps everything, so at a binding budget the paths must agree — **verified 2026-08-20**, gap exactly
0 at R=50/100/150/200 on both `reinvent_seh_seed43_pruned` and `saturn_seh_seed42_pruned`. A
disagreement there means the filter or the re-pricing is wrong and every reactions-per-mode figure in
the benchmark is suspect.

**They do NOT coincide once the pool is exhausted, and that is a property of SPARROW's solution, not
a bug in ours.** Past exhaustion the MILP has budget it cannot usefully spend, and its reported
`total_reactions` keeps climbing while the true cost of the selected set stays flat: Saturn's pruned
cell holds all 65 molecules from R=300 on, priced at **247** reactions by `_price_kept_set`, while
`used_rxns` reads 300 / 322 / 357 / 388 / 387 / 387 across R=300...1000 — up to **140 reactions of
pure slack**, and non-monotonic, which is the signature of the reaction count no longer constraining
the objective. Consequence for reporting: **`used_rxns` is the right stop-reason diagnostic but the
wrong cost number outside the budget-binding regime**, where it overstates the competitor's spend
(i.e. it flatters us). Quote `cost_kept_rxns` for cost, and read the two together — a nonzero
`used_rxns - cost_kept_rxns` gap is itself the exhaustion detector. What the
pruned pool costs relative to the naive one is the price of guaranteeing diversity up front.

**Why both pools rather than one.** Reporting only the naive pool invites "you never let it be
diverse"; reporting only the pruned pool invites "you did the hard part for it". Running both closes
each objection with the other, and neither is a rescue of any particular entrant — the rule is applied
identically to every generator. Note it is not neutral in one respect worth stating: MultiAiZ is
**set-based**, so planning 500 diverse molecules converges on fewer shared intermediates than planning
500 near-duplicates. The pruned pool may therefore cost *more* per molecule, and since our own side
uses native routes and never calls MultiAiZ, that effect must be measured and reported rather than
assumed harmless.

**CORRECTED 2026-08-24: the pruning effect is a COLLAPSE DETECTOR, not a general property.** The
original measurement — pruning worth 2.3-2.9x to the competitor — came from REINVENT running at 12.4x
its authors' oracle budget. Over-training had collapsed it into a narrow high-reward band; at its own
budget it has not collapsed, and pruning buys **1.0-1.36x** (on seed 44 the two pools are identical,
its whole above-gate set being smaller than 500). Measured across all thirteen completed cell-groups,
the lift tracks collapse and nothing else:

| entrant | sEH | DRD2 | ClpP | needs both pools? |
|---|---|---|---|---|
| Saturn | **13.7x** | **5.2x** | **4.0x** | yes |
| TANGO (Saturn's agent) | **7.4x** | **2.7x** | **3.3x** | yes |
| SynFormer | — | 3.5x | — | yes |
| REINVENT | 1.18x | 1.38x | **1.00x** | sEH/DRD2 marginal; ClpP no |
| S3-GFN | **1.00x** | 2.23x | **1.00x** | sEH/ClpP no |

So the naive pool is the honest default and under-reports only for a genuinely mode-collapsed
generator — which, at their authors' budgets, means the Saturn family. Three cell-groups
(`reinvent_clpp`, `s3gfn_seh`, `s3gfn_clpp`) come out at exactly 1.00x and need only ONE pool, saving
nine MultiAiZ runs (~50 GPU-h) on a diagnostic that costs seconds. Run
`mode_saturation.py` per cell before committing route discovery; do not route both variants blindly.

Caveat on all of the above: these are pool *contents*, not routed costs. MultiAiZ is set-based, so a
diverse pool converges on fewer shared intermediates and the routed lift can differ from the mode
lift. The REINVENT sEH seed-42 pilot routes both variants specifically to measure that.

**Which molecules both pools are drawn from — `candidates.csv`, uniformly (decided 2026-08-19).**
Every entrant's pool is drawn from its `candidates.csv`: a fresh sample from the **trained final
policy** (REINVENT's `sample`, Saturn's `n_target`, and the same protocol behind all sixteen matrix
cells). The tempting alternative for a collapsed generator is its *training* history —
Saturn's `oracle_history.csv` holds 10,020 scored molecules against the 2,000 sampled — and it is
rejected on comparability, not on principle: it is a different object ("everything the search
touched") available in no comparable form for our own generators, whose training trajectories were
never a pool for any published cell. Swapping it in for one entrant would change what "the
generator's output" means midway through the benchmark. It is measured and reported as a
*diagnostic* instead, because it separates two very different failure stories, and the naive-pool
number barely moves between the two sources (Saturn sEH s42: **18** modes in the top 500 of
`candidates.csv` vs **16** from the history), so nothing hinges on the choice.

**What the two pools actually contain** (sEH, gate 7.0, τ = 0.5, seed 42, from
`mode_saturation.py`):

| entrant | above gate | modes in **naive** top-500 | modes available in the whole set | **pruned** pool size |
|---|---|---|---|---|
| REINVENT | 1,860 | 201 | 963 | 500 (scan depth 1,127; reward 8.36 → 7.76) |
| S3-GFN | 500 | 206 | 732 | 500 |
| Saturn | 1,911 | **18** | **338** | 338 — short of 500, flagged |

Saturn is the case that justifies the design. Its collapse is **not** a failure of its search: its
2,000 samples contain 338 distinct modes, and its 10,020 training molecules contain 1,251. The
collapse is concentrated at the **top of the reward ordering** — the reward-maximizing pressure that
makes Saturn sample-efficient also makes its highest-reward band nearly one scaffold. Reading only
the naive pool would report that as "Saturn cannot produce diversity", which is false; reading only
the pruned pool would hide the top-of-ranking collapse, which is real and is the thing a chemist
taking the top 500 would actually hit. Both numbers are needed to say either.

A short pruned pool is **not** an exclusion. It is emitted at whatever size the generator can supply,
flagged pool-limited, and still reported — and at a 100-reaction budget it very rarely binds anyway,
since ~100 reactions buys on the order of tens of modes (REINVENT naive sEH: 20 modes for 87
reactions), far below 338. `mode_saturation.py` predicts the stop reason before any route discovery
is paid for, and aborts only below `--min-modes`, where there is genuinely nothing to measure.

**Where the columns already live.** `sparrow_select_frontier.py` emits all of it per budget point:
`rxn_per_selected`, `rxn_per_mode_kept`, `mean_pairwise_sim`, `n_selected`, `n_modes_kept`,
`used_rxns`, plus `milp_status` / `time_capped` — the last two because CBC reports `Optimal` for
whatever it holds when a time limit stops it, so a capped row is a lower bound on the competitor, not
a result.

### What each run must log, audited before the big training run (2026-08-20)

Audited against the question "could we answer this retroactively without retraining?". Three
questions were tested: **modes vs oracle calls**, **where the wall-clock went**, and **per-step
analysis (diversity / SPARROW) on the candidates**.

| generator | per-molecule training record | oracle/step index | wall-clock | routes |
|---|---|---|---|---|
| Saturn | `oracle_history.csv`, 10,020 rows | **`oracle_calls`** | mineable from `saturn.log` (stamped every 60 calls) | MultiAiZ post-hoc |
| REINVENT | `staged_learning_1.csv`, 128k rows | **`step`** | mineable from `.log`, one stamped line per step | MultiAiZ post-hoc |
| SCENT (ours) | `paths.csv`, 278k rows | `iteration` (counts molecules, ÷64 — see [[flow-field-drifts-during-training]]) | **none** | **full verbose path** |
| RGFN (ours) | `unique_molecules/molecules_N.json`, every 500 steps | snapshot granularity only | **none** | **none kept** (re-derivable by resampling the checkpoint) |
| SynFormer | **none — SLURM stdout only** | partial (`N/10000` lines) | **none** | native, via `route_convert.py` |
| S3-GFN | **none — only 1,000 final eval samples** | **none** | **none** | MultiAiZ post-hoc |

**Two runs would have to be repeated to answer the questions, and both are the expensive ones.**
S3-GFN discards its entire training history — at the old 5,000×64 budget that is ~320,000 molecules
gone, leaving a 1,000-row eval sample. SynFormer's history exists only in SLURM stdout, unstructured
and mixed with worker chatter, at ~36 h/cell to regenerate. Neither can answer "modes vs oracle calls"
today.

**SPARROW on the products: yes for our side, already proven.** `paths.csv`'s `path` column is an
alternating sequence of intermediate SMILES and reaction SMARTS with each added building block — a
complete route, strictly richer than what MultiAiZ returns for the baselines, and
`sparrow_select_frontier.py --route-source native` already consumes it. Because `paths.csv` carries
`iteration` alongside the path, **SPARROW can be re-run at any training step retroactively for SCENT**
with no new compute. RGFN cannot: its snapshots are `{smiles: score}` with no route, so per-step
SPARROW there needs re-enumeration from a checkpoint.

**The minimal fix is one file per run.** Every external generator is already wrapped by our own
adapter (`run_{reinvent,saturn,synformer,s3gfn}_fixed.py`), so a uniform trace written from the
adapter costs ~20 lines each and removes every gap above:

```
trace.csv:  oracle_call_index, step, smiles, raw_score, elapsed_s
```

`oracle_call_index` makes the modes-vs-calls curve one query across all five entrants instead of five
bespoke parsers; `elapsed_s` (seconds since run start, per record) is the only timing signal absent
everywhere and is the cheapest thing on this list to add. Saturn and REINVENT already have the
content — the trace just normalises it; S3-GFN and SynFormer genuinely need it written.

**Cheap insurance worth taking at the same time:** periodic checkpoints on every entrant (RGFN already
does this) so any per-step analysis we did not anticipate can be redone by resampling rather than
retraining; and a `timing.json` per run with phase totals (train / oracle / sampling), which our
LSD-Flow side already emits as `sweep_summary.json['compute_time']` (`hub_pick_s`,
`hub_total_s_by_cutoff`, `extra_compute_s_by_cutoff`) and no external entrant emits at all.

### The logging spec for the big run (decided 2026-08-20)

Two artefacts, answering two different questions. Keeping them separate is what makes the cheap one
cheap.

**1. `trace.csv` — every scored molecule, written continuously.** One row per oracle call, including
repeats (duplication rate is itself a mode-collapse signal, so it is recorded rather than deduped):

```
oracle_call_index, step, smiles, raw_score, is_duplicate, elapsed_s
```

At the 10,000-call budget this is ~10k rows per cell — negligible. **This file alone answers goal 1.**
Any "X vs oracle calls" curve — modes, diversity, top-k, mean reward — is a slice of it, at maximum
resolution, with no snapshots and no cadence to choose. Note that "save candidates every N calls" is
strictly *less* informative than the trace and costs more, so it is deliberately not part of this spec.

**2. Milestone checkpoints — model + candidates + routes, at log-spaced oracle counts.** Suggested
500 / 1,000 / 2,000 / 5,000 / 10,000: log-spaced because early training changes fastest, and five
points is enough to see a trend without five times the disk. Each milestone keeps the model weights,
a sampled candidate pool, and (where the generator has them) routes. **This answers goal 2**, which
the trace cannot: the trace records what the model *found while searching*, whereas a checkpoint
answers what the model *would produce if deployed now*. Those are different questions and the
distinction matters when reading any "performance at step N" claim.

**The retroactive-SPARROW question resolves better than expected for the reaction-aware entrants.**
SynFormer, TANGO and our own generators carry routes by construction, so a trace that includes the
route means **any budget cutoff can be re-priced with SPARROW later at zero new compute** — take the
first N oracle calls, filter to modes, price. No MultiAiZ, no resampling, no retraining. Only the
route-less entrants (REINVENT, Saturn, S3-GFN) need route planning at analysis time, and only for the
cutoffs actually asked about. So the expensive step is confined to exactly the generators that cannot
avoid it, and only on demand.

**Disk.** Routes are small (~1-2 KB/molecule, so ~10-20 MB per cell). Model weights are not: S3-GFN's
checkpoint is 561 MB, so five milestones across ~15 cells is ~40 GB. That fits scratch comfortably but
should be a deliberate choice rather than a surprise.

**SynFormer's routes are already wired** and need no new work: `route_convert.patch_get_dataframe()`
patches `Stack.get_tree()` — whose per-node `mol`/`rxn` are otherwise never serialized — to emit
`route_steps_json` (`{step, reaction_idx, reaction_smarts, reactant, fragments, product}`), the runner
writes `routes.jsonl`, and `sparrow_select_frontier.py --route-source external` prices it directly.
The gap is *when*: those artefacts are written only at the end of a ~36 h run, so a job that dies at
hour 35 loses everything. Writing the trace continuously is what fixes that, and it matters most for
the most expensive entrant.

### Baseline configs audited against their authors' defaults (2026-08-20)

Every external generator's config was diffed against the authors' own shipped configs. Verdicts below;
**budget is the systemic problem, not the per-knob settings.**

**Saturn — CLEAN.** Our config is `experimental_reproduction/constrained_synthesizability/experiment.json`
verbatim: budget 10,000, batch 64, `augmentation_rounds` 2, sigma 128, mamba,
`IdenticalMurckoScaffold`/bucket 10, hallucinated memory and beam enumeration off. Worth knowing that
their batch size and augmentation rounds **pair with** the budget — 9 of their 10 configs use
budget 1,000 / batch 16 / aug_rounds 10, and only the budget-10,000 one uses 64 / 2 — so picking the
10,000 config wholesale is internally consistent, whereas mixing knobs across their configs would not
have been. The one unavoidably-ours choice is the sEH reward transform (sigmoid 4→9, k 0.5); they never
scored sEH.

**SynFormer — two minor deviations, both benign.** Everything algorithmic matches their
`experiments/graphga_sf_opt.py`: budget 10,000 (their `max_oracle_calls` default), population 100,
offspring 100, mutation 0.1, `search_width` 24, `exhaustiveness` 64, `time_limit` 180,
`max_evolve_steps` 12, `max_results` 100. Deviations: `num_workers_per_gpu` **2 vs their 1** (pure
wall-clock — each worker runs the identical per-molecule loop, two are just in flight at once), and
starting population from the clone's `chembl_filtered_1k.txt` rather than **their random sample of
TDC's ZINC**. The second is a real deviation and is disclosed; it was chosen so all entrants start from
ChEMBL-like space, matching REINVENT's and Saturn's priors.

**REINVENT — a serious budget error.** Everything else is theirs (sigma 128, lr 1e-4,
`IdenticalMurckoScaffold` bucket 25 / minscore 0.4 / minsimilarity 0.4, `reinvent.prior`), but
`batch_size` is **128 vs their 64** and `max_steps` **1,000 vs their example's 100**. REINVENT has no
budget field — steps × batch *is* the budget — so the run scored **124,587 distinct molecules against
Saturn's 10,020**, a **12.4× advantage**. Fix: `batch_size` 64 (theirs) and `max_steps` ≈ 157, which
lands on ~10,048 molecules and satisfies both their batch size and the PMO budget at once.

**S3-GFN — one slip, one deliberate deviation, one budget error.** Matching their maintained
`experiments/pmo/main/s3gfn/hparams_default.yaml`: lr 1e-4, batch 64, `lr_z` 1e-3, beta 50,
`use_retrosynthesis` true, `max_retro_steps` 3.
- **`aux_coefficient` 0.0001 vs their 0.001 — a 10× slip.** No rationale is recorded anywhere in
  `validation/`, `docs/` or `Logs/`; it is simply repeated across our three S3-GFN configs and
  hardcoded as the fallback in `run_s3gfn_fixed.py`. Treat as an error to correct, not a decision.
- `retro_env` `zincfrag_hb105` vs their `stock` — deliberate and recorded, see
  [[competitor-catalogue-fairness]]; `stock` is not among the envs we hold.
- `n_train_steps` 5,000 × batch 64 ≈ **320,000** molecules against their PMO `max_oracle_calls`
  of 10,000, a ~**32×** advantage. Ours was matched to our *own* generators' 5,000 steps, not to their
  paper.

**THE SYSTEMIC FINDING: three budget conventions are running side by side.** ~10,000 oracle calls
(Saturn, SynFormer — their paper defaults), ~128,000 (REINVENT), ~320,000 (S3-GFN, matched to our own
generators). Every external generator's own paper default sits between 1,000 and 10,000. So
baseline-vs-baseline rows in our tables are **not like-for-like**, and the asymmetry is not in our
favour: the two entrants handed extra budget, **REINVENT and S3-GFN, are precisely the two that
perform best against us** — REINVENT's pruned pool is what overtook our from-scratch arm at 46-51
modes. Correcting this should therefore *improve* our headline, which is a reason to trust the
correction rather than to hesitate over it. Decide one convention, apply it to all five, and re-run;
until then, quote no baseline-vs-baseline comparison.

**Reproducibility anchor: the TANGO authors dock against 7UVU**, the same human ClpP structure our ClpP
cells use (bar −8.0, AUROC 0.895, Logs/045). Their objective is QuickVina2-GPU on 7UVU × QED ×
syntheseus. Before trusting our TANGO numbers, reproduce their published result on that system — it is
the only cell in this benchmark where an author-published number exists on our own receptor.

### TANGO: a reaction-aware entrant, and which inventory it gets (decided 2026-08-20)

**TANGO is not a fifth model.** It is a *reward function* inside the Saturn repo
(`oracles/similarity/tango.py` + `oracles/synthesizability/syntheseus.py`), so the generator is
Saturn and `external/setup_saturn.sh` already pins TANGO's hash (`de5cd7f`) for it. What TANGO adds is
a **constrained**-synthesizability objective: a retrosynthesis model (syntheseus, in its own conda env,
invoked per batch) tries to decompose each generated molecule down to a caller-supplied inventory, and
where it fails, Tanimoto group overlap supplies partial credit instead of a zero.

**It yields routes natively, so it skips MultiAiZ.** For every solved molecule syntheseus writes
`route_0.pkl` + `stats.json`, and the oracle `cp -r`s both into a persistent `results_dir`
(`output_{oracle_calls}/mol_{i}`) rather than discarding them with its temp dir. That puts TANGO in
SynFormer's class — routes by construction, priced through `--route-source external` — and saves the
~8 h per cell that route discovery costs a route-less entrant.

Three caveats before those routes are priced:

- **`steps` is NOT a route length.** The oracle reads `stats["soln_time_rxn_model_calls"]`, which is
  the number of reaction-model calls made when the solution was found — a measure of *search effort*.
  Using it as a reaction count would be wrong. The route length must be counted off the reaction nodes
  in `route_0.pkl`.
- **The shipped extractor throws the reaction structure away.**
  `extract_syntheseus_route_data.py` keeps only `{smiles, depth}` per *molecule* node (it filters on
  `"mol" in dir(node)`), which cannot express which reaction consumed which input. The pickle itself
  still holds the bipartite route, so pricing needs our own extractor emitting the same
  `route_steps_json` schema SynFormer uses.
- **`num_top_results: 1`** — syntheseus stops at the *first* route found, not the cheapest. That
  inflates TANGO's reaction count, i.e. it flatters us, so it is a fairness lever worth raising rather
  than a bug to leave in place.

#### Which route arm TANGO gets: ARM 2, the pool sweep (measured 2026-08-26)

TANGO's in-loop routes are a **by-product of training**, not a plan for the pool it finally emits, so
there are two ways to get routes for the 2,000 molecules it hands us:

* **ARM 1 — reuse the in-loop `route_0.pkl`s.** Free; they already exist.
* **ARM 2 — re-run syntheseus over the emitted pool.** 3.5 h on one GPU for 2,000 targets.

Both were priced on the SAME pool so the route source is the only variable, at the primary R=100
readout, sEH seed 42:

| pool | arm | routed | modes @ R=100 | used_rxns | stop reason |
|---|---|---|---|---|---|
| naive N=500 | ARM 1 | 228 (46%) | 4 | 100 | budget-binding |
| naive N=500 | ARM 2 | 485 (97%) | 5 | 100 | budget-binding |
| pruned N=218 | ARM 1 | **7 (3%)** | 7 | **29** | **pool-exhausted — NOT comparable** |
| pruned N=218 | ARM 2 | 201 (92%) | **47** | 100 | budget-binding |

**Lead with ARM 2 on the pruned pool: 47 modes at 100 reactions.** The reason is not that ARM 2 finds
better routes — on the naive pool the two arms are within one mode of each other, because that pool is
too collapsed for coverage to matter. It is that **ARM 1's coverage is correlated with the collapse**.
TANGO solves routes in-loop for the molecules it is generating most of, which are the near-duplicates
in the high-scoring band; on the pruned pool — the mutually dissimilar molecules — ARM 1 covers 7 of
218. ARM 1 cannot spend even a 100-reaction budget there (`used_rxns` 29), so its 7 modes are a
pool-exhausted cell that must be flagged, never quoted as "7 modes at 100 reactions".

Two things to carry into the paper. First, ARM 2 costs 3.5 h/cell and is the only arm that makes
TANGO's pruned pool priceable at all, so it is the fair arm, not merely the flattering one. Second,
TANGO's whole above-gate set of 1,853 molecules contains only **218 distinct** at tau=0.5 — the same
Saturn-family collapse TANGO inherits from its generator — so even its best case (47) sits below
REINVENT's naive pool (58) and well below REINVENT pruned (71). Report the collapse as the finding;
do not let the arm choice hide it.

**Which inventory TANGO gets: ZINCFrag, the 200k-SMILES ZINC-derived set S3-GFN was given.** The
first choice was "whatever stock MultiAiZ prices against", for commensurability — but that stock
(`data/models/aizynthfinder/zinc_stock.hdf5`, 17.4M rows) stores **InChI keys, not SMILES**, because
AiZynthFinder matches stock by hash. syntheseus needs an `inventory_smiles_file`, and an InChI key is
not invertible, so that file **cannot** be handed to TANGO at all. ZINCFrag
(`external/s3gfn/data/building_blocks/zincfrag.smi.gz`, 200,000 SMILES) is the largest SMILES
inventory we hold, is ZINC-derived, and is exactly what S3-GFN was given — so it keeps the
cross-entrant consistency argument nearly intact. (A stereochemistry-stripped variant of the same set
exists at `external/s3gfn/data/envs/zincfrag_hb105/building_block.smi`, 178,622 SMILES; stripping
stereo makes inventory matching *easier*, so it is the lever to pull if match rates come back too low
to measure.) The intuition from
S3-GFN — "use small fragments" — does not transfer, and reasoning by analogy from it is the trap here.
S3-GFN *constructs* molecules by combining blocks, so its blocks must be small and combinable.
TANGO does the opposite: Saturn generates freely and the retrosynthesis model *decomposes* down to the
inventory. TANGO's `building_blocks_file` is therefore the analogue of **AiZynthFinder's stock**, not of
S3-GFN's fragment library. Two consequences point the same way:

1. **It is TANGO's best case.** A larger inventory means more terminals, shorter routes and more solved
   molecules; a 418-block inventory would be a brutal constraint on a general SMILES generator, and
   most of its reward would come from the partial-credit term rather than real routes.
2. **It is the most defensible available.** Pricing is only commensurable if "purchasable" means
   roughly the same thing for every entrant. Exact parity is impossible here — MultiAiZ matches the
   17.4M-key ZINC stock by InChI key while syntheseus needs SMILES — so ZINCFrag is the closest
   honest match: same provenance, same set S3-GFN builds from. **State the asymmetry rather than
   implying parity:** TANGO's inventory is a 200k subset of the stock MultiAiZ's routes may terminate
   in, which makes TANGO's constraint *harder* than the route-less entrants' and therefore flatters
   them, not us.

**MEASURED 2026-08-20, and it overturns the choice above: ZINCFrag does not work as a retrosynthesis
inventory.** The smoke solved **0 of 50** Saturn molecules, **0 of 42** REINVENT molecules, and 0 of 4
textbook targets (aspirin, paracetamol, caffeine, biphenyl), using a median of only **5 reaction-model
calls** per molecule — the search empties its frontier instantly rather than exhausting its time
budget. The reaction model is not at fault: called directly it returns textbook disconnections
(aspirin → acetyl chloride + salicylic acid; biphenyl → bromobenzene + phenylboronic acid). The
inventory is. ZINCFrag holds **4 of 11** common commodity reagents — salicylic acid, acetic acid,
acetyl chloride, bromobenzene, aniline, phenol and toluene are all **absent** — and its members have
median MW 184 / 12 heavy atoms.

The error in the reasoning above was treating inventory choice as a question of *size* when it is a
question of *kind*. ZINCFrag is a **combinatorial fragment set** curated for S3-GFN to assemble
molecules *from*; trivial reagents are useless as assembly fragments, so they were excluded. A
retrosynthesis inventory needs exactly those trivial reagents, because they are what routes terminate
*on*. Large and wrong-kind beats nothing, but not by much: an inventory missing bromobenzene cannot
terminate a Suzuki coupling.

**RESOLVED by using the authors' own stock (2026-08-20).** Their reproduction config
(`external/saturn/experimental_reproduction/constrained_synthesizability/experiment.json`) names
`frag-reac-zinc-stock.smi`, published with the paper at
[figshare 27225483](https://figshare.com/articles/dataset/TANGO_paper_building_blocks_and_code-base/27225483).
It is **17,721,980 SMILES** — the ZINC purchasable set, essentially the same scale as the 17.4M-key
`zinc_stock.hdf5` MultiAiZ prices against, but in the SMILES form syntheseus needs. It contains
**11 of 11** probe commodity reagents where ZINCFrag had 4. So the authors' default and our
commensurability requirement coincide, and no deviation from the other cells is needed after all.

**What the authors actually run** (their config, mirrored for our cells):

| parameter | authors' value | note |
|---|---|---|
| `reaction_model` | **megan** | not RootAligned, which the first smoke used |
| `building_blocks_file` | `frag-reac-zinc-stock.smi` | 17.7M, the general stock routes terminate on |
| `enforced_building_blocks_file` | `enforced_stock_{5,10,100}.smi` | the *constraint* set, separate from the stock |
| `time_limit_s` | 180 | per molecule |
| `budget` | 10,000 | same PMO convention as REINVENT/Saturn/SynFormer |
| `use_dense_reward` / `reward_type` | true / `tango_fms` | weights: tanimoto 0.5, fg 0.5, fms 0.5 |
| `enforce_blocks` / `enforce_start` | true / false | intermediate constraint, not starting-material |
| `parallelize` / `max_workers` | false / 4 | |

**MEASURED: the authors' `num_top_results: 1` cripples their own search.** That field does double
duty in syntheseus - it is both "how many routes to return at the end" and, via
`get_model_fn(config, default_num_results=config.num_top_results)` (`cli/search.py:279`), **how many
disconnections each expansion requests from the reaction model**. At 1 the search explores a single
chain, so one bad top-1 prediction ends it. TANGO's config comment ("if 1 solution is found,
Syntheseus stops") describes the first meaning and appears unaware of the second. Measured on 50
REINVENT sEH molecules, authors' stock + MEGAN + `time_limit_s` 180:

| `num_top_results` | solved | median rxn-model calls | wall / 50 mols | per 10,000-call cell |
|---|---|---|---|---|
| **1** (authors' config) | 10/50 = **20%** | 5 | 28 s | **~1.6 h** |
| **5** | 47/50 = **94%** | 198 | 450 s | **~25 h** |

So TANGO is affordable either way - the feared 180 s/molecule worst case does not materialise, and even
the generous setting lands near SynFormer's ~36 h/cell. **Run at 5, not the authors' 1.** This is the
same doctrine already adopted for catalogues in [[competitor-catalogue-fairness]]: where a baseline's
shipped default demonstrably handicaps it, give it its best case and disclose the deviation, because
publishing a 20% solve rate obtained under a setting we could see was crippling would read as
sandbagging. State the measurement and the choice in the write-up.

**Two consequences worth acting on.**

1. **The 418-block arm has a natural home, and it is the authors' own mechanism.** TANGO separates the
   general stock (where routes *terminate*) from the *enforced* blocks (which must *appear* in the
   route). Their arms vary the enforced set at 5 / 10 / 100 blocks. So "constrained to our chemistry"
   is simply `enforced_building_blocks_file = our 418 glue blocks`, with their stock unchanged —
   no hack, and directly comparable to their published arms.
2. **Their own experiment docks against 7UVU**, which is the same human ClpP structure our ClpP cells
   use (bar −8.0, AUROC 0.895, Logs/045). Their objective is QuickVina2-GPU on 7UVU × QED ×
   syntheseus. So running TANGO on our ClpP cell reproduces the authors' setup almost exactly — an
   independent argument for keeping ClpP when 6TD3 was paused.

**Environment gotchas, all fixed in the dedicated `syntheseus` env** (none affect results): numpy
pinned `<2` for the pandas ABI; `gcc_linux-64` for Triton's runtime compiler; a `sitecustomize.py`
restoring `torch.load(weights_only=False)` so OpenNMT-pickled checkpoints load under torch ≥ 2.6;
`graphviz` because syntheseus plots 5 routes per target by default and TANGO's config writer never
overrides it; and a patch to `megan.py` whose log cleanup races and raises a **fatal**
`OSError(39) Directory not empty` *after* the model has loaded (backup at `megan.py.orig`).
MEGAN also writes a `logs/` directory into the **current working directory**, so every invocation must
run from a scratch cwd — it landed in the repo root once.

**The superseded option, for the record.** The
17.4M-entry `zinc_stock.hdf5` is the right *kind* but stores InChI keys, which are hashes and not
invertible. Two ways forward, in preference order: (1) download a purchasable set — ZINC20 in-stock or
eMolecules building blocks — on the login node, which is the clean fix and gives one honest definition
of "purchasable"; (2) harvest the leaf molecules from our cached MultiAiZ route artifacts, which are by
construction SMILES from the same ZINC stock the route-less entrants are priced against — cheap and
perfectly consistent, but **circular for a benchmark**, since it hands TANGO exactly the terminals a
competing planner already found. Prefer (1).

**Timing is not yet known.** The observed 6.8 s/molecule is an artifact of searches dying after ~5
calls; with a working inventory they will run until solved or timed out, so the real figure is
somewhere between that and the 180 s/molecule cap, i.e. between ~19 h and ~330 h per 10,000-call cell.
**Re-measure before committing any TANGO cells to the grid.**

The **418-block arm still runs, second, and is labelled for what it is** — the chemistry-homogenized
variant (cf. Logs/048), which forecloses "you gave the baselines a different catalogue" by making both
sides build from identical chemistry. It is *not* TANGO's headline: a failure there is as likely to mean
418 blocks do not span drug-like space as that TANGO underperforms.

### MultiAiZ — the competitor's route planner

Given molecules with no recipes, MultiAiZ runs a retrosynthesis search over the whole batch for
several cycles, each time adding discovered intermediates to the "you can buy this" list so later
molecules converge onto shared pieces. Measured cost: **~16 s per molecule, linear** (2.25 h for 500).
Our side pays **zero**, because our recipes already exist — that asymmetry is a result, and we report
it rather than trying to equalize it.

Two implementation details with real consequences:

- **Routes must be stitched down to real stock.** MultiAiZ ends recipes at its discovered
  intermediates and marks them purchasable. Priced naively they would be **free** and the entire
  convergence saving would read as zero, so we expand them into their build sub-trees.
- **Plan once, price many.** Route discovery used to re-run inside *every* pricing call. It is now a
  standalone step whose output is cached (`submit_multiaiz_discover.sh`), so sweeping SPARROW
  parameters costs seconds. The cache key is the **pool**, never the molecule: MultiAiZ is set-based,
  so routes depend on which other molecules were planned alongside them.

### Three correctness traps, all learned the hard way

**1. Shallow native routes must be recipe-expanded.** Our generators *attach* a promoted
dynamic-library fragment in one step rather than synthesizing it. Handed to SPARROW unexpanded,
SPARROW **buys** what count-once **builds** — the two then price different assumptions and the result
is meaningless. This produced a spurious 62.7% "disagreement" on DRD2 (entry `049`). Every native
route is now expanded via `expand_route_with_recipes`, which requires the run's fragment snapshot to
carry `smiles_to_route` recipes; the drivers abort if coverage is below 95%.

**2. A sampled row is not a candidate.** A GFlowNet re-samples the same molecule many times, so
`records.csv` rows are sampling *events*. The top-500 rows of the sEH run are only **115 distinct
molecules**. Any pool built from rows without deduplication is a fraction of its intended size.

**3. `n_scaffolds` and `median_reward` are TIE-BREAK-SENSITIVE, and on DRD2 that is not a small
caveat.** Greedy mode selection walks a hub's children in the order they appear in
`enum_children.json` and takes the first of any equally-good candidates. So two enumerations of the
*same chemistry* in a *different order* select a marginally different set — same count, same cost,
slightly different membership. Measured 2026-08-24 by re-enumerating `rgfn_drd2` (all three seeds) to
add reactions:

| field | s42 | s43 | s44 |
|---|---|---|---|
| `reactions_per_mode` | 1.197 → **1.197** | 1.310 → **1.310** | 1.243 → **1.243** |
| `case1_modes_at_100rxn` | 82 → **82** | 84 → **84** | 89 → **89** |
| `n_scaffolds` | 267 → 266 | 284 → 283 | 270 → 268 |
| `median_reward` | 0.915 → 0.917 | 0.929 → 0.928 | 0.883 → 0.881 |

The chemistry was verified identical, not assumed: same 200 hubs, same 168,006 distinct children, **zero
hubs whose child set differs** — while child *order* differed in **200/200** hubs.

**Why it bites DRD2 and not sEH, quantified:** DRD2's oracle is a saturated classifier, so **51.6%** of
enumerated children share a reward with another child (top value repeated **×302**), against **30.4%**
on sEH (top value ×6). `rgfn_seh`'s summary came out bit-identical under the same reordering because it
had almost no consequential ties.

**THE BAND IS MEASURED, so quote it rather than hedging.** `tiebreak_band.py` shuffles children WITHIN
each hub — identical hubs, identical children, identical rewards, different order — and re-runs the
campaign. Six shuffles on each of four docking cells (24 campaign runs, 2026-08-25):

| cell | `reactions_per_mode` | modes@100rxn | edge (via r/m) | edge (via modes) |
|---|---|---|---|---|
| `rgfn_clpp` s43 | **0** | 65–66 (**±1**) | 2.2717× **±0** | 2.321–2.357× (±1.5%) |
| `rgfn_6td3` s42 | **0** | 64 (**0**) | 2.3610× **±0** | 2.4615× **±0** |
| `scent_clpp` s42 | **0** | 79 (**0**) | 2.9306× **±0** | 2.9259× **±0** |
| `scent_6td3` s42 | **0** | 80 (**0**) | 2.9991× **±0** | 2.8571× **±0** |

**The published edge — computed from `reactions_per_mode` — has EXACTLY ZERO tie-break sensitivity on
all four cells.** So do `total_reactions`, `total_modes`, `case2_reactions_at_300modes` and
`distinct_hubs_used`: every one identical across all 24 runs. Only `case1_modes_at_100rxn` (±1, and on
just **one** of four cells) and `n_scaffolds` (0–5) move at all.

**How to apply.** This is a **footnote, not an error bar** — ±1 mode on one docking cell is an order of
magnitude below seed variance (median CV 3.2%), so it does not belong beside seed error bars and it
threatens no cell-to-cell difference we treat as real. Concretely: `reactions_per_mode`, mode counts,
`total_reactions` and `distinct_hubs_used` are immune, and the headline is safe. Quote
`case1_modes_at_100rxn` for a docking cell as ±1. Do not quote `n_scaffolds` or `median_reward` for a
docking or DRD2 cell to better than ±5 / ±0.005, and never read a small change in them across two runs
as a seed or method effect — it can be pure tie-break arbitrariness. Both appear in eight analysis
scripts, so any claim leaning on them needs this bound stated.

A worked instance of the trap: re-enumerating `rgfn_clpp` s43 to add reactions moved
`case1_modes_at_100rxn` 66 → 65 and the repair-verifier called it a regression. Three of six shuffles
returned each value, so 65 was simply a draw from the band.

### How much room SPARROW actually has

SPARROW optimizes over the **whole** merged graph automatically — `build_network` unions all routes
and deduplicates compounds by canonical SMILES, so shared intermediates collapse into one node and
alternative producers become alternative reaction nodes. The limit is solver size, not expressiveness.

But the *ceiling* differs sharply by side, and it does not favour us:

| side | alternative recipes per molecule |
|---|---|
| competitor (MultiAiZ) | ~16 |
| ours (native routes) | **1.07** — only 6.3% of molecules can be made more than one way |

So the optimizer has far more freedom on the competitor's side. Giving it real route choice on ours
would need multi-trajectory logging at sample time (a GFlowNet reaches the same molecule many ways,
but `routes.json` keeps one recipe per product) — a re-sample, not a config change. Open future work.

**Practical limit:** the MILP is superlinear in pool size — ~1 s per budget point at 500 targets,
>110 s at 2,000 — so "give SB as large a pool as possible" is bounded by solve time, and a pool that
fails to solve is itself a reportable datapoint.

---

## Paper target

Realistic tiering (revised down from an earlier "NeurIPS primary / Nature stretch" framing to match what an in-silico-only result can actually clear; see the project's venue-strategy notes):

- **Fast / first** — an ML workshop: **NeurIPS AI4Science**, **ICLR MLDD**, or **GEM**. Low-risk, quick, citable; most are non-archival, so a fuller journal version can follow. Best way to leave the lab with a result locked in.
- **Primary journal** — **Digital Discovery (RSC)**: the best fit for a rigorous in-silico methods+application paper without wet validation. **Journal of Cheminformatics** is a strong alternative if we lean on the reusable-method/codebase framing.
- **Stretch** — **JCIM**: achievable *with* ≥2 validated systems, clean/transparent data splits, recovery of known glues, and strong baselines including a non-synthesizable generator.
- **Out of scope without wet-lab** — NeurIPS/ICLR **main track** and **Nature-family**. The competitive ML tracks and the Nature tier effectively require a synthesized, tested compound.

**What reviewers will ask:**
- Does the oracle generalize beyond one system? (Need 2+ validated systems.)
- Does RGFN actually generate better molecules than random sampling / a non-synthesizable baseline? (cf. `[bengio2021gflownet]` Fig. 7 — GFlowNet beats random acquisition in the multi-round setting; we need the analogous plot.)
- Can you show the neosubstrate differential specifically matters (ablation vs. Tier 2 absolute score only)?
- How do you position against existing **conditioned** glue generators, and is "synthesizable" doing real work here? (Our answer: reaction-grounded synthesis routes, not merely valid/drug-like molecules.)
- (Top-tier only) Did you synthesize and test any generated molecules?

---

## Current project status

A list of objectives for our project. Tiers: **MVP** (minimum publishable result), **Target** (the full glue story), **Stretch** (upside). Check items `[X]` as they land; blocked items carry a ⚠️.

### Objective 0 — Oracle validation *(Foundation)*
- [X] Validate the docking oracle on **6TD3**: 78-pp separation between known glues and warhead-matched decoys on the DDB1 neosubstrate differential — the validated testbed for RGFN. (exp `002`)
- [X] Confirm the **neosubstrate differential** (Tier2 − Tier1, same pose) as the discrimination metric. **The specific signal is the *Vina* Tier2 − Tier1 differential.** Did six-way ablation and validated again with MW matching. (exp `006`, `007`)
- [X] Stand up the batched **gnina** pipeline on Balam (16 workers, 4× A100; ~14 min / 400 molecules). (exp `004`)
- [ ] ⚠️ **CRBN / 5HXB docking oracle** — blocked at a ceiling (−3 pp; docking can't see CRBN's PPI-driven recognition). Not usable as-is; revisit via MD (Objective 3) rather than sinking more time into box-docking tweaks. (exp `001`, `003`)
- [ ] Validate the oracle on **≥1 additional system** from MolGlueDB (generalization evidence the journals require).
### Objective 1 — First end-to-end RGFN run *(MVP)*
- [X] Wire the glue oracle into RGFN's reward interface (`glue/` package → `[koziarski2024rgfn]` proxy/reward API). (exp `009`)
- [X] Build the seed dataset `D_0` and **warm-start the proxy** (`[bengio2021gflownet]` Alg. 1 init) — 408 labels, proxy val MSE 0.243. (exp `009`)
- [X] Run RGFN through the **active-learning loop** on the validated 6TD3 oracle. Multi-round loop (3 rounds) completed on a compute node with the query-batch docking intact (exp `011`, job 69445): generated molecules dock as real glues (median ΔT2−T1 ≈ −2.4, 61/96 ≤ −2.0). Inner-loop learning was first confirmed at 1 round (exp `009`); the login-node CPU cap that killed earlier query-batch docking is gone on compute nodes. A **fast GPU-docking oracle for sEH** also exists (`DockingSEHOracle`, exp `010`, 0.60 s/mol over 300 molecules) for driving the loop on the standard RGFN benchmark target via `experiments/active_learning/seh/submit_al_seh.sh`.
- [ ] **Instrument oracle-call counting from the very first run** (cannot be reconstructed later).
- [ ] Produce the **top-k-vs-oracle-calls** curve with a **random-acquisition baseline**.
### Objective 2 — Reward design & ablations *(Target)*
- [ ] Assemble the multi-objective reward: differential + QED (+ synthesizability comes free from RGFN).
- [ ] Ablation: **differential vs. Tier-2-absolute-only** — does the cooperativity term actually matter?
- [ ] Ablation: contribution of each reward component / building-block set.
- [x] Ablation: **pose selection — CNN vs. Vina** — **DONE** (exp `008`, Balam job 69443): CNN pose selection wins, AUROC **0.946 → 0.795** under Vina selection (Cohen's d 2.38 → 0.95; rules agree on only 41%/18% of known/decoy poses). Go/no-go on gnina resolved **in favour of keeping gnina** — the CNN has a load-bearing role as pose picker, so the 6TD3 oracle still needs a GPU-docking-**plus-CNN-rescore** path (not pure QuickVina2-GPU) to run multi-round.
### Objective 3 — Beat the CRBN ceiling with MD *(Target)*
- [ ] Test **MD stability** as oracle `O` for CRBN-type systems where docking plateaus.
- [ ] If MD discriminates, fold it in as the expensive `O` with a learned proxy `M` (multi-fidelity loop).
### Objective 4 — Generalization & baselines *(Target / journal bar)*
- [ ] Drive **≥2 systems** end-to-end through generation (not just oracle validation).
- [ ] Add a **non-synthesizable baseline generator** (e.g., graph GFlowNet / REINVENT) on the *same* oracle and budget. **FragGFN** (Recursion `gflownet` fragment env) implemented + locally validated through the AL loop on the same proxy/oracle/seed/budget as RGFN (exp `015`); GPU docking run pending.
- [ ] **≥3 seeds** per headline result, reported with error bars.
### Objective 5 — Evaluation suite *(Target)*
- [ ] **Synthesizability**: SA distribution + by-construction route advantage vs. the non-synthesizable baseline (the headline differentiator).
- [ ] **Diversity** (internal + scaffold) under a fixed oracle budget.
- [ ] **Recovery** of known glues / glutarimide chemistry (retrospective enrichment).
- [ ] **Anti-gaming**: in-loop-proxy vs. high-fidelity-oracle correlation on top-k; medchem sanity (PAINS, MW, logP).
- [ ] **High-fidelity validation** of top-k (co-folding / Boltz-2 / short MD).
### Objective 6 — Positioning & release *(always-on)*
- [ ] Write the **novelty paragraph** vs. the conditioned JT-VAE glue generator (reaction-grounded synthesizability + goal-directed sampling, not a conditioned VAE).
- [ ] Keep **code/data release-ready**: pinned env, fixed seeds, dataset provenance; archive every run's full output.

---

## Experiment log index

Chronological record; the objectives above cite these by number. Full entries in `../Logs/`.

| # | Date | Title | Verdict |
|---|------|-------|---------|
| [001](../Logs/001_5hxb-crbn-anchored-docking.md) | 2026-06-11 → 06-18 | 5HXB / CRBN — warhead-anchored docking oracle + decoy control | Works as a docker; **discrimination ceiling** — GSPT1 bonus is a geometric artifact, not a glue signal |
| [002](../Logs/002_6td3-cr8-validation-and-discrimination.md) | 2026-06-18 | 6TD3 / CDK12-DDB1 — oracle validation + discrimination run | **Decisive discrimination** — 85.6% vs 7.3% on DDB1 differential (+78 pts); validated oracle |
| [003](../Logs/003_crbn-vs-6td3-cross-system.md) | 2026-06-18 | 5HXB vs 6TD3 — head-to-head neosubstrate differential comparison | **6TD3 discriminates (+78 pts); 5HXB does not (−3 pts)** — failure is structural, not methodological |
| [004](../Logs/004_compute-benchmark.md) | 2026-06-18 | Compute benchmark — login node vs Balam debug_full_node (4× A100) | **CRBN 21× / 6TD3 6.6× faster; batching is the dominant lever** — conformer embedding is the new bottleneck |
| [005](../Logs/005_tier2-vina-roc-pr-curves.md) | 2026-06-23 | Tier 2 Vina — ROC and PR curves for 6TD3 and 5HXB | **6TD3 AUC=0.890 / AP=0.872; CRBN AUC=0.627** — absolute Tier 2 is a strong 6TD3 oracle; CRBN ceiling confirmed structural |
| [006](../Logs/006_6td3-violin-distributions.md) | 2026-06-25 | 6TD3 — which metric discriminates best (Tier 1 vs Tier 2 vs Δ, Vina vs CNN) | **Vina ΔT2−T1 wins (AUROC 0.946)**; Vina Tier 1 worst (0.691) — the signal lives in what DDB1 adds |
| [007](../Logs/007_6td3-molecular-weight-control.md) | 2026-06-25 | 6TD3 — controlling glue-vs-decoy discrimination for molecular weight | **Differential survives MW-matching (0.95→0.87); absolute scores collapse (Vina Tier 1 → 0.38)** — it isn't reading ligand size |
| [008](../Logs/008_pose-selection-cnn-vs-vina.md) | 2026-06-26 | 6TD3 — does pose selection (CNN vs Vina) change discrimination? | **CNN pose selection wins: AUROC 0.946 → 0.795 under Vina** (Cohen's d 2.38 → 0.95; rules agree 41%/18% known/decoy). gnina **stays** — CNN pose-picking is load-bearing; oracle needs GPU-dock + CNN-rescore, not pure Vina |
| [009](../Logs/009_first-rgfn-inner-loop-learning.md) | 2026-06-25 | 6TD3 / RGFN — first end-to-end active-learning run (1 round, inner-loop learning) | **Generator learns** — loss ↓24×, mean predicted ΔT2−T1 0→−0.62, stays diverse; but best plateaus short of real glues (no scaffold <−2.0) and QED drifts down 0.40→0.17. Query-batch docking killed by login-node CPU cap — no true labels yet |
| [010](../Logs/010_seh-gpu-docking-oracle.md) | 2026-06-26 | sEH — fast GPU-docking oracle (`DockingSEHOracle`) for the active-learning loop | **Works and is fast** — QuickVina2-GPU sEH dock at **0.60 s/mol** over 300 molecules (~6× faster than a tiny batch), reproduces aspirin −6.3; thin wrapper over RGFN's own docking proxy, wired into the loop with the MPNN proxy + a 250-molecule RGFN-generated seed. Unblocks the multi-round loop the CPU-bound glue oracle couldn't finish |
| [011](../Logs/011_6td3-multiround-al-true-labels.md) | 2026-06-26 → 06-27 | 6TD3 / RGFN — first multi-round AL loop with true oracle labels (3 rounds, compute node) | **Generated molecules dock as real glues** — median ΔT2−T1 ≈ −2.4 (in known-glue range), 61/96 ≤ −2.0, best −4.7; docking step intact (no CPU cap). Uses the **CPU two-tier gnina oracle** — the pre-GPU baseline that entries 012→013→014 build on. Cost: **GFN training 65% + docking 35%** of 5 h wall-clock (the 35% docking cost is what motivated the GPU-docking arc), proxy fit + sampling <0.5%. Fixed an inverted `top_k` deliverable. Size drift persists |
| [012](../Logs/012_pre-gpu-docking-substep-timing.md) | 2026-06-28 | 6TD3 / RGFN — pre-GPU-exploration: instrumented docking sub-step timing baseline (job 69451) | **Tier-2 search IS the docking cost: `tier2_dock` = 99.3%** of the docking phase (58 s/mol), embed 0.6% / rescore 0.2% / pose-pick ~0%; sub-steps sum to `oracle_score` exactly. Scales with molecule size (53→70 s/mol). ⇒ Tier-2 search ≈ 33% of whole loop = GPU-dock speed-up ceiling. Embed bottleneck worry (exp 004) refuted |
| [013](../Logs/013_gpu-pose-gen-vs-gnina-search.md) | 2026-06-29 | 6TD3 — GPU pose generation (QuickVina2-GPU) vs gnina CPU search, discrimination held fixed | **GPU search keeps most of the signal at ~50× speed** — swapping only the Tier-2 search to GPU (QV2-GPU `num_modes=9`, gnina CNN pose-pick + two-tier gnina `--score_only`) gives AUROC **0.88** (0.875 compute / 0.892 login) vs the **0.948** same-data gnina-search control (≈006's 0.946): −0.06 AUROC for **1.1 vs 58 s/mol**. New reusable `Docking6TD3GpuOracle`. Also: QV2 OpenCL is wedged on balam008 (`clCreateContext -5`) but fine on balam009 → submit script excludes it + gates on an OpenCL health check |
| [014](../Logs/014_gpu-oracle-al-loop-suggestion-logging.md) | 2026-07-01 | 6TD3 / RGFN — GPU-oracle active-learning loop + standard candidate-dataset logging (job 69517, **complete**) | **First full GPU-oracle AL loop; found+fixed a GPU-memory-contention bug.** 3 rounds, \|D\| 408→483, molecules dock in known-glue range (per-round median −2.0…−2.4, best −4.92, matching entry 011), maximally diverse (32/32 modes, novelty 1.0), heavy/non-drug-like (MW ~650, QED ~0.12 — size drift persists). Docking collapsed to ~1 min/round (<2% of loop) vs entry-012's ~31 min (33%). **Root cause of 3 failed attempts: torch's caching allocator starves the QV2-GPU docking subprocess (40 GB→1.1 GB free → 0 poses); `torch.cuda.empty_cache()` before docking fixes it.** New standard candidate format (`glue/datasets/candidates.py`) + `SuggestionLog` (96 candidates + routes + per-round metrics); + pre-flight dock gate, all-NaN abort guard, manifest provenance |
| [015](../Logs/015_fraggfn-6td3-baseline.md) | 2026-06-29 | 6TD3 / FragGFN — non-synthesizable fragment-GFlowNet baseline through the active-learning loop | **Ran end-to-end on the real GPU oracle (job 69482)** — non-synthesizable baseline docks as real glues, competitive with RGFN: best −4.86, median −2.06, 54% ≤ −2.0, fully diverse, same MW/QED drift; `has_route=0`. ⇒ synthesizability (not glue score) is RGFN's differentiator. Now a **clean same-oracle head-to-head**: the matched RGFN-GPU run has since completed (entry 014, job 69517), so FragGFN-GPU is compared against RGFN on the identical `Docking6TD3GpuOracle` (best −3.90 / median −2.14 / 44%), not the earlier CPU-oracle RGFN run (entry 011) |
| [016](../Logs/016_rxnflow-6td3-baseline.md) | 2026-06-30 | 6TD3 / RxnFlow — synthesizable reaction-template+block GFlowNet baseline through the active-learning loop | **Ran end-to-end on the real GPU oracle (job 69518, 51 min)** — the *synthesizable* peer to RGFN (vs FragGFN's non-synth foil): RxnFlow (`[seo2024rxnflow]`, ICLR 2025) through the same loop/oracle/seed/budget/β/proxy, isolating the generator's action space. 96 candidates, best −4.22, median −1.19, 31% ≤ −2.0, all synthesizable (`has_route=96/96`, 2-step). **Key finding:** the only entrant that is both synthesizable *and* drug-like (MW 489 / QED 0.36 vs RGFN's 665 / 0.12) — RGFN's synthesizability is real but doesn't buy drug-likeness; a block-constrained generator gets it free. Clean matched-oracle **three-way** with RGFN (69517) + FragGFN (69482). Adapter mirrors FragGFN (own `rxnflow` py3.12 env), bridge now **route-aware** (`--routes` → `has_route=1` + `routes.jsonl`) |
| [017](../Logs/017_scent-6td3-baseline.md) | 2026-06-30 | 6TD3 / SCENT — cost-aware template GFlowNet baseline through the active-learning loop | **Ran end-to-end on the real GPU oracle (job 69513, 2 h 03 m)** — SCENT's *cost-aware* generator (`[gainski2025scent]`, an RGFN fork w/ Recursive Cost Guidance + Exploitation Penalty + Dynamic Library) matches RGFN/FragGFN on glue quality while staying fully synthesizable: 96 candidates, median dvina **−2.12**, best **−5.81** (strongest of any entrant), 51% ≤ −2.0, **`has_route=96/96`** (tree routes via the dynamic library). Notable: cost-awareness appears to **counteract the size drift** — MW falls 694→579, QED rises 0.11→0.21 across rounds (opposite of RGFN `011`/FragGFN `015`). Same loop/oracle/seed/budget/β/proxy; own `scent` env (namespace clash), gin-driven, route-aware bridge. Bring-up needed 3 fixes (setuptools<81, explicit `Trainer` import, sign-safe `train_metrics`). RGFN-GPU anchor (entry `014`, job 69517) now complete → clean matched-oracle **four-way** (RGFN/FragGFN/RxnFlow/SCENT); SCENT + RxnFlow are the two drug-like entrants |
| [019](../Logs/019_fixed-reward-pipeline-seh-fourway.md) | 2026-07-02 | Fixed-reward (single-shot) pipeline mirroring the RGFN paper + matched four-way sEH benchmark (RGFN/FragGFN/RxnFlow/SCENT) | **Pipeline built + validated; RGFN done, four-way in flight.** New `glue/fixed_reward/FixedRewardPipeline` (no AL loop, no oracle — train once vs a fixed reward generator) + baseline single-shot runners, all trained vs the *same* frozen Bengio-2021 sEH proxy (β=8, 5000 steps). RGFN fixed-reward COMPLETE (job 69563, 14h38m): 1000 unique synthesizable candidates, sEH reward mean 7.0/max 8.3, MW 535/QED 0.26 (size drift persists); AiZynth path validated (top-5 = 4/5 routes). FragGFN OOM→chunked-sampling fix, RxnFlow/SCENT timeout→24h walltime, all re-queued (69606/07/08); RGFN-on-docking (test #2) hit an upstream QV2-GPU no-pose failure. Four-way AiZynth table pending baselines |
| [020](../Logs/020_stdlib-fourway-seh-fixed-reward.md) | 2026-07-02 → 07-06 | sEH — same-library four-way fixed-reward (RGFN/SCENT/RxnFlow share `glue_standard_v1`; FragGFN native foil) | **COMPLETE — the library was not entry `019`'s confound.** New `glue.chemistry.ChemLibrary` + `GlueReactionDataFactory` + canonical `data/libraries/glue_standard_v1` (SCENT SMALL: 418 frags/112 rxns, priced) + retroactive cost evaluator (`validation/harness/cost.py`). All 4 ran on the shared lib (RGFN 69616 / RxnFlow 69614 / SCENT 69608 / FragGFN 69606 foil); AiZynth+cost table job 69867. On identical chemistry the three synthesizable generators separate, not converge: **SCENT** best sEH (7.63) + cheapest routes (2.9, w/ 74% dynamic-lib fallback caveat) + best AiZynth (0.71); **RxnFlow** most drug-like (QED 0.62, MW 413) but lowest reward (6.22) and lowest AiZynth (0.29 despite has_route=1); **RGFN** scores well, drifts largest/priciest (34). FragGFN foil: reward parity but AiZynth 0.02. ⇒ has_route=1 is not uniform; RGFN ~2×/iter slower on the larger library (metric audit: metrics only ~11% of per-iter, size-drift dominates). Results: `validation/results/seh_fixed_reward_stdlib/`. |
| [018](../Logs/018_synthesizability-metric-aizynthfinder.md) | 2026-06-30 | Synthesizability metric — AiZynthFinder + SA over the standard candidate dataset (Objective 5) | **Works end-to-end on Balam.** Post-hoc evaluator (`validation/harness/synthesizability.py`) that runs the same way on every entrant: AiZynthFinder route-found "success rate" (`[genheden2020aizynth]`; the metric RGFN/RxnFlow/SCENT all report) + RDKit SA distribution over the standard candidate-dataset format, with a by-construction-vs-independent route cross-check. AiZynth 4.4.1 in its own `aizynth` env (`external/setup_aizynthfinder.sh`); real API matched the code with no changes. Test set of 8 known glues + 3 anchors: **0.82 success (9/11)**, SA mean 2.50, 0 errors; ibuprofen's miss reproduces AiZynth's known conservatism. Ready to run across all generators' outputs → committed comparison table (next) |
| [021](../Logs/021_fixed-reward-docking-matrix.md) | 2026-07-03 → 07-06 | Fixed-reward 4-reward × 4-generator matrix — per-step GPU docking for EVERY generator (ClpP + 6TD3 differential) on the SMALL library | **LAUNCHED (400 iters) + AiZynth partial; 2 RGFN cells pending.** Benchmark (69691): cross-env per-step docking amortizes to ~10-13% at 400 iters → per-step `score_batch.py` bridge, no server. New reusable `glue.proxies.OracleRewardProxy` (any `GlueOracle`→GFN reward, `clip(-raw/norm)` + per-step `empty_cache`; the guard job 69567 lacked), `DockingClpPOracle`, baseline `DockingBridgeReward`/`DockingBridgeProxy`. Both mechanisms validated (smoke 69692/69693), then full matrix fired `submit_matrix.sh` (jobs 69695-69704). **8/10 emitted candidates; baseline per-step docking worked end-to-end.** Docking quality (dvina/Vina): SCENT strongest 6TD3 glue (med −2.51, best −5.81) > FragGFN (−2.17) > RxnFlow (drug-like, MW 379/QED .45 on ClpP); ClpP all ≥196/200 ≤−2. **AiZynth (69870/71/72):** FragGFN foil = 0.00 route-success everywhere; synth gens 0.2–0.61 (RxnFlow ClpP 0.61) — independent-route ≠ by-construction. **PENDING:** RGFN-6TD3 (timed out iter 260/400 → resumed 69868) + RGFN-DRD2 (69703) still training → blank rows in `validation/results/{6td3,drd2}_fixed_reward/comparison.md`; re-run aizynth per Logs/021 "▶ PICK UP HERE" to fill. Not committed (branch GPU-Dock). |
| [023](../Logs/023_oracle-call-curve-pilot.md) | 2026-07-06 | 6TD3 / RGFN — single-seed pilot of the oracle-call curve (RGFN vs random acquisition) | **Instrumentation works on the real oracle; RGFN already beats random.** New oracle-call counting + best-so-far trace (`glue/active_learning/acquisition_trace.py`) + a **random-acquisition baseline arm** (uniform-policy over the same reaction blocks, proxy/training off) for the Objective-1 top-k-vs-oracle-calls curve (`[bengio2021gflownet]` Fig. 7). Both arms COMPLETED on the 6TD3 GPU oracle (jobs 69945/69946): at 96 oracle calls RGFN improves the top-16 mean differential **−0.68** vs random's **−0.14** (≈5× faster/call), from a shared −3.209 seed baseline; both best stuck at −4.915 (D_0's CR8 purine — budget too small to beat it). Docking 1.6% of the loop (reproduces entry 014). Found+fixed a run-dir collision (concurrent same-config jobs shared a timestamped dir → now keyed by arm+seed). Curve validated end-to-end; ready for the 3-seed × 10-round campaign (`launch_curve_6td3.sh`). Not committed (branch GPU-Dock). |
| [024](../Logs/024_scent-backward-policy-recovery.md) | 2026-07-07 | SCENT — persist the trained backward policy (P_B) so it survives checkpointing / is recoverable for flow analysis | **Fixed + verified bit-exact.** Acid test found SCENT's `last_gfn.pt` saves only forward policy + logZ (131 keys, 0 backward) — the cost-guided P_B's two learned MLPs (`SimpleCostModel`/`BinaryDecomposableModel`, ~263k params each) live in a plain Python list on `JointlyGuidedBackwardPolicy`, which `state_dict()` skips, so reload lands on random weights (differs on 146/195 multi-parent steps). Fix: `guidance_io.py` sidecar (`guidance_models.pt`) wired into our `fixed_reward.py`/`al_loop.py` adapters (SCENT clone untouched). Login-safe round-trip verifier: with the sidecar, reloaded P_B is **bit-exact** (max \|ΔlogP_B\|=0.0 over 690 steps); without it, 146/195 C-phase steps wrong. **All 4 SCENT fixed-reward runs re-run with the patched adapter (70066 seh / 70067 drd2 / 70068 6td3 / 70069 clpp) — COMPLETE and confirmed end-to-end** (`check_trained_sidecar.py`: sidecar clean, trained P_B differs from a random reload by 7.6–12 nats max / carries real info, reload bit-exact deterministic). All 5 pre-fix SCENT checkpoints flagged on disk (`BACKWARD_POLICY_NOT_SAVED.txt`). Not committed (branch GPU-Dock). |
| [025](../Logs/025_lsdflow-phase1-hub-extraction-seh.md) | 2026-07-08 | sEH / RGFN — LSD-Flow phase 1: post-hoc hub extraction from the trained flow field | **Machinery built + validated end-to-end; batchable hubs exist, with severe-test caveats.** New LSD-Flow primitives (`glue/metrics/lsdflow_flow.py`+`uncertainty.py`, `glue/samplers/lsdflow/**` = flow recovery + `U(h)` + 6 hub-selection strategies) and analysis world (`validation/lsdflow/**` = RGFN adapter, canonical `HubDAG`, paper-comparable diversity + the count-once cost model, sampling harness), split across the two axes per `docs/LSD_FLOW_PROPOSAL.md`. Ran on the trained RGFN sEH stdlib model (entry `020`, job 69616, epoch 5001; **caught + rejected two cancelled 30/40-iter sibling stubs first**). On 10k sampled trajectories: 613/8183 hubs multi-child (max 9); hub-amortized selection is cheaper per mode than building independently — **but** 91% of multi-child hubs sit at the `max_num_reactions` boundary (forced stop), flow ranking is a near-tie with the `parent_of_topk` control on cost (choosing *which* hubs is a diversity story, not a cost one), and the flow-vs-visitation TB-integrity correlation is near zero (0.086). **Phase-2 addendum (job 70140, fresh 30k stereo-keyed DAG):** exhaustive `enumerate_children` fully fixed deeper-hub enumeration (all hubs 100% sampled-child recovery; the limitation was stereo-stripped reconstruction, not the max-depth boundary) and lands the reactions-per-mode amortization — costing one representative per mode (bounded by trajectory length), hub ≈1.0 vs independent = depth+1, so the saving is `~(depth+1)×`: depth-1 ~2×, depth-3 ~4× (depth-0 fragments free → no saving). Not committed (branch Hub-Analysis). |
| [049](../Logs/049_t44-cost-model-optimality-gap.md) | 2026-07-27 | T4.4 — the cost-model optimality gap ("what we leave on the table"), + why DRD2 can't be audited yet | **T4.4 ANSWERED: count-once is within 3.1% of provably optimal, and hub-batching is CLOSER to the optimum than best-candidate (3.1% vs 4.8%).** Handed the same molecules + same native recipes to SPARROW's MILP (independent global optimizer, status **Optimal** ⇒ a bound, not an estimate): sEH hub-batching **288 count-once vs 279 optimal = 9 reactions (3.12%)**, best-candidate 314 vs 299 (4.78%), 100 modes at 1.00 coverage. Kills the "you graded your own homework" objection and shows the flow-based selection sits nearer the achievable floor. **Tried a 2nd target (DRD2, via the merged matrix16 cell); the gate correctly REFUSED it (62.7%) — a missing INPUT, not a broken cost model.** count-once charges each promoted fragment's build; SPARROW can only charge it if given `smiles_to_route` recipes, and the DRD2 snapshot has **0/1600** (sEH's recipe-re-run snapshot, job 70180, has 1600/1600) → SPARROW *buys* what count-once *builds*. Visible because DRD2 hub-batching's 100 modes come from just **4 hubs, 97 off one depth-0 bought fragment**, so the un-expanded library is nearly pure final-couplings (steps to SPARROW: DRD2 106 vs sEH 505→286 distinct). Two wrong hypotheses recorded+rejected in the log (missing hub prefixes — 190/200 hubs ARE in routes.json; count-once over-charging). ⇒ **sEH 3.1% is the milestone number; DRD2 needs a recipe-carrying snapshot (a re-run, not a code fix) before any native-route SPARROW figure is quoted; the merged DRD2 count-once numbers are UNAFFECTED.** TODO: make the reconciler abort loudly on a zero-recipe snapshot. `results/scent_drd2_recon_reconcile/`. Post-merge tree `b7b72dd`; log not committed. **UPDATE 2026-08-04 — DRD2 NOW AUDITED, T4.4 is TWO-TARGET.** Ran the predicted native sample+enum (job 72145) off the recipe-carrying `scent_drd2/2026-07-10` checkpoint (100.0% recipe coverage, NO retrain) → gate PASSED: **DRD2 hub-batching 117 count-once vs 117 optimal = 0.00% gap (exactly optimal), best-candidate 364 vs 346 = 4.95%**, 100 modes at 1.00 coverage, status Optimal. So on BOTH targets hub-batching is nearer the floor than best-candidate, and on DRD2 it hits the global optimum exactly. **Quote T4.4 as a range: hub-batching 0.0–3.1% of optimal, best-candidate 4.8–5.0%.** (Fix that made 72145 run after 71800 crashed: pre-stage `external/scent/oracle/drd2_current.pkl` — compute nodes have read-only `$HOME` + no internet, so the TDC oracle's `./oracle` self-download fails.) `results/scent_drd2_native_reconcile/`. |
| [050](../Logs/050_matrix16-lsdflow-evaluation.md) | 2026-07-27 | Four generators × four targets — LSD-Flow hub-batching across the publication matrix + a severe test of the flow field | **Hub-batching wins on every reaction-grounded cell (1.2–3.2×) and the flow recovery passes a strong internal audit — but two internal signals don't mean what we assumed.** New manifest-driven 16-cell eval (`experiments/lsd_hubs/matrix16/`): uniform per-env `<gen>_worker.py --mode {sample,enumerate}` for all four generators, exhaustive one-step enumeration, count-once campaign. **6/8 surrogate cells done** (30k traj / 200 hubs): SCENT 1.303/1.160 rxn/mode vs best-candidate 3.383/3.673 (2.60×/3.17×, free_frag+K20; naive 1.22×/1.38×), RxnFlow 2.446/1.833 vs 3.000/2.935 (1.23×/1.60×). 16-point **gate sweep**: the edge grows as the bar loosens (rxnflow_seh 1.23→2.32×) while best-candidate is nearly gate-invariant — reproducing `035`'s built-in control; rxnflow_seh's weak bar-7 number is the gate starving the pool (only **1.2%** of 232k children clear 7.0), not a bug. **Severe tests:** (1) `Σ_x P_F(x|h)` = **0.999/1.000**, 0/200 hubs out of range → independent proof the enumeration is exhaustive AND recovered P_F is correctly normalized; (2) our shipped `pick_hubs` estimate of F(h) is nearly **unbiased** (+0.43/+0.28 nats vs a conservation-reconstructed `F_true`) but a poor **ranker** (ρ≈0.6) because its error spread ≈ the whole across-hub spread — explaining `025`'s flow-vs-control near-tie; (3) **`U(h)` is mostly forward-policy peakedness** (Var[logP_F] 214 vs Var[logF(x)] 23 for RxnFlow) and the *sampled* U(h) is **40–55× smaller** than the enumerated one and undefined for 114/200 hubs → **undercuts the planned UCB hub acquisition**; P_F-weighted `U_w` is the consistent fix (hub flow carried by only ~18–23 effective children of 1,161–2,195); (4) **flow conservation at hubs is violated on BOTH generators** — −6.05 nats on rxnflow_seh (200/200 hubs, ~430×) and **−14.03 on scent_seh** (198/200), i.e. *larger* where P_B is trained, so it is a property of the trained models, not of RxnFlow's heuristic backward policy; RxnFlow's learned `logZ` (53.07) is even *below* its own max molecule reward (63.69), so that anchor is unusable. RxnFlow hub ranking is **P_B-insensitive** (87.5% same hubs with the term ablated; only 8/1000 candidates have >1 parent) with SCENT as the working control (70.5%). Also 5 batch/scale bugs found+fixed (conda-before-emit, SLURM spool CWD, TOPK capping hubs, RxnFlow asserting `.block`, SCENT early-terminal DFS). **Update (same day):** the corrected **cap-6** FragGFN DRD2 cell landed (job 71856) and changes that row materially — best-cand 4.68 / hub **1.173** (**3.99×**) with **300/300** modes, versus cap-9's 8.0/3.98 (2.01×) where best-candidate could only assemble **19** modes: entry `046`'s collapse reproduced at library-assembly level. It also emitted the new `prefix_terms.csv` for free, giving the **first full-scale TB residual** (n=30,000): prefix−suffix median **−0.86** nats (sd 3.85) ⇒ a *local* P_F/P_B imbalance, not a global logZ scale error, and much healthier than the n=40 smokes (RxnFlow −3.62, SCENT +1.65). **Caveats:** FragGFN remains a cost-model **control** (attachments ≠ synthesis steps); `fraggfn_cap9_*` rows are the superseded model; cap-6 sEH not trained yet; RGFN cells (71766/71767) still queued. Merged `b7b72dd`; fixed post-merge `928dc48`. |
| [053](../Logs/053_hub-ordering-ablation.md) | 2026-07-30 | Hub-ordering ablation — is the flow sort load-bearing, or would any 200 scaffolds do? | **The flow signal is real but it picks the right NEIGHBOURHOOD, not the right rank — and on the two cost axes together every frontier point is flow- or reward-informed while both no-signal controls are strictly dominated.** Six arms over one fixed substrate (SCENT sEH `70189` / ckpt `2026-07-10_17-28-06`, the only sEH run with `smiles_to_route`), changing ONLY which 200 hubs hub-batching may walk and in what order; everything else — model, sampled pool, cost model, `free_frag`+K20, 200-hub budget — held fixed. At τ=7 / cutoff 0.5 / 300 modes: `flow_top` (all 20,874 hubs, flow ↓) **1.190 rxn/mode** < incumbent 1.217 < `cand_order_fixedset` 1.263 < `cand_order` 1.297 << **`random` 1.820 (1.53×)** << **`flow_bottom` 2.05 — which CANNOT reach 300 modes at all** (pool-exhausted at 279, 108 hubs walked, and the MOST compute of any arm) << best-candidate 3.097. **Reversing the sort breaks the method; randomising it costs 1.5×; but the three good-hub arms sit within 9% of each other**, so the claim to make is that flow finds the batchable region, not that it ranks within it. **Pareto**: reactions and measured compute trade off — `flow_top` 357 rxn / 5,648 s vs `cand_order` 389 rxn / **2,481 s** (2.3× less compute for +9% reactions); `random` is dominated on BOTH axes by `cand_order`, `flow_bottom` by everything. **Why best-candidate order does so well** (`hub_rank_overlap.py`): it never reads flow, yet its 200 hubs sit at median rank **546/20,874** (top 2.6%, 63% in the top-1000) vs 11,043 for random (uniform null 10,437) — sampling routes trajectories through high-flow hubs, so "parents of the best molecules" is a flow proxy; the three good arms agree *because they select the same region by different routes*. Correlational (high-flow hubs also carry high-reward children) — clean test noted in the log. Mechanism: flow prefers shallow hubs (11 depth-0/150 depth-1, 1.08M children) — more saving per hub, far bigger enumeration bill — while the arm enumerating FEWEST children (`flow_bottom`, 283k) is the one that fails. **Pool-limitation is itself monotone in flow quality** (flow arms fail <0.40, random <0.45, reverse <0.55) and the flow arms' 0.30/0.35 failure reproduces `052`'s strict corner. All arms converge to ~1.04× by cutoff 0.90, where loose distinctness stops making hub quality bind — report the crossover, don't truncate the sweep. Also settled the substrate audit (5 SCENT sEH runs; `07-01` crashed, `07-02` P_B irrecoverable, `07-07` + `seed42` recipe-less) and established that `log R = 8·reward` dominates `F_hat` (policy term ~9 nats ≈ 1.1 reward units; Spearman(flow, best-child reward) = 0.76). `pick_hubs.py` gained `--pool/--order/--seed/--restrict-to`, default path verified byte-identical. 16 `debug` slices / ~9.2 h; 402 of 1,000 arm-hub slots served from cache. Caveat: n=1 model, 1 target, 1 random draw. **Incidental find worth following up:** the budget is 300 MODES (the 200 hubs are an enumeration *pool* — completing arms consume only 17-52% of it), yet pool size still leaks into the result because `rank_fragments` picks pre-select-K stock by scanning EVERY enumerated hub including unwalked ones: flow_top = 357 rxn at a 200-hub pool vs 352 at 100 (1.4%), **saturating below 100 hubs**. Small, but it means pre-select reads fan-out from hubs the compute-time axis never charges — and the saturation hints a cheap 'scout' enumeration could pick the stock for a much smaller walk. `experiments/lsd_hubs/hub_order/`. Committed (branch Hub-Analysis worktree, not pushed). |
| [054](../Logs/054_filter-ablation-reward-vs-diversity.md) | 2026-07-30 | What does each half of the mode definition buy — ablate the reward gate and the diversity filter, one at a time, and measure the library that comes out? | **Both filters are load-bearing, and removing either one hurts HUB-BATCHING more than the baseline — so they are what makes our advantage real, not what flatters it.** New `experiments/lsd_hubs/filter_ablation/` scans each filter from `off` through its operating value to strict (τ ∈ {off, 4→8}, cutoff ∈ {off, 0.9→0.3}, crossing at τ=7/0.5, + the both-off corner) = 18 cells × 2 strategies in **34 s** on the login node, every arm delivering a **matched 300-molecule library** (Logs/051's set-size constraint) via the strategies' never-used `mode_selector_factory` seam. Hub-batching: dropping the reward gate looks **1.13× cheaper** (1.2167→1.0767 rxn/mode) but only **27/300 (9%)** of the library clears the bar (median reward 7.30→5.47, p10 7.04→1.79) → **11.96 rxn per QUALIFIED mode, 9.83×** worse; dropping the diversity filter looks **1.12× cheaper** but the 300-molecule library holds only **39** molecules distinct at 0.5 / **153** at 0.7 (mean pairwise 0.161→0.298, and 67 exact duplicates were still suppressed) → **8.33 rxn/qualified, 6.85×**; both off → **29.31×**. **A reward-BLIND Butina clustering confirms it** (the greedy count is fed best-reward-first, i.e. ordered by the variable under test): 144 families @0.7 / 31 @0.5 vs the greedy 153/39 — same verdict, slightly stricter, as in `051`. The two filters defend near-orthogonal things: with the gate off the library is still **300/300 distinct on both estimators**, and with the diversity filter off median reward is unchanged (7.29–7.38 across the whole cutoff row). Note a boundary correction: greedy rejects sim **> c** while Butina merges at sim **≥ c**, and a library built at cutoff c piles pairs onto it (36 pairs at exactly 0.500, none above), so Butina's threshold is nudged 1e-9 at the call site — without it the *unablated* library reads 266/300; `validation/lsdflow/metrics/diversity.py` is untouched, so `051` stands. The strategy comparison **inverts**: hub-batching's 2.545× advantage becomes an apparent 2.876× on rxn/mode with the gate off but **0.259× (3.9× WORSE)** per qualified mode. Failure has §2.5's predicted shape — the walk collapses from 43 hubs to 5 (2 with both off) and median nested depth 2→1, i.e. the depth-0 catalog optimum observed, not hypothesised. **Best-candidate is EXACTLY reward-gate-invariant** (every readout identical from τ=off to 8.0), the exact-limit form of `035`/`050`/`052`. Delivered-library self-similarity is flat (0.153–0.163) for τ=4→7 and rises at 7.5–8 — reproducing `051`'s flat-then-collapse on libraries, not pools. Case-1 readout reproduces `052`'s operating point exactly (1.2245 / 3.1263). Supplies §2.4's missing numerator guard (reward + depth beside the ratio). 2 pool-limited cells flagged (hub τ=8, cutoff 0.3). Adds `RewardOnlyModeSelector` to `glue/samplers/lsdflow/mode_select.py` + a `mode_selector_factory` passthrough in `build_strategy` (default path verified byte-identical). One checkpoint/target/draw. Not committed (branch Hub-Analysis). |
| [055](../Logs/055_surrogate-matrix-complete-bar5-surfaces.md) | 2026-08-04 | Completing the surrogate matrix, recalibrating the hit bar, and mapping the advantage over both knobs for all four generators | **Hub-batching is cheaper in 380/380 comparable surface cells across all four generators — and the bar-7.0 gate was distorting the comparison: at the calibrated bar 5.0 the edge nearly DOUBLES for two cells and the between-generator spread halves.** Closed the last two gaps in `050`: both RGFN cells (71981 rgfn_seh 31.6 h enum-only, 71982 rgfn_drd2 26.2 h full — the queued 71766/71767 were DEAD, submitted from a worktree deleted by the merge, and 71767 was additionally enum-only with no sample) and the newly-trained **cap-6** `fraggfn_seh` (72178, 29 min; pairing verified 3 ways — config `max_nodes: 6`, the run's own `run_config.yaml` snapshot, and median 35 heavy atoms vs 52 for the archived cap-9 run). **8/8 surrogate cells** at bar 7.0/0.5: rgfn 1.34×/3.21×, scent 2.60×/3.17×, rxnflow 1.23×/1.60×, fraggfn 3.75×/3.99×. **Bar sweep 5/6/7** (20 points): rgfn_seh 1.34→2.32× (+74%), rxnflow_seh 1.23→2.32× (+89%), scent_seh 2.60→3.10×, fraggfn_seh 3.75→4.21×. The point is not the increase but that **at bar 5.0 all four reach 300/300 modes for BOTH strategies** whereas at 7.0 the baseline is pool-starved (9 modes for rgfn_seh, 83 for rxnflow_seh) — so the ratio there understates hub-batching (rgfn_seh actually delivers **15× more modes**) and the between-generator spread collapses from 3.0× to 1.8×. Best-candidate's COST is bar-invariant (rgfn_seh 4.000→3.920) while its YIELD goes 9→300 — the exact-limit behaviour of `052`/`054` on three more cells. **Four-generator two-knob surfaces** (new `matrix16/surface_all_generators.sh`, a thin driver over `052`'s `tau_similarity_surface.py`; 9 bars × 13 cutoffs × 2 strategies per cell, 140–346 s each): hub cheaper in **71/71, 109/109, 85/85, 115/115** = 380/380, no crossover anywhere for anyone — generalising `052`'s 206/206. Pool-limited counts order the generators identically (fraggfn 2, scent 8, rxnflow 32, rgfn 46); for rgfn the WHOLE region above bar ≈6.3 is pool-limited **including the (7.0,0.5) operating point our headline is quoted at** — an independent statement that 7.0 is the wrong place to quote that cell. **Measured compute** now covers all 8 cells (was 3): rxnflow_seh is **97.8% flow-extraction** (29,345 s of 30,019 s), confirming a 3-hub pilot's 97.9% prediction — nearly all its enumeration cost is the backward-policy term `050` showed its ranking is 87.5% insensitive to; rgfn_seh is the most expensive cell outright (113,586 s, necessarily `lumped` — its enumeration is one opaque call into `glue/`); fraggfn_seh the cheapest by 20× (1,089 s). Also: `submit_cell.sh` auto-skips a completed stage (`RESUME=0` to force) so a requeue can't redo a 30k-trajectory sample; new `scripts/backup_scratch_critical.sh` mirrors the purge-vulnerable scratch artifacts to `$HOME` (19 GB, tier-1 checkpoints + SCENT P_B sidecars first — a sidecar loss makes a trained P_B unrecoverable even with the weights); fixed `tau_similarity_surface.py`'s hardcoded "SCENT sEH surrogate" subtitle, which silently mislabelled all four new surfaces. **Caveats:** one seed per cell, no error bars; FragGFN is a cost-model control throughout (attachments ≠ synthesis steps) though it posts the highest raw numbers; surfaces are sEH only; superseded cap-9 rows still in `gate_sweep/summary.csv`. |
| [057](../Logs/057_trillium-h100-docking-throughput-and-queue.md) | 2026-08-06 | GPU docking on a second cluster (Trillium H100) — what a newer card buys, and what actually sets the wall-clock | **The H100 is only ~1.3x faster, and the two-process-per-card question entry `036` could not answer is now settled: it is NOT a memory limit — on 80 GB two QuickVina2-GPU processes run with 32 GB SPARE and still gain nothing (ClpP 2.43->2.54 lig/s, 1.05x; 6TD3 2.10->2.34, 1.11x; util 36.7->41.7%). So docking throughput scales with card COUNT, not card quality or packing.** Entry `036`'s benchmark re-run unmodified (job 721183): at the production batch 200, ClpP **0.398->0.298 s/mol (1.34x)**, 6TD3 **0.632->0.528 (1.20x)** — the differential oracle gains less because part of each molecule is gnina CNN rescoring; batch 200 stays optimal, so `docking_batch_size=200` needs no revisit. **Controlled production comparison** (same hub, identical 1,860 children, Balam 72283 vs Trillium 721184): 0.9497 -> 0.7171 s/child = **1.32x**, docking 96.6% of it — agreeing with the synthetic 1.34x to within 0.02x. `rxnflow_clpp` (721189): 0.4185 s/child, so **SCENT costs ~1.7x per molecule what RxnFlow does** (larger promoted-fragment molecules). **COVERAGE FINDING, found while planning not testing: only 4 of the 8 docking cells can run** — `rgfn_worker.py`/`fraggfn_worker.py` have ZERO docking references (fraggfn raises `SystemExit("reward not wired")`), yet `manifest.py` reported `STATUS=ready` for all 8 because it only checks checkpoint/sidecar/candidates, so a job passed the gate, started the docking server, paid ~35-45 s of oracle construction PLUS a live preflight dock, then died in the worker — reading like a cluster fault. Fixed in **`27a1123`** (a `docking_wired` flag derived from worker source; docking now 4 ready / 4 worker-not-wired, surrogate unaffected at 8/8). **The docking half of the matrix is a TWO-generator comparison (RxnFlow, SCENT)** where the surrogate half is four (`055`). **6TD3's provisional -2.0 bar looks well calibrated** — `scent_6td3` smoke (721320) passes every substance assertion (0.0% oracle failures, `reward` raw-negative 200/200, two-column contract exact with beta=4 recovered) and the gate admits **63.0%**, a real filter — unlike ClpP's calibrated -8.0, measured admitting **88%** for SCENT, i.e. nearly non-binding; the recalibration concern is ClpP's, not 6TD3's. **The real bottleneck is scheduling, not hardware.** Trillium's GPU side is a separate, UNFEDERATED scheduler (`grillium`); of 252 H100s, **20 are permanently admin-held** (`MAINT,IGNORE_JOBS`, 365-day) so the effective pool is ~232 against ~228 allocated — saturated (reading `AllocTRES` without node State overstates availability 5x). Priority is **essentially all fairshare**: partition `compute` has `PriorityJobFactor=0` so the largest weight (10M) contributes NOTHING, QOS `normal` priority 0, leaving ~245k of the 5M fairshare weight vs a **median 411k across ~860 pending jobs** — bottom quartile; `def-naeilum` holds `RawShares=1` as do **3,312 other default accounts** (RAC accounts 10,000-48,000), and 17 min on two cards moved RawUsage 312->2,111 / fairshare 0.049->0.030 (7-day half-life, no reset). Two behaviours explain everything observed and were confirmed live: **`bf_max_job_user=3`** (8 slices submitted together -> **exactly 3 started**) and backfill luck (two jobs started 91 s after a whole-node `rrg-fwood` job was CANCELLED, with four jobs from three users taking those 4 cards in 26 s; a third job 3 min later waited **32 min**). **`squeue --start` is worst-case, not a forecast** — it advertised that job as starting 23 h out. Across 288 default-allocation GPU users over 2 weeks, max-concurrent cards p25=2, **p50=4**, p75=12, p90=21, p95=29 (62.5% reach >=4, 20.5% >=16, 4.2% >=32); 6-12 h requests wait median 0.2 h / p75 6.7 h / **p90 68.6 h** — so the median share equals Balam's reliable 4, with far heavier tails. Corollary acted on: request the walltime the work needs, not the max (71% of <=2 h requests start within the hour vs 50% of >12 h). **Remaining work, 4 runnable cells: 274.9 GPU-h on Trillium vs 342.0 on Balam** (`scent_6td3` alone 136.2 = half the bill) => at the median 4 cards, **2.9 d vs 3.6 d — a ~25% saving at equal card count**, everything beyond that being opportunistic. Running both clusters costs nothing to arrange: `submit_docking_cell.sh` runs **UNMODIFIED** on Trillium (no `--account` needed; untyped `--gpus-per-node=1` gives 1 card + 24 cores on a SHARED node, not a whole node; same partition names), slices are disjoint by construction, and the merge refuses partial publication. Oracles agree numerically across clusters (ClpP -5.9/-7.9/-6.6 vs -5.8/-7.9/-6.5; 6TD3 -2.44/-1.19 vs refs -2.20/-1.26). New `submit_batch_concurrency_trillium.sh` (3 documented deltas from the Balam launcher). In flight: `rxnflow_clpp` 8 slices (721347-721354) + `scent_6td3` sample (721346) on Trillium; Balam holds `scent_clpp` (12 slices, all PENDING, no progress after ~2 h) and `rxnflow_6td3`. `scent_6td3`'s slice count deliberately deferred to a hub-0 calibration. **Caveats:** one seed; the 63% gate figure is smoke-scale (240 children / 6 hubs); rgfn/fraggfn docking remains unwritten. |
| [060](../Logs/060_greedy-oracle-selection-ceiling.md) | 2026-08-12 | Four generators x four targets — the selection ceiling: how far below an adaptive cost-benefit greedy does the static flow ranking sit? | **The flow ordering recovers a median 94.6% of the achievable gain (0.82-0.99), costing a median 9.2% more reactions/mode (1.9-26.4%) and 10.9% fewer modes at a fixed reaction budget (0.0-32.7%) — and the greedy's residual edge is bought with 1.0-26.8x more molecule scorings, up to 80x more selection wall-clock, and library quality.** New `AdaptiveGreedyHubStrategy` (`glue/samplers/lsdflow/greedy_oracle.py`) re-scores every remaining hub each step by marginal (new modes)/(marginal reactions); all cost decisions delegate to `HubBatchingStrategy`'s own helpers and acceptance to a clone of the live selector, and `order="static"` **reproduces the shipped strategy bit-for-bit on 14/14 cells** (the regression that validates the probe). Ran on every enumerated matrix16 cell (8 surrogate + 6 docking), ~20 min CPU total, no GPU. **Two severe tests on the delivered libraries:** (1) reward-blind Butina at the operating cutoff 0.5 passes for every arm (300/300, but no discriminating power there — as in `054`); at a strict 0.35 it DOES discriminate and the greedy's libraries are the **most** scaffold-collapsed of the three (0.132 clusters/molecule vs flow 0.160 vs best-candidate 0.288) — the predicted cheap-means-shared-substructure mechanism, measured on the optimiser; (2) the greedy's median library reward sits closer to the acceptance bar than flow's on 12/14 cells (`scent_clpp` -8.60 vs -9.85 at a -8.0 bar). **Quality-matched control** (2 cells): re-running the greedy at stricter bars until its library matches flow's quality cuts its `scent_clpp` advantage 20.2%->~12.7% (~37% of the gap was drift) but barely moves `scent_6td3` (8.4%->7.8%). **Unexpected:** the greedy does NOT rediscover our top-ranked hubs — its picks sit at median flow-rank **76/200** vs a uniform null of 100, and Jaccard overlap with our walk is 0.00-0.19 on the easy cells (high 0.42-0.89 only where both must walk most of the pool) => within the flow-selected pool many different hub subsets are near-equivalent, which is `053`'s "neighbourhood not rank" conclusion reached from the opposite direction. **Replaces a planned but inapplicable optimality guarantee:** the shipped walk is a static ranking, not a step-wise optimiser, AND the objective is not submodular (modes are decided by order-dependent greedy sphere exclusion), so KMN's bound does not transfer — a measured gap is reported instead, and since the gap is small it is the stronger statement. **Caveats:** both arms choose within the same flow-selected top-200 pool, so this measures ordering and NOT pool selection; the greedy is an approximation, never "the optimum"; one seed per cell; quality-matched control on 2 of 14 cells; on `rgfn_seh` the greedy reaches fewer total modes (129 vs 135) at a better rate, so only the rate comparison stands there. `experiments/lsd_hubs/greedy_oracle/`. |
| [056](../Logs/056_s3gfn-sparrow-selects-pipeline-headline.md) | 2026-08-04 | sEH — the competitor pipeline run as a chemist would run it (S3-GFN → MultiAiZ → SPARROW **selects**) vs LSD-Flow native routes | **HEADLINE REVERSED IN OUR FAVOUR: 3.13× fewer reactions as-run (411 vs 131), and 1.79× against the competitor's STRONGEST configuration (235: MultiAiZ + a diversity-aware selection — run precisely because it could only weaken us; QUOTE 1.79×). The OLD from-scratch pricing had us LOSING (S3-GFN 34–35 modes@100rxn vs hub-batching 20–27).** The whole flip is *who does the retrosynthesis*: entry [047]'s stock mismatch meant re-deriving our routes measured the planner's catalogue coverage (48.7% ours vs ~90% S3-GFN's), not library cost. Rebuilt as a real pipeline — competitor gets S3-GFN (median score **8.03 > our 7.61**, 99.7% above gate), MultiAiZ (n_iters=5, its own plain-ZINC catalogue, **95.6% routed**), and SPARROW **doing its own selection** (`constrain_all_targets=False`, reward objective, hard `max_rxns`) rather than being handed a pre-chosen list to price. Equivalent reading: at 131 reactions it delivers **32 modes to our 100**. **SPARROW's diversity blind spot MEASURED:** its selections are *less* diverse than the pool it draws from when the budget binds — **27% mode rate vs the pool's own 41%** — converging to neutral (0.40) only at R=1000 where it takes everything; cheap routes come from shared intermediates, which come from similar molecules. **Route-planning cost is linear at ~16.2 s/target** (16.66/15.99/16.03/16.17 over N=50→500), so N=500 = **2.25 h the competitor pays and we pay 0**. **T4.4 coherence gap CLOSED + pre-select-K break-even found:** entry [049]'s 3.12% was measured on the *naive* policy, not the headline's; re-audited, sEH free-frag+K20 agrees to **0.75%**. DRD2 free-frag+K20 FAILED the gate (12.69%) — falsification test (K=0 → **exactly 110/110, 0.00%**) proves it is entirely pre-select-K's **26 up-front reactions**, ~17 unused by the 100-mode selection. Count-once charges what we spent, SPARROW only what hindsight needed ⇒ **our numbers are conservative**; and since K=20 *wins* at 300 modes (1.16 vs 2.65, [050]), pre-select-K has a **break-even library size**. Defaults still reproduce [049] bit-for-bit (314/299, 288/279). **Asymmetries reported, not matched** (pool 500 vs 26,069/64 hubs; surrogate calls ~500 vs 155,764; planning 2.25 h vs 0). Infra: plan-once `submit_multiaiz_discover.sh` + `sparrow_select_frontier.py` + `--select` mode. Jobs 72187–72190. sEH only, one seed. |
| [052](../Logs/052_tau-similarity-cost-surface.md) | 2026-07-29 | The two-knob cost surface — does the hub-batching advantage survive everywhere on (reward bar × diversity cutoff) at a fixed 300-reaction budget? | **Hub-batching is cheaper in 206/206 comparable cells (1.59×–3.02×) — no crossover anywhere on either knob, so the headline ratio is not an artifact of where the knobs sit.** New `campaign/tau_similarity_surface.py` (+ `submit_tau_similarity_surface.sh`) sweeps a 17 τ (4.0→8.0 by 0.25) × 13 cutoff (0.30→0.90 by 0.05) grid = 442 selection runs in **639 s** on one `debug` node, reusing `run_campaign.py`'s own loaders/`build_strategy` so each cell is bit-comparable to a `run_campaign` invocation. Operating point (τ=7, cut 0.5): hub **1.2245** vs best **3.1263** = 2.55× (245 vs 95 modes at 300 rxn); best cell 3.02× (τ=4, cut 0.40); worst comparable 1.59× (τ=8, cut 0.55). Advantage peaks mid-cutoff (2.70× at 0.45) and declines monotonically in τ (2.61×→1.88×). **Best-candidate is EXACTLY τ-invariant** (spread 0.00 across τ=4→7.75 at every cutoff; it never gets below the top reward slice inside 300 reactions) and varies only with the cutoff (2.48→3.70) — the exact-limit version of `035`/`050`'s "nearly gate-invariant". **15 pool-limited cells** form one strict-cutoff × high-τ corner, 13 of them hub-batching running out of its 200-hub library (hatched, excluded from tallies, NOT counted as wins) — plausibly a library-size artifact (cf. `031`), not a method limit. Hub-batching's degradation begins exactly where entry `051` independently found the pools collapsing (τ ≳ 7). Needed one production speedup: `glue/samplers/lsdflow/mode_select.py` bulk-Tanimoto (3.5–6.1×, accepted curves byte-identical). Note this is the fixed-**300-reaction** readout, not matrix16's fixed-300-**mode** one (1.303/3.383) — same run, different budget convention, same ratio. Not committed (branch Hub-Analysis). |
| [051](../Logs/051_reward-cutoff-vs-intrinsic-diversity.md) | 2026-07-28 | SCENT / sEH — is a molecule set's intrinsic diversity coupled to the reward cutoff we condition it on? | **The coupling is real and perfectly monotone, but negligible at every bar we report at and steep only past ~7 — so the τ-conditioned reactions/mode comparisons are not riding a diversity artifact.** New `experiments/lsd_hubs/reward_diversity/` sweeps 44 cutoffs (1,2,3 + 4.0→8.0 by 0.1) over both SCENT-sEH pools — the 30k generated sample (26,069 unique, `scent_seh_70189`) and the 200-hub enumerated library (604,840 unique, `campaign_enum_seh_70363`) — scoring **exactly 2,500 randomly drawn molecules at every cutoff** (×3 draws) so mode/scaffold counts can't be confounded by set size. Spearman(τ, mean pairwise Tanimoto) = **+1.000** on both pools, ρ(τ, modes/mol) ≈ **−0.995**; but in magnitude `modes/mol` falls only **0.2%/0.7%** from bar 5→6 and **2.3%/4.2%** from 5→7, then collapses **44%/42%** by 8.0 (first 0.1-step costing >0.5 pp lands at **τ=7.1/7.0**). Entry `034`'s calibration puts that collapse inside the proxy-optimised tail (0/2,315 real actives reach 8.0), i.e. *outside* the meaningful ≈5–6 range. Our headline bars (5/6/7) are all in the flat regime; the committed **`gate_curve` sweeps run to 8.0**, so their top two points (7.5/8.0) are a different regime, not the same curve continued. Two secondary findings: the **Murcko cross-check disagrees in sign** for the enumerated library (scaffolds/mol *rises* 0.968→0.998 with τ), so the mild sub-7 fingerprint trend is not a robust "high-reward chemistry is redundant" claim; and at matched N the **enumerated library is *less* self-similar than the model's own generated sample** at every bar below ~7.6 (0.1459 vs 0.1686 at τ=1) — the opposite of the intuition that hub children must be redundant. **Reward-BLIND cross-check agrees**: Butina clustering (centres by neighbourhood density, never reads the reward) gives ρ=**−0.998/−0.991** and a slightly steeper collapse (−47.2% vs modes' −44.2%), so the coupling is a property of the molecules, not of feeding the greedy mode selector in reward order — our count is marginally optimistic in the tail only. Also extended `validation/lsdflow/metrics/diversity.py` (mean pairwise similarity, `count_butina_clusters`, cacheable ECFP/Murcko, bulk sphere-exclusion — verified output-identical, 2.7–5.3× faster). 381 s on a login node. Cost side = entry [052]. Not committed (branch Hub-Analysis). |
| [048](../Logs/048_chemistry-homogenization-seh.md) | 2026-07-27 | Chemistry homogenization — can a SMILES generator or a SOTA planner match the reaction-GFN's route economy? (Exp A/B/C) | **A SOTA planner recovers most but not all of the from-scratch penalty (native routes still win) — rebutting "the route-cost penalty is real"; Exp A came out INCONCLUSIVE and must not be cited.** FRAGMENTS ONLY (418 SMALL blocks to the stock, never the reactions). **Exp A** (S3-GFN, SMALL-blocks-only retro lib, job 71522): synth_ratio **0.0** / 0 candidates — but a follow-up falsification test (`experiments/oracle_validation/s3gfn_retro_signal/`) showed this is a **template–block incompatibility, not a synthesizability result**: raw SMALL blocks score 100% (env fine), `max_steps` 3→6 changes nothing, and SCENT molecules score ~0% at every depth **even under `zincfrag_hb105` with 178k blocks** while S3-GFN's own molecules score 93% there ⇒ `hb105` templates can't reverse reaction-GFN chemistry, so **fragments-only transfer cannot homogenize a template-based synthesizability signal** (it works for AiZynth's broad USPTO templates — why [047] succeeded). **Exp B** (ZINCFrag∪SMALL, 71523): trains fine, 2,000 candidates, and the SMALL blocks are NOT inert (14/59 molecules have ≥1 route using one; 6.5% of block-uses) → still a valid "best chance" arm. **Exp C** (SCENT priced via MultiAiZ→SPARROW, cutoff 0.5): hub-batching **1.85 rxn/mode** (ZINC) / **1.91** (ZINC∪SMALL, 163→283 modes = the [047] coverage effect) vs plain-AiZynth from-scratch **2.74** and native **1.22** — the convergent planner recovers ~60% of the gap but native by-construction still wins, and **hub-batching beats best-candidate (~1.9 vs ~3.0) under every pricing**. **T3.2 MVP done** (S3-GFN frontier, 71536): S3-GFN is 88% from-scratch-routable vs SCENT 48.7% *because* it emits ZINC-native molecules → looks competitive at from-scratch pricing = the [047] confound → motivates native-route pricing as primary. **[047] ceiling** (zinc+blocks @10× budget): 48.7%→73.8%→**~92%**. 3 jobs timed out (14h/10h) but write incrementally → results recovered; wall-clocks need ~20h/~13h. `experiments/{lsd_hubs/campaign,oracle_validation/aizynth_failure_modes}`. Infra committed `3888c35`; log not committed. |
| [047](../Logs/047_aizynth-failure-stock-mismatch.md) | 2026-07-24 | Why AiZynth "fails" half the SCENT sEH pool — a stock/chemistry mismatch, not bad molecules | **The from-scratch AiZynth 48.7% (entry [041]) is a catalogue mismatch, not molecule quality: adding the reaction-GFN's own 418 blocks to stock lifts pool-wide route-success to 73.8% (+25 pts).** Diagnosed why molecules fail by re-routing (read-only) a 50-sample of the 2,435 failures while recording AiZynth v4.4.1's four failure-mode signatures. **0/50 are out-of-distribution** — every failure disconnects ≥75% into stock with common templates; budget (iter 100→1000) recovers ~18%, filter-off ~4%, depth 6→12 ~6%. The residual is **one leftover ~15-HA fragment = the reaction-GFN's OWN scaffold in a different form** (THIQ/proline/azetidine as the ester/aldehyde/N-carbamate/flat-vs-stereo acid; block match 0/43 with stereo, 4/43 stripped) plus specialized reagents ZINC won't stock (dibromoesters, B2pin2, Wittig salts, Boc/SEM/trityl, C#N). Test: build a NEW blocks-only stock (418 blocks, stereo+flat InChIKeys) unioned with ZINC (**ZINC hdf5 untouched**) → 50-sample failures flip 0→24 at production / 9→39 at 10× budget; **full pool (4,749, job 71436): 48.7%→73.8%**, control reproduces baseline (46.3%). Rebuts both "reaction-GFN makes worse molecules" and "the from-scratch SPARROW 2.74-rxn/mode penalty is real" → motivates native-route pricing as primary + the chemistry-homogenization plan. `experiments/oracle_validation/aizynth_failure_modes/`. Not committed (branch Hub-Analysis). |
| [046](../Logs/046_fraggfn-drd2-maxfrag-recovery.md) | 2026-07-23 | FragGFN–DRD2 — recovering the paper's ~0.9 reward (the fragment-cap fix) | **The FragGFN-DRD2 collapse (mean 0.03, 0 modes) was a mis-set fragment cap, not a fragment-assembly limit — recovered to the paper's ~0.9.** Diffed our config against the RGFN paper's recipe (App. B.2): reward `exp(β·proxy)`, fixed β=48, bengio2021flow lib, TDC DRD2 oracle ALL match — the **only** delta is `max_frags` (paper **6**, ours **9**). Three extra fragments → MW ~664 oversized molecules the drug-sized DRD2 SVM scores ~0. Also explains why [019]'s two temperature fixes failed (β was never the difference; size was). Smoke at `max_frags=6` (1,500 steps, login A100): DRD2 **mean 0.908 / median 0.982 / 93% modes / MW 385** (was 664) — matches the paper in fewer steps. Full 5,000-step retrain queued (job 71433) → `fraggfn_drd2_maxfrag6`. **Broader:** all 4 FragGFN cells ran at max_frags=9 vs the paper's 6 (only broke DRD2); whether to re-run sEH/6TD3/ClpP at 6 is a pending methodology call. `validation/configs/fraggfn_drd2_maxfrag6.yaml`. Not committed. |
| [045](../Logs/045_clpp-docking-threshold-calibration.md) | 2026-07-23 | ClpP docking — mode-threshold calibration (known binders vs property-matched decoys) | **Replaced the useless provisional −2.0 ClpP hit bar with a data-grounded −8.0 kcal/mol.** Identified the matrix's ClpP receptor as **human ClpP (PDB 7UVU)** from its residue range (chains B/C 58–249 → only the 277-aa human ortholog fits) → ChEMBL target **CHEMBL4523305**. Docked **183 real ClpP binders** (mostly EC50 imipridone activators) vs **183 property-matched DUD-E decoys** with the SAME QuickVina2-GPU oracle the cell trains on (`DockingClpPOracle`): actives median **−8.90** vs decoys **−7.00**, **AUROC 0.895** (MW-matched 0.897 — not a size confound). **Youden-optimal cutoff −8.0** (TPR 88 %/FPR 23 %); high-specificity variants −9.0 (95 %) / −9.78 (99 %). Payoff: the old −2.0 passed 99.5–100 % of every generator; −8.0 recovers the ranking (SCENT 99 % · RGFN 73 % · FragGFN 34 % · RxnFlow 7.5 %). Wired into worktree `matrix16/targets.py`. Gate on the **raw Vina** value, not the ReLU `score` column. `experiments/oracle_validation/docking_clpp/`. Not committed. |
| [044](../Logs/044_lsdflow-strategy-compute-reward-call-summary.md) | 2026-07-22 | LSD-Flow selection — consolidated compute & reward-call accounting (best-candidate / free-frag / pre-select-K) | **One figure, both cost axes, all three strategies — nothing new run, just consolidated from `037`+`039`.** Puts best-candidate / free-frag / pre-select-K on the same two axes at the benchmark's baseline point (SCENT sEH, τ=0.5, bar 7.0, 300 modes, surrogate reward). **best-candidate ≈ free** (0 scoring calls, ~1.1 s — reuses sampled scores, pays only the O(n²) diversity filter; spends bench reactions instead, 3.10/mode). **Hub-batching's compute is enumeration, not scoring:** free-frag = 7,472 s of which enumeration (RDKit build children) is ~79%, reward-gen only ~5%, flow-extract ~15% (building children ≈12× scoring them under the surrogate). **pre-select-K is the dial:** K=0→200 cuts calls 6.0× (315,539→52,865) AND compute 6.1× (7,472→1,219 s), for more reactions/mode (1.22→1.85). **Caveat (from `039`):** the ~5% reward-gen share is an artifact of the ms sEH surrogate — a ~1 s/call docking oracle flips reward-gen to dominate, so the **call-count is the reward-agnostic axis** for the AL loop. New reusable driver `strategy_compute_summary.py` + `results/scent_seh_strategy_summary/`. External-referee compute (route-finding dominates) stays separate, in `041`. Not committed (branch Hub-Analysis). |
| [043](../Logs/043_multiaiz-second-competitor-planner-build.md) | 2026-07-22 | MultiAiZ — the second competitor route-planner (T4.1): build + convergent-route pricing design | **Build/design entry: the strongest post-hoc planner for the non-reaction competitor, priced so shared intermediates cost their build once (not free).** MultiAiZ (`[ianez2026multiaiz]`, MolecularAI) runs AiZynthFinder over a SET of targets for 5 cycles, appending discovered intermediates to the stock so targets converge on shared intermediates → `--evaluator multiaiz`, feeding the SAME SPARROW MILP as the from-scratch headline. **Per-pool isolation** (each acquisition function's pool priced alone; hub-batching's sharing never leaks to best-candidate/S3-GFN). **Key design decision:** MultiAiZ ends routes at discovered intermediates as `in_stock` leaves → priced as-is they'd be FREE and the convergent saving would read zero; fix = flatten+**stitch each route down to real ZINC stock** (expand intermediate leaves into their build subtrees) so `build_network` marks them non-buyable and SPARROW builds each **once + amortizes** across sharers. Feed ALL candidate routes/target, let SPARROW's MILP pick the max-sharing set. **Env: reuse `aizynth`** (has aizynthfinder 4.4.1, no cgrtools) — separate-env poetry build failed on legacy cgrtools; MultiAiZ installs `--no-deps` there. n_iters=5 (paper value). Headline numbers pending the coarse-cutoff run (nodes down for maintenance). Not committed (branch Hub-Analysis). |
| [042](../Logs/042_t15-cost-model-reconciliation-gate.md) | 2026-07-20 | SCENT sEH — T1.5 cost-model reconciliation gate (count-once vs native-route SPARROW), full coverage | **GATE PASSED at full coverage — the cost model is trustworthy, comparisons can be plotted.** Our fast internal count-once estimate (Logs/033) and an independent SPARROW MILP run on the molecules' by-construction routes agree within ~5% on the same 100 modes: best-candidate 314 vs 299 (rel_diff 4.8%), hub-batching 288 vs 279 (3.1%), both at **1.00 native-route coverage** (vs the earlier smoke's 59%). SPARROW consistently finds slightly *more* sharing (global optimizer) but well inside the 10% tolerance. Doubles as a T4.4 preview: SPARROW shaves only ~3% off hub-batching's own count → the flow-based selection is near-optimal. Required first repairing the enumerator (the DFS crashed on `ReactionStateEarlyTerminal` → silent no-output in job 70974; one-line `isinstance(state,(RSA,RSB,RSC))` guard in scent_worker.py; enum re-run 71010 wrote all 64 hubs/169,230 children). `submit_scent_seh_native.sh` now resumable + guarded. `results/scent_seh_recon_full_reconcile/`. Not committed (branch Hub-Analysis). |
| [041](../Logs/041_sparrow-headline-compute-frontier-seh.md) | 2026-07-20 | SCENT sEH — from-scratch SPARROW headline + compute frontier (neutral referee, real route-finding time) | **Hub-batching's reactions/mode edge SURVIVES neutral from-scratch pricing, and route-finding is the entire cost.** Priced SCENT's sEH library through the fair referee (AiZynth re-routes every molecule over USPTO/ZINC → SPARROW MILP), with the timed route cache (job 70976) supplying real per-molecule search time. **hub-batching (free-frag+K100) = 2.74 rxn/mode (502 rxns, 183 routable modes) vs best-candidate 3.66 (564 rxns, 154 modes)** — fewer reactions AND more routable modes. Compute breakdown at τ=0.5: **AiZynth route search dominates (~12–13k s)**; the reaction-GFN's own enumeration is ~2k s and the **SPARROW set-cover MILP is 0.06 s** — so from-scratch pricing is bottlenecked by retrosynthesis search, exactly the step a reaction-grounded generator gets for free. Caveat: from-scratch AiZynth solves only **48.7%** of the pool → the fixed-300-modes plot is empty (use the fixed-reaction-budget plot as the from-scratch headline, or raise coverage). Job 71009 (Balam), 7m37s, CPU-only over cached routes. free-frag adopted as the single "hero" acquisition for smokes (variant bake-off deferred). Not committed (branch Hub-Analysis). |
| [040](../Logs/040_s3gfn-baseline-bringup-synth-env.md) | 2026-07-20 | S3-GFN (non-reaction baseline) — env bring-up, sEH-reward injection, and the synthesizability-library decision | **The marquee non-reaction baseline runs against OUR sEH reward and its synthesizability method works — using ZINCFrag + 105 templates, not the paper's (unavailable) Enamine stock.** Stood up the `s3gfn` env (fixes: `gflownet --no-deps` to stop its `torch==2.1.2` pin clobbering the 2.5.1 stack; `setuptools<81` for PyTDC; RDKit `fpscores` for SA; `mol2graph` try/except for exotic atoms; stub `rxnflow.tasks.unidock_vina`). Reward injected by replacing S3-GFN's module-global `get_scores` with a swappable provider (`SEHFrozenReward` — the SAME Bengio-2021 proxy, training reward = native `clip(raw/8,1e-4,100)`); pool emitted `has_route=0` (routed post-hoc). **Library decision:** paper's `stock_hb` (Enamine) is vendor-gated; ZINCFrag is S3-GFN's public alternative AND in AiZynth's ZINC stock (aligns with our neutral referee). ZINCFrag fragments are small → paper's 71 templates give only **0.8%** starting synth_ratio (won't bootstrap); the fuller **105-template** set gives **4.7%**. 200-step de-risk: synth_ratio climbs **4.7%→93.8%**, pool **96% synthesizable** (meets T3.1 ≥95%), ~3 s/step (→ full 5000-step ≈ 3.5–4 h). Choice shapes only training, not the score. Decision documented in `build_s3gfn_retro_env.sh` header + `s3gfn_seh_fixed.yaml`. Not committed (branch Hub-Analysis). |
| [039](../Logs/039_lsdflow-compute-time-accounting.md) | 2026-07-15 | SCENT sEH — measured compute-time accounting for hub-batching vs best-candidate | **Hub-batching costs ~150× more compute than best-candidate (+169 s vs 1 s at cutoff 0.5) to save ~113 bench reactions — and for the sEH proxy that compute is 64% *enumeration*, only 5% reward-gen, so the reward-gen call-count badly under-represents real time.** Measured live (not modelled): worker records per-hub enum/reward/flow wall-clock → drivers attribute over each strategy's hub walk. Diversity cutoff + pre-select-K are the two big compute levers (strict 0.30 = 13,485 s; K=200 = 6.1× less than free-frag). |
| [038](../Logs/038_hub-batch-size-distribution.md) | 2026-07-15 | SCENT sEH — per-hub synthesis-batch-size distribution across the pre-select-K sweep | **The "one giant synthesis step" worry is unfounded: batches are moderate and reasonably even, and K is a clean consolidation dial.** Diagnostic-only re-selection over the cached 200-hub enumeration (`037`, enum 70363) for a 300-hit library, grouping accepted diverse hits by `source_hub` to recover each hub's batch size (the `k` in `depth(h)+k`). Raw enumeration fan-out is a red herring for synthesis load: a scaffold with **~12,000** possible one-reaction children yields only **7** kept diverse hits (reward-bar + Tanimoto-0.5 dedup collapses it ~100–1000×, because distinct modes need distinct fragments). Largest single batch anywhere in the sweep = **65** (K=200 extreme, 10 hubs); at K=0 it's **17** (57 hubs). Worst hub-to-hub ratio **28×** (K=50), not 300×; Gini stays 0.37–0.44. Raising K packs the 300 hits into fewer/larger/relatively-more-even hubs (Gini 0.44→0.37, max/min 17→9×) but concentrates the library (top-1 hub 6%→22%, top-5 24%→81%) — low K = many small parallel steps, high K = few larger consolidated steps, neither pathological. New `experiments/lsd_hubs/campaign/batch_size_distribution.py` + `results/scent_seh_batch_dist/` (2 figures + stats CSVs). Caveat: max batch scales with the library-size target (fixed 300 here). Not yet committed (branch Hub-Analysis). |
| [037](../Logs/037_free-frag-smart-frag-hub-child-selection.md) | 2026-07-14 → 07-15 | SCENT — fragment-aware hub-batching: free-frag, and pre-select-K for cheap enumeration | **Winner = pre-select-K: stock a few widely-reusable blocks up front, then free-fill — cutting the oracle/enumeration bill ~2–6× at ~constant synthesis cost.** `free_frag` (keep only already-available-fragment children) gets **1.22 rxn/mode** (2.5× cheaper than best-candidate) but must walk 57 hubs → **315k reward-gen (≈ oracle) calls**. Fix (measured: promoted fragments are cross-hub, median ~50 of 200 hubs): **pre-select-K** pre-synthesizes the top-K fragments by build-score `(reward−bar)·fanout/build_rxns` (charged once, no hub double-count), then free-fills → far fewer hubs. 200-hub Pareto: K=20 → 1.22 rxn/mode @ **241k** calls (free lunch); K=100 → 1.41 @ **85k** (−73%); K=200 → 1.85 @ 53k; median reward drifts 7.30→7.51. Blocks picked are chemically-sensible universal building blocks (bromo-aryl amide/amine + azetidine). **Lean code:** kept `reward` (=naive hub-batching, no cost awareness; still 819/929) + `free_frag` + `prebuilt_fragments`/`rank_fragments` (swappable `RANK_METHODS`) + `preselect_sweep.py`; **DELETED** static smart-frag, dynamic fan-out move-on, `$`-cost, and the SCENT mean-reward utility machinery (all tried-and-dropped negatives, documented). New `results/scent_seh_1kx200_preselect/`. Not yet committed (branch Hub-Analysis). |
| [036](../Logs/036_docking-batch-size-concurrency-sweep.md) | 2026-07-14 | QuickVina2-GPU — docking batch-size sweep & 1-vs-2 concurrent-process test on one A100 (40 GB) | **Batching the whole per-step call into ONE QuickVina2-GPU process is a free ~3.3× docking speed-up for the single-target oracle; a 2nd concurrent process buys nothing and nearly fills the card.** The 16-cell run's engine (entry `030`) is **QuickVina2-GPU-2.1** (`thread=8000`). Measured on a dedicated A100 (job 70623, partition `debug`) over a fixed 200-molecule pool: ClpP docking falls **1.31 → 0.40 s/mol (3.3×)** and 6TD3 **1.75 → 0.63 (2.77×)** as batch goes 25→200, at **flat ~20 GB VRAM** (ligands dock sequentially inside a process, so batch size is memory-free; scores are batch-invariant, 199/200 ok throughout). The single-target oracle (`DockingClpPOracle`/`DockingSEHOracle`) chunks at **25** in production → leaving the 3.3× on the table; the **6TD3 differential oracle already docks the whole per-step call in one process** (its `score_detailed` ignores `docking_batch_size`), so it's already at the fast end. **Concurrency:** two QuickVina2-GPU processes on one A100 gave **1.01× (ClpP) / 1.07× (6TD3)** throughput while pushing VRAM to **~39.9 GB of 40** — one process at `thread=8000` (~20 GB) already exhausts useful capacity. GPU util stays <50% even at batch 200 → remaining ceiling is per-ligand host/dispatch, not compute. **Fix belongs in the NEXT run, not mid-campaign** (`docking_batch_size=200` in the single-target oracle default / docking-server launch). New `experiments/fixed_reward/docking_benchmark/{bench_batch_concurrency.py,submit_batch_concurrency.sh,results/70623/}`. Not committed (branch Hub-Analysis). |
| [035](../Logs/035_hub-batching-mode-threshold-robustness.md) | 2026-07-14 | SCENT — hub-batching vs best-candidate at relaxed "mode" thresholds (5 and 6) | **The cost comparison is robust to the hit bar — relaxing it to where real actives live (`034`: proxy 7.0 is mis-calibrated, empirically useful bar ~5–6) does not change the `029`/`033` conclusion.** Pure-CPU re-run of `run_campaign.py`+`sweep_campaign.py` at `--reward-threshold` 5 and 6 over the *same cached* enumerations (50-hub 70295, 200-hub 70363) + 30k sampled pool — no GPU, no re-scoring. **best-candidate is byte-identical at every cutoff and every threshold** (its top-300 diverse modes are all high-reward, so the bar never binds — a built-in control); **hub-batching matches the bar-7 curve from cutoff ~0.55 up** and diverges only at strict diversity, where a lower bar is slightly cheaper (200-hub, cutoff 0.30: **882 → 847 → 823** reactions as bar drops 7→6→5). The one real change is on the *enumeration* axis: at cutoff 0.5 a **single** hub now serves all 300 modes instead of two → reward-gen calls **halve (18,856 → 9,428)**, median mode reward softens 7.48→7.35, edge stays ~1.13×. The 50-hub cutoff-0.30 ceiling is a scaffold-diversity floor (unreachable even at bar 5), not a reward limit. New `experiments/lsd_hubs/campaign/compare_thresholds.py` + `results/{scent_seh,scent_seh_1kx200}_thr{5,6}/` + `results/threshold_comparison/` overlays; bar-7 baselines (`results/scent_seh{,_1kx200}/`) left untouched. Not committed (branch Hub-Analysis). |
| [034](../Logs/034_seh-proxy-hit-decoy-discrimination.md) | 2026-07-14 | sEH — known-inhibitor vs decoy discrimination & hit cutoff (proxy + docking) | **Proxy enriches real sEH inhibitors over decoys but weakly, and its scale ≠ activity. AUROC 0.76 vs random RGFN molecules (0.74 MW-matched) but only 0.68 vs property-matched (DUD-E) drug-like decoys → enrichment collapses 5–7×→2–3×. 0/2,315 ChEMBL actives reach the 8.0 "hit"/mode threshold (only 9 reach 7.0), proxy≈measured-potency r=0.22; empirically useful cutoffs ~5–6. "Modes ≥8.0" is a benchmark diversity count, not an activity claim. Follow-up: **real QuickVina2-GPU docking beats the proxy on both negatives (AUROC 0.86 vs 0.76 random; 0.73 vs 0.68 matched) on an interpretable scale (actives median −9.7 kcal/mol); docking↔proxy agree only weakly (Spearman 0.42).** New `experiments/oracle_validation/seh_proxy/` + curated `data/validation-molecules/sEH_{actives_chembl,decoys_matched}.csv`. Not committed (branch Hub-Analysis).** |
| [033](../Logs/033_fair-cost-model-double-count-accidental-hubs.md) | 2026-07-14 | SCENT — fair cost model: fix the fragment double-count + credit accidental hub-batching | **Most of hub-batching's "halving" was a cost-model artifact.** Two unfairnesses in `029`/`031`, both fixed on ONE count-once model applied to both strategies: **(1) double-count** — SCENT's `num_reactions` is *fully nested* (already builds every promoted fragment; confirmed in `external/scent/…/reaction_env.py` + 245/245 fragments match nested cost exactly), yet the campaign *added* the fragment builds again → per-molecule term ~2× inflated. Fix: charge shallow *assembly couplings* (`num_reactions − Σ nested frag builds`) + each distinct fragment once. **(2) accidental hub-batching** — best-candidate's top-N diverse picks share parent scaffolds; now credited via a swappable `MostSharedAssignment` (immediate-parent hubs from `records.csv`, prefix-validity filtered so sharing can never raise cost). **Result (sEH, cutoff 0.5):** 300 modes cost best-candidate **929 rxn (3.10/mode)** vs hub-batching **819 (2.73)** — edge **1.13×, not 1.81×**. Of best-candidate's 1,479→929 drop, the double-count fix is 530, accidental hubs only ~20 (diverse modes → diverse scaffolds). At loose cutoffs (≥0.75, 200-hub) best-candidate *beats* hub-batching on reactions; hub-batching's real remaining cost is its 7k–19k reward-gen (enumeration) calls vs 0. Regenerated `results/{scent_seh,scent_seh_1kx200}/`. `029`/`031` cost numbers superseded here (hub-stats/ceiling findings stand). Not committed (branch Hub-Analysis). |
| [032](../Logs/032_scent-hub-batching-diversity-pairs-gallery.md) | 2026-07-14 | SCENT hub-batching — diversity-pairs gallery (closest still-distinct pair per cutoff) | **The diversity cutoff is a faithful, interpretable structural dial — visualized.** For the 200-hub enumeration (`031`, job 70363) we rebuild the hub-batching library at each sweep cutoff (0.30→0.90) and draw the closest *still-distinct* accepted pair — the two most-similar molecules the cutoff still counts as separate modes (MCS-aligned + highlighted). **The tightest pair lands essentially on the cutoff:** exactly on it 0.30–0.80 (dense hub-child neighborhoods realize the exact rational Tanimoto = threshold; the selector rejects only *strictly* above), 0.84/0.88 at 0.85/0.90 (300-mode library fills first). Structurally intuitive: at 0.90 the "distinct" pair differs by one ring-size (azetidine→pyrrolidine), at 0.75 by an oxazole↔isoxazole swap, at 0.30 only a shared fused-ring core remains. Supporting/intuition figure for the `029`/`031` diversity axis. **Addendum — synthesis routes + step-by-step protocols:** each molecule is a hub diversification whose fragment routes are logged (`smiles_to_route`, incl. the RGFN reaction **template** per step). Every molecule = **3 logged reactions + hub attach** from 4 purchased blocks; reactions named from templates (amide/Suzuki/Buchwald/benzoxazole/N-alkylation/…), reactants tagged buy vs make. **11/13 pairs share the same hub;** of those, **3 (0.60/0.85/0.90) share a fully-built intermediate** (diverge only at the final decoration), 8 share hub+building-blocks+reaction-sequence but diverge at the first reaction, and 2 (0.35/0.45) use different hubs (independent). New `{diversity_pairs.py,route_trees.py,synthesis_routes.py}` → `diversity_pairs/{gallery.png,pairs.csv,route_trees.png/csv,synthesis_protocol.md,synthesis_steps.csv,schemes/}`. Not committed (branch Hub-Analysis). |
| [031](../Logs/031_hub-batching-scaleup-200hubs-strict-diversity.md) | 2026-07-14 | SCENT — hub-batching vs best-candidate at scale (200 hubs, diversity to 0.10) | **[COST NUMBERS SUPERSEDED→033: reactions/mode recomputed on the fair count-once model; the ceiling/floor finding below stands.]** **Scaling hubs 4× moves the diversity ceiling down; below ~0.30 the wall is chemistry, not strategy.** Scale-up of `029`: top-1,000 candidates → 575 distinct hubs → **top-200 enumerated** (job 70363, ~5.5 h, 828k child records), diversity sweep **0.10→0.90** on the same 30k pool `scent_seh_70189`. **(1) Ceiling moves with hub count:** at cutoff 0.30 hub-batching now reaches 300 modes in **898 reactions**, where the 50-hub run (`029`) capped at **135** (couldn't reach 300 at all). **(2) Hard floor below ~0.30 is the sEH chemical space, not the method:** from 0.10–0.25 *neither* strategy reaches 300 modes — best-candidate ignores hubs and also fails → no 300 mutually-Tanimoto-<0.30 hits exist at Morgan r=3 (modes@100rxn: 0.10=6/4, 0.15=19/14, 0.20=28/19). Where both can serve (≥0.30), hub-batching still wins ~1.7–1.8× (770–898 vs 1,386–1,479 rxns for 300 modes); best-candidate numbers byte-identical to `029` (built-in control). Artifacts in `experiments/lsd_hubs/campaign/results/scent_seh_1kx200/`. Not committed (branch Hub-Analysis). |
| [030](../Logs/030_publication-scale-fourway-matrix.md) | 2026-07-13 | Four systems × four generators — publication-scale re-run (uniform 5,000 iters) | **(COMPLETE — 48/48, 3 seeds; full comparison in entry [063](../Logs/063_fourway-matrix-complete-three-seed-comparison.md))** Uniform **5,000-step** re-run of the full 4×4 matrix (RGFN/FragGFN/RxnFlow/SCENT × sEH/DRD2/6TD3/ClpP) — closing the surrogate-vs-docking budget mismatch (docking was 400 in entry `021`). **Reaction cap kept at 4**: a GPU smoke measured the proposed +1 (→5) at **~3× per-iter compute** + worse size-drift, so it was dropped (deferred sensitivity study). Infra: **3-day auto-requeue as a login-submitted dependency chain** (Balam forbids self-`sbatch` from compute), a **persistent docking server** (kills ~40 s/step cross-env spawn) with busy/idle utilization logging, and per-run `dock_timing.json`. RGFN validated end-to-end on GPU (resume→emit); baseline server-wiring + FragGFN/RxnFlow checkpointing pending. Not committed (branch Hub-Analysis). |
| [029](../Logs/029_hub-batching-vs-best-candidate-campaign.md) | 2026-07-11 | SCENT — hub-batching vs best-candidate budget campaign (reactions/mode) | **[COST NUMBERS SUPERSEDED→033: the fragment double-count is fixed + best-candidate now gets accidental-hub credit; hub-batching's edge is ~1.13×, not the ~1.8× below.]** **The current cost comparison: two SELECTION strategies from scratch under a budget (supersedes the fixed-set framing of 026/028).** best-candidate (top-reward sampled modes, each built independently) vs hub-batching (build a shared scaffold once, diversify its enumerated children); scored on honest count-once nested reactions. Two budgets off one curve: Case 1 modes@reactions, Case 2 reactions@modes(≈oracle calls). AL-ready swappable strategies (`glue/samplers/lsdflow/campaign.py`, identical `CampaignResult`, **disjoint inputs** so best-candidate never depends on hub data); analysis in `experiments/lsd_hubs/campaign/`. **Result (sEH, job 70295):** hub-batching roughly HALVES synthesis cost per hit — 300 modes in **820 reactions (2.73/mode)** vs best-candidate **1,479 (4.93/mode)** at the default diversity cutoff 0.5 (0.7 gives 706 vs 1,445); **33 vs 16 modes** at a 100-reaction budget; both fully diverse (300 scaffolds), same quality (best sEH 8.4). The win is **scaffold amortization** (all 300 modes from ONE depth-1 hub, built once + 300 cheap diversifications) — NOT intermediate concentration (hub-batching uses *more* distinct intermediates, 268 vs 200). Trade: hub-batching spent 9,428 reward-gen evals (31/mode) enumerating; best-candidate reused sample scores (0). Not committed (branch Hub-Analysis). |
| [028](../Logs/028_scent-recipe-reruns-nested-cost-model.md) | 2026-07-10 → 07-11 | [OUTDATED] SCENT — synthesis-recipe re-runs (sEH/DRD2) + nested-amortization cost model | **Charging SCENT's promoted intermediates honestly ~doubles the true cost the old metric reported; hub-batching saving unchanged.** Re-ran the fragment-promoting SCENT targets with `--log-recipes` (sEH job 70180, DRD2 70184; 1,600 fragments + **1,600 synthesis routes** each; sEH **438 nested** fragments-from-fragments vs DRD2 6), then the dependent frozen 30k hub analyses (70189: 20,519 hubs/2,709 multichild; 70190: 28,865/864) — all COMPLETED over the weekend via an `afterok` dependency chain. New `dynamic_amortization.py` charges each distinct promoted fragment **once** per costed library (reactions primary, exact nested via routes; SCENT $ secondary), + per-molecule composition capture (`compositions.json`). Result (flow acquisition): sEH base 1.15 → **augmented 2.59** rxn/mode hub (indep 1.77 → 3.21; +98 rxns/$348 for 77 shared fragments); DRD2 1.66 → **2.25** (indep 2.49 → 3.08). The saving is preserved (shared build cancels) — batching still wins; the *absolute* cost was undercounted. 6TD3/ClpP don't promote at 400 iters (deferred). Fixed a DRD2 hit-bar bug (inherited sEH's 7.0; DRD2 proxy is 0–1 → use 0.5). Not committed (branch Hub-Analysis). |
| [027](../Logs/027_lsdflow-scent-crossenv-hub-extraction.md) | 2026-07-10 | [COST DECISIONS SUPERSEDED→029] sEH / SCENT — LSD-Flow cross-env hub extraction from the cost-aware flow field | **SCENT wired into the hub analysis; batchable hubs present in the cost-aware model too.** Cross-env bridge (worker in the `scent` env + in-process client; SCENT's package is also named `rgfn`) lets the harness sample SCENT with **no harness changes** — every downstream step (ranking/acquisition/cost/diversity/TB-integrity) runs unchanged. Validated end-to-end on the 5,000-iter patched sEH checkpoint (entry `024`, job 70066): forward policy + logZ clean (74.33 = trained value), guidance sidecar loads → **P_B recovered exactly** (the cost-tilted trained policy, §5). At N=2k: 19 multi-child hubs (best sEH 8.2), hub-amortized **2.49** vs independent **3.92** rxn/mode (as in RGFN). Preliminary flow-vs-visitation correlation **0.35** (vs RGFN's 0.086 @ 10k) — a candidate cost-guidance TB-integrity signal, pending the matched-N run. **Addendum (same day):** analyze the *faithful full* SCENT — building from a checkpoint leaves it restricted to 418 base fragments, so freeze the dynamic library (418 + **1,600 promoted**) via `on_update_fragments_library` for both sampling + enumeration (job 70179, base-lib, cancelled). Built + validated **`enumerate_hub_children`** (frozen library, exhaustive: depth-0 hub 3,975 paths, 2/2 sampled recovered). Cost decisions: reactions primary + SCENT $-cost secondary, each promoted fragment charged once per costed library, exact nesting → new **synthesis-recipe logging** (`recipe_logging.py` monkeypatch, `--log-recipes`) records each promoted fragment's route (feeds nested cost + a future chemist "make these intermediates" view); recipe re-run **job 70180**. Next: nested-amortization cost model + the §8 hub-coincidence study. Not committed (branch Hub-Analysis). |
| [026](../Logs/026_lsdflow-hub-modes-dropoff-funnel.md) | 2026-07-10 | [COST METRIC SUPERSEDED→029] sEH / RGFN — paper-comparable hub "modes" + the filter-dropoff funnel | **Mode definition aligned to the papers; the binding filter — not similarity — is the dominant dropoff.** Redefined a "mode" to match upstream `TanimotoSimilarityModes` / `[bengio2021gflownet]`: reward-gated + best-first greedy sphere-exclusion (ECFP Morgan r=3, 2048, 0.7), replacing the earlier structure-only ECFP4/0.65 Butina; also fixed `_per_mode_cost` (one representative per hit-mode). New modular analysis home `experiments/lsd_hubs/` with `dropoff/funnel.py` (per-hub filter funnel; reuses `validation/lsdflow` mode primitives). On the 30k sEH enumeration (job 70140, 12 hubs, 4586 raw one-reaction children): binding gate dominates — 30% survive sEH≥6, **3% ≥7, 0% ≥8**; Tanimoto dedup gentle (raw→structure 2.3×, 135 ≥7-hits→71 hit-modes). **Depth-0 fragment hubs are structurally diverse but yield 0 high-affinity hits; depth-3 hubs carry the hits (19–29 hit-modes) and amortize ~3.6× reactions/mode.** ⇒ breadth from fragments, hits from depth. Not committed (branch Hub-Analysis). |
| [058](../Logs/058_docking-matrix-complete-six-cells.md) | 2026-08-11 | The hub-batching advantage measured against REAL GPU docking, on every cell we can run (ClpP + 6TD3 × RxnFlow/SCENT/FragGFN) | **The surrogate result survives contact with real docking: hub-batching is cheaper in all 6 docking cells, 2.43–4.43× (median 2.76×), and leads at all 42 gate points tested — with all TWELVE arms reaching the full 300-mode budget, so nothing is flattered by a starved baseline.** Closes `055`'s biggest gap (380/380 wins were all surrogate-scored) and supersedes `057`'s "docking half is a TWO-generator comparison": FragGFN docking is now wired and smoked, making it three (RGFN excluded by the training gate, not by missing code). **2,022,682 docked children** over 1,200 hub enumerations, **412.5 GPU-h** of enumeration. At each cell's calibrated bar (ClpP −8.0, entry `045` AUROC 0.895; 6TD3 −2.0, provisional): rxnflow_clpp 1.223 vs 2.977 (**2.43×**, 34 hubs), scent_clpp 1.290 vs 3.420 (**2.65×**), fraggfn_clpp 1.093 vs 4.840 (**4.43×**, only 7 hubs), rxnflow_6td3 1.193 vs 2.977 (**2.50×**), scent_6td3 1.157 vs 3.320 (**2.87×**), fraggfn_6td3 1.533 vs 5.000 (**3.26×**). **Threshold sweeps** (7 gates/cell, re-scoring already-docked children — no GPU): edge grows monotonically as the bar loosens (scent_clpp 1.63×@−11.0 → 2.65×@−8.0; rxnflow_6td3 2.06×@−4.0 → 2.54×@−1.0), and **only the two strictest ClpP gates are pool-limited** (scent_clpp 280 modes @−11.0, rxnflow_clpp 30 @−10.5) — which is what makes **−8.0 the defensible ClpP bar** rather than a convenient one, while for fraggfn_clpp the ClpP gate is effectively **NON-BINDING** (300 modes at every gate −11→−8, matching its 91.5% smoke qualification). Each bar-point reproduces its campaign number exactly (bit-identity). **Measured compute inverts the surrogate picture**: docking is **87.5–99.7%** of enumeration cost here, versus flow-extraction up to 97.8% for rxnflow_seh in `055`; hub-batching's own walk consumed only 0.96–18.53 h of the 26.1–143.1 h per cell. **Automation** (`autoharvest.sh`, 215 rounds over 4 days) merged + evaluated + swept + backed up each cell unattended, detecting completion from ARTIFACTS because the slices ran on two unfederated schedulers sharing /scratch (a Balam job cannot depend on a Trillium job id); readiness is delegated to the strict merge so a partial set can never be promoted. **Four bugs found by executing paths rather than trusting them:** `--gates "-11,..."` (argparse reads a leading `-` as the next option → needs `--gates=`); `gate_curve.py` needs the `rgfn` env not `base`; it defaulted to child policy `reward`/K=0/no-snapshot for EVERY generator — right for the baselines, **wrong for SCENT**, and it does not crash, it silently yields a sweep incomparable to the cell's own campaign; and **`best_reward` was direction-blind** (unconditional `max()`), so every docking cell reported its WORST qualifying molecule as best (rxnflow_clpp −8.0, exactly the bar, beside median −8.9) — fixed, summaries regenerated, every other field bit-identical (re-confirming determinism); cost metrics were never affected. Corrected best dockers: scent_clpp **−14.2**, fraggfn_clpp −13.5, scent_6td3 −7.398. **Operational:** 8/12 scent_clpp slices hit the 8 h walltime at 147/200 hubs, and the per-10-hub partial flush turned that into a **top-up** (53 missing hubs, 8 slices, nothing re-docked) — but sizing from the raw oracle rate was wrong by **2.3×** (11 min/hub predicted, ~25 actual; the cross-env bridge is the gap), so size slices from observed per-hub time. Also: **editing a script does not reach an already-running loop** — a `--gates` fix committed 11:13 never applied to cells harvested at 20:21/03:06/05:37 because bash had already parsed the function; all 6 sweeps re-run by hand. **Caveats:** one seed/checkpoint per cell, no error bars; cross-generator MAGNITUDES not comparable (best-candidate's own cost differs, 4.84–5.00 vs 2.977, so FragGFN's ratio is partly a worse denominator) and FragGFN stays a cost-model control (attachments ≠ synthesis steps); `log_reward` is cross-generator INCOMPARABLE (each its own beta·clip, SCENT unclipped to 56.8 vs FragGFN clipped at 10) — only raw `reward` is; 6TD3's −2.0 bar has no AUROC calibration behind it. **COMPLETENESS AUDIT** (run because 8/12 scent_clpp slices died to walltime): hub coverage is **exact in all 6** — merged hub set set-equal to `hubs.csv` (200/200), 0 duplicates, 0 hubs in >1 slice (so scent_clpp's of12+of8 union was genuinely disjoint), and a truncated hub is **impossible by construction** (the worker appends a hub with its COMPLETE child list before calling the flusher, so a mid-hub kill drops it entirely) — 0 hubs below 10% of any cell's median child count. **BUT per-hub enumeration is NOT child-exhaustive for the two SCENT cells:** `ENUM_MAX=4000` binds only on SCENT (baselines top out at 1680–2099), capping **25 hubs on scent_clpp / 51 on scent_6td3**, and capped hubs are INSIDE the walk (5 of scent_clpp's 37 walked incl. rank 1; 7 of scent_6td3's 17 incl. ranks 0–1). Measured via `Σ_x P_F(x|h)` (entry `050`'s own normalization readout, which closes to ~1.0 on uncapped hubs, validating it): scent_clpp capped hubs retain only **0.1039** of reachable forward mass (SEVERE, ~90% lost) vs 0.9945 uncapped; scent_6td3 **0.9288** (mild) vs 0.9977. The cap truncates by depth-first action-index order — deterministic but arbitrary, neither top-scoring nor random. **Bias direction understates us** (fewer children/scaffold → fewer modes per build → more hubs needed for 300 modes → higher rxn/mode), so 2.65×/2.87× are conservative — mechanism, not measurement. **PRE-EXISTING, not docking-specific:** `scent_seh` (57 capped, median capped mass **0.5555** vs 0.9998 uncapped) and `scent_drd2` (31) share it, so it affects every SCENT cell in `050`/`055`. **Entry `050` needs an interpretation correction:** its stated criteria (0/200 exceed 1, 0/200 below 0.01) both CONFIRM, but it read SCENT's low-mass tail as "genuine stop-probability mass at hubs the policy likes to terminate at" — it is the 4000 cap; its "independently confirms the enumeration is exhaustive" holds for the 143 uncapped scent_seh hubs, not the 57 capped. Jobs 72493–72663 (Balam) + Trillium `7213xx`. `42eeb26`, `bf494c0`, `b36c3d5`. |
| [059](../Logs/059_sparrow-batching-competitor-arms.md) | 2026-08-11 | sEH — handing the batch optimizer OUR OWN molecules (BC-SB) and our own enumerated children (BC-Enum-SB) | **The strongest non-flow competitors do NOT disprove the method, and the harder one shows WHY.** Both use SPARROW-Batching (SB — SPARROW as a competitor choosing a batch under a reaction budget, vs SPARROW-Verifier auditing our arithmetic; see the naming section). At a **300-reaction budget**: hub-batching **228 distinct** molecules · **BC-SB 47.7 ± 1.5** (n=3 seeds, from the full 21,001-candidate pool) · **BC-Enum-SB 14** (from 131,474 enumerated children of 64 reward-picked hubs). **SPARROW WINS on cost per molecule and we should say so** — 1.02–1.07 rxn/candidate vs our 1.32 — because it optimizes exactly that and has no diversity term; it loses ~5×/16× on molecules that are actually DIFFERENT. **MECHANISM MEASURED, not inferred:** BC-Enum-SB concentrates its picks on **3 hubs at a 100-rxn budget (78% off one)**, and `rxn/candidate` sits pinned at **≈1.02 at every budget** — once a hub is built each further child costs ~1 marginal reaction, and same-hub children share their whole route prefix. ⇒ **enumeration alone is NOT the ingredient; spreading selection across hubs is.** Solver hit no ceiling (21k and 50k pools both Optimal). **Caveat: report RAW COUNTS — at tight budgets BC-Enum-SB yields 2–3 distinct molecules, so any per-distinct RATIO swings ~2× on one molecule.** Also fixed two silent failures: `$HOME` read-only on compute made 3 jobs "COMPLETE" in 12 s doing nothing, and the sweep reported that filesystem error as a *"practical ceiling"* — a solver-limitation claim manufactured by a bug. `debug` partition is **2 h** (not 1 h) and starts instantly ⇒ smoke there first. **[056] tempered in the same pass** (n=1 on the competitor; the honest framing is a density/cost TRADE). |
| [061](../Logs/061_s3gfn-competitor-replicates-seh.md) | 2026-08-13 | sEH — giving the competitor the same error bars we give ourselves (S3-GFN seeds 43/44, full pipeline re-run) | **STUB — jobs running.** The headline of [056] is the last arm in the benchmark with no replicate: our side is n=3 (**124.3 ± 6.1** reactions for 100 modes, SPARROW-priced — and the figure quotes seed 42's **131**, the WORST of the three, so the drafted number is conservative by construction) while the competitor is a single training run plus a single MultiAiZ planning run. Replicating it means re-running the WHOLE pipeline per seed, not re-solving: MultiAiZ is set-based, so a molecule's route depends on what else was planned alongside it. Chains `73364→73365` (seed 43) and `73366→73367` (seed 44), pinned to seed 42's own budget ladder (50…1000) and greedy mode points (25…150) so the curves are read at the same x-positions. **Two findings already banked.** (1) `external/s3gfn/data/envs/zincfrag_hb105/` was GONE — lost when the s3gfn clone was recreated 2026-07-30 — and the first smoke reported **COMPLETED, exit 0** while having crashed on the missing `template.txt`, i.e. [059] Method 5's silent-success mode in a new place; caught in 8 min on `-p debug` instead of 4 h. Rebuilt (178,622 blocks) and checked FAITHFUL rather than merely present: the re-smoke's `synth_ratio` landed at 1.6–6.3% against that env's documented 4.7% prior. (2) A real bug in averaging the competitor across seeds — SB sweeps the reaction BUDGET while the diversity-aware greedy sweeps MODE POINTS, so averaging the greedy over reactions intersects on `used_rxns`, which no two seeds share, silently yielding an empty curve and a missing readout; found by dry-running the multi-seed path against synthetic stand-ins before the real data existed. New `build_s3gfn_pools.py` (`--verify` reproduces all four seed-42 reference pools bit-for-bit) + `submit_s3gfn_replicate_routes.sh`. Headline figure gained the Logs/059 arms in the same pass. `5a866d9`, `5784961`, `8e0e4ca`. |
| [062](../Logs/062_diversity-aware-sparrow-batching.md) | 2026-08-18 | sEH — switching ON the batch optimizer's OWN diversity mechanism ([fromer2025diversity]), on both competitor arms, read at the PRIMARY 100-reaction budget | **At the benchmark's primary readout (R=100) we deliver 82 distinct molecules vs the best competitor arm's 24 — ~3.4× — while ALSO being cheaper per distinct molecule (1.22 vs 1.67 rxn), so it is not a diversity-for-cost trade.** [059] beat a diversity-BLIND optimizer, inviting the obvious objection. [fromer2025diversity] (same group as SPARROW) adds a diversity weight; our clone ALREADY had it (dated 2025-06-30) — configuration, not an upgrade. Swept λ ∈ {0,0.1,1,10,100} × budgets on both arms, clusters = OUR τ-modes (the paper defines clusters as caller-supplied). **The weight is load-bearing but saturates short of us:** BC-Enum-SB goes 2→24 distinct from λ=0→1 (12× on their own knob) and gains NOTHING at λ=10. **⚠️ CORRECTION TO THE FIRST PASS:** this entry originally reported the advantage 'nearly vanishing' (1.05–1.07×). That number is CORRECT but was read at ~290 reactions — ~3× the headline budget, and the regime where OUR pool is nearly exhausted (we plateau at ~232 distinct). The curves are not parallel, so no single ratio describes them: 3.4× at R=100, 1.07× at R≈290. **Mechanism = hard constraint vs soft penalty:** SPARROW trades diversity against reward under a cap, so at a tight cap the cheapest reward is another analogue off an already-paid-for intermediate (λ=0 returns 98 compounds = 2 distinct); our greedy REFUSES anything within τ, so it pays to move away from step 1. A soft penalty loses to a hard constraint exactly when the budget is small — the one-plate regime. **Candidate-count confound CLOSED on the arm that matters:** BC-Enum-SB saw 131,474 enumerated candidates (1.8× our 74,606 reward-gen calls) and still lost 3.4×; a within-method control bounds the elasticity — doubling candidates (`reward`→`free_frag`, 37,774→74,606 calls) buys only +17% distinct (70→82). Still OPEN for BC-SB (29,997 calls, 0.4× ours). **Effort axis:** our selection ~1 s; the optimizer at a 2 h cap failed to converge on 25 of 36 points (λ=0/0.1 never capped — the diversity term is what makes it hard). **Correctness bug that produced a publishable-looking wrong number:** CBC reports `Optimal` when it merely ran out of time (pulp 3.3.2 `sol_status` doesn't distinguish). Tell: budget-1200 returned LOWER objective (4893) than budget-800 (5760). Now detected by wall-clock; capped rows are LOWER BOUNDS ⇒ true ratio ≥3.4×, i.e. they FLATTER the competitor. **Rejected a hard 'touch ≥K clusters' rule on evidence:** sphere exclusion guarantees REPRESENTATIVES differ, not members — 75 families → 54 distinct via arbitrary members; a 25-family constraint gave 14. **Prune-and-reprice is symmetric** (no-op on hub-batching: 100 in, 100 out). **Pair gallery** (`results/paper_pair_gallery/`, split into closest/distant figures) shows our worst pair at exactly τ=0.50 (the constraint's floor) vs 0.87–0.90 for both competitors, whose closest pairs differ by one acyl cap on a shared scaffold. Jobs 73396-73400, 73564-73570. sEH only; R=100 competitor rows at λ=1/10 are TimeLimit (lower bounds). **UPDATE 2026-08-19 (jobs 74093-74100):** the 3.4x rests on a `TimeLimit` competitor row = LOWER BOUND; both replicate seeds CONVERGED higher (32/33), so **quote ~2.5x**. The converged competitor is also CHEAPER per mode (1.03 vs our 1.22), so the 'we win on both axes' claim does NOT survive — the COUNT advantage does. Ratio is cross-seed (ours n=1 at gate 7.0). Candidate-count confound now CLOSED on both arms: a 15x sampling increase buys BC-SB only +35% (43->58 distinct), near-flat past k=5k. An earlier smoke-test claim that MORE sampling made BC-SB WORSE was an uncapped-vs-`--top-n`-capped artifact and is WITHDRAWN. **UPDATE 2026-08-20 (jobs 74441-74442, 74444-74446):** our side is now **n=3 and very tight — 82/81/82 distinct @ R=100 (mean 81.7, CV 0.7%)** — so the ratio is WITHIN-seed at last: **2.51x** on the two seeds where the competitor converged (2.45 / 2.56); seed 42's 3.42x still excluded as a capped lower bound. **NEW ARM HB-Enum-SB returns a NULL:** feeding SPARROW candidates enumerated from OUR flow hubs instead of reward-picked hubs gives 42/37/23 (all `Optimal`) = mean 30.0 on converged seeds vs BC-Enum-SB's 32.5 — no advantage, and the sign FLIPS between seeds (43: +12%, 44: -28%). Stated as 'no evidence of a difference', NOT 'no difference': HB-Enum-SB's CV is **29.0%** against our 0.7% (SPARROW's selection is far more sensitive to its candidate set than our greedy is), and a confound is live — these enums are 200-hub/top-1000 vs BC-Enum-SB's 64/100, so net WIDTH differs as well as hub ranking. **MOST IMPORTANT — the selection-only contrast is SMALLER than the headline:** on the SAME enumeration at the SAME gate (matrix16/scent_seh, 7.0, seed 42) ours=70 vs HB-Enum-SB=42 = **1.67x**. So a real share of the 2.51x comes from candidate ORIGIN, not selection — selection is worth ~1.67x on identical candidates. One seed only (seeds 43/44 matrix cells sit at bar 5.0), and our two gate-7.0 seed-42 numbers (82 vs 70) differ by prebuild_k 0 vs 20, so the 1.67x is directional, not a settled constant. Costs quoted as `cost_kept_rxns` per the pool-exhaustion rule. New `experiments/lsd_hubs/campaign/{submit_seh_seed_curves.sh,submit_hb_enum_sb.sh}`. **BAND LANDED same day (jobs 74482-74485) — the 1.67x is WITHDRAWN as the value:** same-enum, same-gate, within-seed at n=3 gives **2.27x (K=20, sd 0.76; per-seed 1.67 / 2.03 / 3.13)** and 2.87x on same-enum K=0 rows. 1.67x was seed 42, the LOWEST of three. **So the 'candidate origin explains a big share of the 2.51x' reading is withdrawn — selection is worth ~2.3x against a 2.51x headline, so most of the advantage IS selection.** Spread is driven by the COMPETITOR (HB-Enum-SB 42/37/23, CV 29%), not us (ours 70/72/75 at K=20). Secondary: on a fixed enum `prebuild_k=20` COSTS modes at R=100 (79->75, 83->72) — cheaper oracle calls (Logs/037) are not free on the reaction axis. **UPDATE 2026-08-23 (jobs 74633/74634 TIMEOUT at 20 h):** on the CORRECTED (uncapped) enumerations the pools are 123k-150k above gate and **the MILP does not converge** — 12 h cap + gapRel 1e-3 both TimeLimit, and the capped answers do not trend (seed 43 reads 12 / 27 / 37 distinct across three pool sizes with no ordering). **HB-Enum-SB is therefore reportable only as a LOWER BOUND on the competitor, i.e. it flatters us, and cannot carry a ratio.** The converged rows all used pre-fix truncated enums so they are unusable too. Per CLAUDE.md this is solver-truncated = COMPUTE-limited, a different claim from pool-exhausted. Itself a result: our greedy answers the same pool in ~1 s. 2.51x BC-Enum-SB headline UNAFFECTED (different arm, converged). **UPDATE 2026-08-25 (jobs 74727/74728): DRD2 CONVERGES — the arm is NOT bound-only.** All six budget points `Optimal` (2 h / 3 h 54 m vs a 12 h cap): seed43 ours 78 vs 28 = **2.79x**, seed44 78 vs 15 = **5.20x**. **Caveat that must travel with it:** DRD2 was capped at `--top-n 50000` to match its own competitor rows while sEH ran uncapped at 123k-150k, so the DRD2 MILP saw ~1/3 the variables — the two targets are NOT comparable on pool size. Spread is the COMPETITOR's (ours 78/78 identical; theirs 28/15), so quote the RANGE not a 2-seed mean. rxn/mode near parity at this budget (theirs 1.25/1.40 vs ours ~1.28), unlike at 300 rxns. Both new guards fired green (provenance OK + run-fragment coverage 100%) — first clean end-to-end run. **Status is SPLIT: converged on DRD2, lower bound on sEH.** |
| [063](../Logs/063_fourway-matrix-complete-three-seed-comparison.md) | 2026-08-14 | Four systems × four generators — the complete **three-seed** comparison (all 48 runs done; entry `030` finished) | **The single-seed rankings HOLD with error bars on all 4 systems — and RxnFlow's instability is now a measured result.** All 48 runs (4 gen × 4 sys × 3 seeds, 5,000 iters) complete + backed up (0 gaps). Aggregated (mean±std over seeds): **SCENT leads reward everywhere** (ClpP median Vina **−11.4** vs RGFN −9.1 / FragGFN −9.4 / RxnFlow −7.4; sEH 7.61; best 6TD3 top-10 6.67), tightest spread; **RxnFlow trails everywhere AND is the only unreliable one** — dedups to uneven N (DRD2 = 1000/849/260) and mode-collapses (DRD2 modes 326±213, ClpP 63±40); **RGFN drifts largest** (MW up to 685 on 6TD3); **FragGFN foil** matches reward, `has_route=0`, and at the corrected `max_nodes=6` is now the **most drug-like** (QED 0.68 / MW 380 on DRD2). **DRD2 saturates** for all four (top-10 = 1.00). Metrics via `diversity.py` (ECFP r3/2048, modes sim≤0.7, bars ClpP −8.0 / 6TD3 −2.0); `experiments/fixed_reward/scale5k/analyze_matrix.py` → `analysis/{per_cell,aggregate}.csv`. **Caveats:** RxnFlow counts N-confounded (read medians); sEH proxy≠activity; AiZynth 3-seed external-synth cross-check still pending. Not committed (branch Hub-Analysis). |
| [064](../Logs/064_surrogate-three-seed-error-bars.md) | 2026-08-18 | How much of the advantage is luck? Three seeds per surrogate cell — plus a headline-bar change | **The advantage is stable: median seed CV 3.2% on a mean 3.29x across the five cells with three complete seeds and both arms at the full budget — the same order as the ~2% oracle noise from `058`, so neither known source of run-to-run variation explains the effect.** Closes the one caveat that topped BOTH `055`'s and `058`'s own gap lists. Needed no training: `063`'s campaign had already trained all 16 surrogate checkpoints (4999-5000 on every cell), so this is enumeration + selection only, 113 GPU-h. Per cell at the new bars: fraggfn_seh **4.12x+/-1.9%**, fraggfn_drd2 3.95x+/-3.5%, scent_drd2 3.24x+/-2.9%, scent_seh 3.10/2.92/2.94 = **2.99x+/-3.2%**, rgfn_seh **2.17x+/-6.4%**; rxnflow_seh 2.14x and rxnflow_drd2 1.75x are n=3 but NOT budget-comparable (still pool-limited at 5.0) and rgfn_drd2 is n=1 (seeds 43/44 still enumerating at 100/200 and 70/200). **HEADLINE sEH BAR MOVED 7.0 -> 5.0.** At 7.0 an arm falls short of the 300-mode budget in 3 of 8 cells on EVERY seed, so those ratios were measured over starved libraries and understated us: rgfn_seh reads 1.34x with best-candidate assembling only **9 modes** at 7.0 versus **2.32x with both arms at 300** at 5.0. `055` saw this on one seed; three seeds prove it systematic. Justified by calibration (`034` AUROC 0.76/0.68, 0/2,315 actives reach 8.0; `051` meaningful range ~5-6), and 7.0 stays in `threshold_variants` as the paper-comparable variant. **A phantom seed ORDERING (42>43>44) was OUR TOOLING, not the models** -- and finding it is the entry's methodological result. Three checks: (1) models equivalent (scent_seh sample reward 7.486/7.498/7.472, 83.4/83.7/83.2% above gate, yet 300/106/86 modes; rxnflow_seh's seed 44 is the BEST model of its three at 7.824 vs 5.641/5.358 -- opposite of the alleged order); (2) no code drift (seed-42 campaign reproduces `055` bit-for-bit at 1.303 r/m / 300 modes / 57 hubs, ruling out `062`'s mid-flight-edit mode); (3) the fingerprint -- seeds 43/44 walked exactly **10 hubs** and stopped `pool_exhausted` vs seed 42's 57 hubs stopping on budget, and 10 is a `PartialFlusher` boundary. Cause: `harvest_surrogate.sh` tested readiness with `[ -s enum_children.json ]`, but every worker flushes a PARTIAL enumeration every 10 hubs as walltime insurance, so the file appears minutes into a 15-h run and grows -- **14 of 16 campaigns ran on partial pools**. The 2 exceptions were the FragGFN cells (28-min enumerations, done before the first poll), which is exactly why FragGFN was the only cell showing no trend: **a control hiding in plain sight**. Fixed to hub COVERAGE vs `hubs.csv` (the standard `merge_docking_slices.sh` already enforced), verified to refuse 70/200 and accept 200/200; all 14 recomputed; nothing incorrect reached git (all affected dirs untracked). **Third time 'ready' meant less than it sounded** (`docking_wired` in `057`, the stale epoch sidecar, now this) -- each a cheap proxy standing in for the real property. Also corrected: my DRD2 bar sweep had used sEH gates (5/6/7) against a 0-1 probability scale and returned nothing; re-run at DRD2's declared 0.5/0.7/0.9. **Cell-property caveats, not bugs:** rxnflow_drd2's hub pool differs by seed (**200/177/105**, `pick_hubs` capped by distinct parents among the top-1000, `031`), so part of its spread is library size; both rxnflow cells stay pool-limited at 5.0 and are excluded from the median. Three seeds give a spread, not a CI; FragGFN remains a cost-model control. `1ef9d9f`, `09698e7`, `b706304`. |
| [065](../Logs/065_reaction-budget-readout-matrix.md) | 2026-08-18 | The 16-cell matrix re-read on the PRIMARY axis: modes at a fixed 100-REACTION budget, not reactions-for-300-modes | **The headline survives the axis change at 2.88x (median, 25/26 comparable cells, min 1.67x, max 4.80x) — close to but BELOW [064]'s 3.29x on the mode axis, and 2.88x is the number to quote.** Pure re-read, NO new compute: both readouts are projections of the same per-step greedy ordering in `curve_{hub_batching,best_candidate}.csv`. **The advantage GROWS with budget** — 2.50x @ R=50, 2.88x @ 100, 3.02x @ 150, 3.12x @ 200 — so the headline is the second-most-conservative of four measured budgets, and [064]'s 3.29x sits at the top of the range rather than outside it. Holds in EVERY cell; no cell ties or loses. **Each cell is labelled budget-binding / pool-exhausted / mode-capped** per CLAUDE.md, and only both-budget-binding pairs count: `rxnflow_drd2` seed44 is excluded because best-candidate was POOL-EXHAUSTED at 32 (ran out of candidates, budget unspent) — counting it would credit us a cost win the data does not support. **Seed scatter roughly DOUBLES on this axis: median CV 7.0% vs 3.2%**, concentrated in three cells (rxnflow_drd2 20.4%, rxnflow_seh 16.0%, rgfn_seh 14.9%) while scent/fraggfn sit at 2-4%; cause not yet separated (early-curve point vs generator behaviour) and flagged as open, not asserted. **0 gate mismatches** — all 29 cell readings already sat at `targets.py`'s headline gates (sEH 5.0 after [064], DRD2 0.5, ClpP -8.0), so nothing needed re-evaluation. **6TD3 EXCLUDED** (3 cells): its -2.0 bar rests on WARHEAD-matched, not property-matched decoys (all carry the CR8 purine; MW only range-bounded 250-650, no other property matched), so the cutoff is not yet defensible — parked pending a property-matched decoy set. New `experiments/lsd_hubs/matrix16/reaction_axis_readout.py` + `results/reaction_axis/{reaction_axis,reaction_axis_ratio}.csv`. **FIGURE ADDED 2026-08-20:** `results/reaction_axis/reaction_axis.{png,pdf}` from new `matrix16/plot_reaction_axis.py` — per-cell dots at R=100 (every seed individually, `n=` shown per row) + median-ratio-vs-budget with the per-cell range as a band. Hollow marker = THAT arm was pool-exhausted (not both members of an excluded pair). |
| [066](../Logs/066_drd2-oracle-gate-saturation.md) | 2026-08-19 | DRD2 — is the 0.5 hit bar miscalibrated, or has the generator saturated the oracle? | **The bar is SOUND; the generator has SATURATED it. 0 of 3,408 true-negative drug-like molecules score >0.5** (CDK12-DDB1 known glues n=160 median p=0.007; CDK12 decoys n=248 p=0.002; 3,000 ZINC building blocks p=0.003) **against 0.980 median for our 29,822 generated molecules.** DRD2 was the ONLY one of four targets with no oracle-validation entry — its 0.5 came from [028] as a BUG FIX (sEH's 7.0 was leaking onto a 0-1 scale and passing nothing), i.e. it is the midpoint of the range, never a calibration. Checked because 97.5% of our pool clears it, which is the exact pattern [045] diagnosed as a broken bar on ClpP. **CONSEQUENCE FOR THE PAPER: the gate is non-binding at EVERY declared variant** (0.5→97.5%, 0.7→94.6%, 0.9→83.0% of distinct molecules pass), so **DRD2 cells are a DIVERSITY-ONLY comparison** and must NOT be pooled with sEH, where the bar removes a real fraction. **NOT shown (open):** every molecule tested is a true negative, so this is SPECIFICITY against unrelated chemistry, not PRECISION — no known DRD2 actives were scored, and this oracle (TDC/Olivecrona SVM) is a known reward-hacking target, so 0.98-on-everything is also what being gamed looks like. A 79-171 Da median size gap (ours 515 vs negatives 343/436/184) is a live alternative explanation, cf. [007] where exactly that confound explained most of an apparent signal. Fix = the [045] protocol (known actives vs property-matched inactives). **Does NOT disturb the benchmark:** all arms share the identical oracle, so a gamed oracle is gamed equally and the batching comparison survives. Measurement only, no repo files changed. |
| [067](../Logs/067_naive-vs-pruned-pool-design.md) | 2026-08-19 | Two ways to pick the 500 molecules we price: the **naive** top-500-by-score pool vs a **pruned** pool of 500 mutually distinct molecules, each reporting reactions/candidate AND reactions/mode | **The pool construction, not the generator, decides whether a cell is measurable — and Saturn is priceable after all.** Saturn sEH s42's top-500 holds **18** distinct molecules, on which basis it had been written off; the SAME 2,000 samples hold **338** overall (its training history holds 1,251), so the collapse sits in the highest-scoring band, NOT in its search. Pruning recovers those 338 with no config change, no extra sampling, no extra route planning, and 338 clears the ~100 modes a 100-reaction budget could buy, flipping the cell from `pool-exhausted expected` to `budget-binding possible`. Neither pool alone is honest: naive-only says "Saturn cannot produce diversity" (false), pruned-only hides an effect a chemist taking the top 500 would hit. Pool ceilings (sEH, gate 7.0, tau 0.5, s42): REINVENT 201 naive / 963 available / 500 pruned at scan depth 1,127; S3-GFN 206 / 732 / 500; Saturn 18 / 338 / **338 (short of 500, flagged, reported at true size)**. **Pool source resolved to `candidates.csv` uniformly** — it is a fresh sample from the TRAINED FINAL POLICY (`run_reinvent_fixed.py:301`, `run_saturn_fixed.py:285`), the protocol behind all 16 matrix cells; training history is a different object with no comparable form for our own generators, and nothing hinges on it (Saturn naive reads 18 from samples vs 16 from history, 0 overlap). **Three defects the design exposed:** (1) the saturation gate still asked the old fixed-MODE question and would have ABORTED Saturn's cell — the exact exclusion CLAUDE.md forbids — now predicts the stop reason and aborts only below `--min-modes`; (2) `build_s3gfn_pools.py` warned about a short pruned pool then silently skipped it, so Saturn's pool was never written — now emits at true size with a truthful dir name (`_N338`, never `_N500`) plus `pool_meta.json`; (3) these MILP solves CANNOT run on a login node — CBC held 99.8% CPU for 16 min on REINVENT R=50 against `ulimit -t` 3600, so it would have been SIGXCPU'd with no CSV. **CLAUDE.md's "the MILP converges at 100" QUALIFIED: tractability is POOL-dependent, not budget-dependent** — REINVENT's sEH network carries 5,597 intermediates vs S3-GFN's 2,943 for the same ~455 targets, so read `time_capped` before calling any SB row optimal (a capped row is a lower bound, i.e. it flatters us). Regression pin held bit-identical (206 modes at n=500, rate 0.412, 100 modes at 250 candidates); naive pool path verified bit-for-bit unchanged. **NO routed cost numbers yet** — reactions/candidate, reactions/mode and mean pairwise similarity for both variants are what jobs 74439/74457-74462 produce. Not committed (branch Hub-Analysis). |
| [068](../Logs/068_scent-enumeration-cap-correction.md) | 2026-08-20 | The enumeration cap that was quietly shrinking ONE generator's pool — audited across all 16 cells, corrected, and re-measured | **The cap was truncating a lot (pools grew 8-61%) but moved the headline only +0.1% to +3.3%, every change IN OUR FAVOUR — so the published numbers were right and slightly conservative.** `058` found this on the docking half and fixed one cell (+10.6%); a co-agent then found it is WORSE on the surrogate cells that underpin `050`'s headline and `055`'s 380/380 claim — **scent_seh 57 truncated hubs at s42 vs scent_clpp's 25, rising 57->75->82 across seeds**; scent_drd2 31/12/23. Re-enumerated all six SCENT surrogate cell-seeds at **ENUM_MAX=50000**: cap stopped binding everywhere (0 hubs at 50k), children 438,984->644,759 / 474,870->702,574 / 492,471->791,503 (seh) and 344,469->485,398 / 298,491->317,957 / 352,046->503,616 (drd2). Corrected: scent_seh 3.10/2.93/2.96x (+0.1/+0.3/+0.7%), scent_drd2 3.25/3.38/3.31x (+2.5/+1.0/+3.3%). **The co-agent's 50,000-over-my-20,000 call was decisive**: scent_drd2 s44 came back at **17,147** children/hub, above scent_clpp's 14,943 max, so 20,000 would have re-truncated and we would only have found out after re-running everything downstream. **THE MECHANISM, MEASURED — the correction's size is set by WALK LENGTH, not by hub count.** A truncated hub only bites if the walk reaches it, and scent_seh's walk is **8 hubs at bar 5.0, 14 at 6.0, 44 at 7.0**, so the same defect gives +0.1% at our headline bar and **+4.7% at 7.0** (2.60x -> 2.722x vs `050`). That is why scent_clpp moved 10.6% (34-hub walk at its bar) while these barely moved, and it means the headline-bar choice (`5.0`, per `064`) and the cap interact — one sentence in the paper, not two caveats. `055`'s surfaces span bars 4-8 so they shift up to ~5%, ALL upward: the 380/380 claim is unaffected in kind and regeneration is precision, not correctness. **SCOPE: exclusively SCENT, verified matrix-wide** — 12 of 16 cells have ZERO truncated hubs on every seed (maxima 895-3,435), because only SCENT grows its own fragment vocabulary. Audited against the cap each cell ACTUALLY used, not a fixed 4,000. Still truncated: scent_6td3 s42 (51 hubs, recap running) and rgfn_6td3 s43 (**1** hub of 200 — reported, not worth ~160 GPU-h). **Method choice recorded:** re-ran FULL cells (~30 GPU-h) not just the truncated hubs (~15), because submit_cell.sh writes one un-sliced file so a subset run would overwrite 200 hubs with 57 and stitching it back needs a surrogate-side merge path that does not exist — 15 extra GPU-h was the cheaper risk than new code on the artifact behind published numbers. **Two shared-checkout failures, not science:** 12 of 16 docking-recap slices died `ModuleNotFoundError: No module named 'glue'` (a concurrent `from glue...` import added to the per-env artifact writer, which cannot work in a generator env — fixed by loading the dependency-free schema BY PATH, `7325abb`, verified in all 4 envs); and all 4 hub_order repairs died in 6 s with `/var/spool/validation/...` because that launcher derived REPO from BASH_SOURCE instead of \$SLURM_SUBMIT_DIR. `5a9156b`, `7325abb`, `2b7c39c`. |
| [069](../Logs/069_oracle-validation-6td3-drd2-matched-decoys.md) | 2026-08-20 | 6TD3 + DRD2 — the two uncalibrated oracles vs PROPERTY-MATCHED (DUD-E style) decoys | **OPPOSITE VERDICTS. DRD2 PASSES and its 0.5 bar is justified; 6TD3's -2.0 gate COLLAPSES and is not defensible.** Reused `docking_clpp/make_matched_decoys.py` unchanged (MW/logP/HBD/HBA/RotB/charge matched, ECFP4 Tanimoto <0.35 to every active) from the cached background pool — no network. **6TD3:** the actives' scores are IDENTICAL between rows, only the negatives change — AUROC **0.946 -> 0.688**, and at the incumbent -2.0 gate decoy pass-rate goes **1% -> 31%**, enrichment **82.9x -> 2.1x**. **No cutoff rescues it:** the max-enrichment point on the matched set IS -2.02 at only 2.1x (Youden -1.66 is worse); medians are actives -2.20 vs decoys -1.52. Old decoys were warhead-matched but 93 Da lighter (MW 343 vs 436); new ones are MW 440. **AND THE DIFFERENTIAL'S ADVANTAGE REVERSES — contradicts [005]/[007]:** vs warhead-matched, differential 0.946 > absolute T2 0.890; vs property-matched, differential 0.688 < **absolute T2 0.744**. Mechanistically sensible (the differential cancels the kinase-pocket term — right when all decoys share the warhead, wasteful when none do); plausibly a right-sized molecule gains from DDB1's surface without binding the ATP pocket. ⇒ 6TD3 may survive as a RANKING signal but not as a pass/fail gate; keep it out of the headline (it was already parked — this is the number that justifies it). **DRD2:** 2,287 HELD-OUT actives (earliest ChEMBL evidence >=2017) vs 2,265 matched decoys → **AUROC 0.949**, and at bar 0.5 **18.0x** enrichment (0.7 -> 27x, 0.9 -> 35x). **Memorisation gap is NEGLIGIBLE: in-domain AUROC 0.961 - held-out 0.949 = +0.012**, so the number is generalisation, not recall — the check that mattered, because DRD2 alone is scored by a TRAINED model. **Provenance established from the paper:** the SVM is Olivecrona 2017, trained on **ExCAPE-DB** (7,218 actives at pIC50>5 + 100k sampled inactives, Butina 0.4 split), and ExCAPE-DB (posted 2017-03-07) is built from PubChem + ChEMBL — so nothing post-2017 entered the model and a 2017 holdout is SAFE. Residual leak: ExCAPE draws on PubChem too, so a molecule could have entered training via PubChem with a later ChEMBL date, which a date filter cannot see (the 0.012 gap suggests small). Decoys are PRESUMED, not measured, inactive. New `experiments/oracle_validation/{docking_6td3/{prep_actives_6td3.py,benchmark_6td3_gate.py,submit_dock_6td3_matched.sh},drd2_proxy/{fetch_drd2_actives.py,benchmark_drd2_proxy.py}}`; `dock_cluster.py` inputs now env-overridable. Job 74500. |
| [070](../Logs/070_route-artifact-integrity-and-what-a-retrain-buys.md) | 2026-08-24 | All 36 cells audited: do we still hold a usable RECIPE for every molecule we select? | **Only 5 of 36 cells hold a complete recipe, and most of what is missing is UNRECOVERABLE — but no published headline depends on it.** Three artifacts are needed (① `routes.json` from SAMPLE, ② `children[].reaction` from ENUMERATE, ③ `smiles_to_route` recipes from TRAIN); a child's route = ①prefix + ②step, with ③ expanding promoted fragments. Audit: **5 complete** (`scent_{seh,drd2}` s43/s44 + `scent_6td3` s43), **23 missing ①** (all RGFN + all RxnFlow), **6 missing ③** (every seed-42 SCENT cell + `scent_clpp` all seeds), 12 n/a (FragGFN — attachments not reactions, empty file is CORRECT). **Seed 42 is absent from every complete cell.** ③ is unrecoverable (a fragment's route is observable only while it is BUILT; once promoted the model consumes it atomically) — logging regressed for runs launched 07-14→07-26 because SLURM snapshots submit scripts, fixed since 07-29. **① is not cheaply recoverable either — the decisive new measurement:** RxnFlow is `PYTHONHASHSEED`-sensitive like RGFN (same checkpoint, same `--seed`, **61% hub overlap**, 994/1,619), and originals ran with it unset, so a re-sample REPLACES a cell (new hubs → re-enumerate → re-campaign → published numbers move), never repairs it. **HB-Enum-SB does not converge on the corrected pools** (12 h cap + gapRel 1e-3 both TimeLimit; capped answers do not trend — 12/27/37 distinct across three pool sizes with no ordering) ⇒ reportable only as a LOWER BOUND that flatters us, cannot carry a ratio; our greedy answers the same pool in ~1 s. **Every failure was SILENT** — empty routes → SPARROW prices the EMPTY library as `Optimal` at zero cost; wrong snapshot → coverage reads 100% while only 53% of the run's fragments are expandable (549/1,169 bought not built = [049]'s 62.7% with a green light). Common shape: an EXISTENCE check where the real question was a MATCH/CONTENT check. **Headlines unaffected** — 2.93× matrix and 2.51× competitor are count-once, which reads `{smiles, reward, added_promoted}` and never a route. New `docs/RETRAIN_RUNBOOK.md` (what to re-train: 6 cells, ~300-600 GPU-h; what NOT to; 6-item verification checklist) + `docs/ROUTE_DATASET_SCHEMA.md` + `submit_hashseed_probe.sh`. |
| [071](../Logs/071_yield-blindness-of-the-reaction-budget.md) | 2026-08-24 | SCENT / sEH + DRD2 — what a REACTION count cannot see: per-reaction YIELD, and whether hub-batching makes it worse | **The model does pick below-average-yield reactions in 77-87% of steps — but it is following the REWARD, not being careless; the real cost of hub-batching is RISK CONCENTRATION, not yield.** SCENT's library ships a per-template `yield` (0.65-0.95) we had never read. State-conditional test (6,000 steps/cell): chosen yield sits **-0.026 (seh) to -0.048 (drd2)** below the mean of the templates applicable to the molecule actually in hand, and is below it in **76.9/81.8/85.8/86.9%** of steps; compounded over route length that is **7.9/10.6/16.4/16.9%** of expected route yield forgone. **WHY, settled by counterfactual, not assumed:** the library holds two families on the SAME acid+amine reactants (amide **0.75**, direct acid->amine **0.70**) with *identical* reactant applicability and 4 anchored ids each, so the null is a clean 50/50. Re-running the amide on the same reactants and scoring both with the cell's own frozen reward: on DRD2 the amine wins **0.559 vs 0.348 (93.8%/94.4% of 4,000 matched pairs)** — an aminergic target needs the basic amine an amide cannot give — and the policy takes the 0.70 route **99.8/99.9%** of the time; on sEH the reward mildly prefers the AMIDE (amine higher in only 38.0/35.5%) and the policy takes the 0.70 route **below chance (31.7/42.3%)**. Sign of reward preference == sign of usage preference in all four cells. **THE SELECTION-LEVEL RESULT IS A NEW LIMITATION, AND IT IS NOT ABOUT AVERAGES:** at R=100, hub-batching's expected molecules beat best-candidate **2.7x (seh) to 5.3x (drd2)** (44.6/47.3/51.4/51.1 vs 16.7/17.6/9.7/10.0) and its route yield is *higher* on DRD2 (0.535/0.544 vs 0.359/0.356, purely because routes are shorter: 2.0-2.2 vs ~3.4 steps). But molecules off one hub share a PREFIX, so failures CORRELATE: E[molecules] is identical under both failure models (the prefix probability factors out), while sd rises **4.4->7.8, 4.5->8.8, 4.9->23.6, 4.7->14.5** — **94-100% of hub-batching's library sits in shared batches**, largest single batch **63 of 96 molecules**, worst prefix fails w.p. **0.30-0.44**, E[lost to a prefix failure] **13.7-21.4 molecules**. Best-candidate shows ~none (0-14% batched). **NOT REPLICATED — recorded as unresolved:** hub-batching doubles the low-yield share on drd2 s43 (24.2->55.7%) but is FLAT on s44 (31.9->33.0%) and slightly LOWER on both seh cells; two seeds cannot separate a target effect from seed noise, and seed 42 has no routes ([070]). **TRAP RECORDED:** `templates_yields.csv` lists the amide TWICE — 0.95 with an isotope-labelled `[12C:3]` and 0.75 with a plain `[C:3]`; `templates.txt` (what the runs load) has ZERO isotope labels, so 0.75 is operative and reading 0.95 would have inflated this entry's headline ~5x. Its `Family` column is block-style and the plain duplicates are appended with BLANK family cells, so forward-filling mislabels them — family labels there are display-only. `--check` now asserts both. New `experiments/lsd_hubs/campaign/{yield_preference.py,yield_by_strategy.py,reaction_names.py}`. CPU only, login node. Not committed (branch Hub-Analysis). |
| [072](../Logs/072_6td3b-cnnscore-oracle-and-reward-exploitation.md) | 2026-08-21 | 6TD3 — scoring OUR OWN generated candidates against a signal the generator was NEVER trained on | **MEASURED REWARD EXPLOITATION. Our SCENT candidates clear the trained 6TD3 gate 78% of the time (real glues 67%) and the untrained 6TD3-B gate 0% of 400 (real glues 66%, property-matched decoys 1%).** 400 candidates (top-200 by reward + 200 random) docked in ONE pass beside [069]'s 160 glues and 160 matched decoys, scored 4 ways. Trained signal `ddb1_dvina`: ours −2.58 median, beats **85%** of real glues. Untrained `cnnsc_t2`: ours +0.392, beats **2%** — and sits BELOW the decoys (+0.587). `cnnaff_t2` sits between (46%), so the molecules are not uniformly poor, just poor at pose plausibility. **Mechanism:** the old reward is a DIFFERENCE of two docking scores, maximisable by making Tier 1 worse rather than Tier 2 better, with no requirement the pose be physical; the generator's reward reaches **−7.97** where the best real glue is **−4.09**. **Size does NOT explain it** — ours are 165 Da heavier (MW 601 vs 436, itself a finding: the objective does not penalise growth), but inside the glues' own p10–p90 MW band **0%** of ours clear the bar vs **70.3%** of glues; corr(MW, cnnsc_t2) only −0.27. **⇒ 6TD3-B becomes the reward for all new 6TD3 training — as gnina's `CNN_VS` = CNNaffinity x CNNscore, gate 6.718** (superseding an initial choice of cnnaff_t2 alone, which is BLIND to the effect: AUROC 0.521 vs our own candidates, because gnina's affinity head is hinge-trained on high-RMSD poses and so is weakly supervised on a low-confidence pose). CNN_VS is gnina's OWN shipped VS metric: 0.917 vs decoys, 0.946 vs our candidates, keeps 78.1% of glues at 5% FPR. **ALSO CORRECTED: the 'made Tier 1 worse' mechanism is WRONG** — Tier1 +0.02, Tier2 -0.46, so our molecules genuinely dock better; the supportable claim is only that they do so via low-confidence poses**: CNNscore discriminates better (0.923 vs 0.804) but is bounded [0,1] and saturates (real-glue p90 0.987), leaving a GFlowNet no headroom; affinity is unbounded (glues 5.64-9.39) so it is the weaker classifier but the better REWARD, and cnnsc_t2 stays available for a post-hoc gate from the same pass. NOTE cnnaff_t2 was being DISCARDED by the oracle and is now captured (additive; pose selection unchanged), so pre-2026-08-21 enumerations cannot be re-scored without re-docking. New `Docking6TD3BGpuOracle` + `docking_6td3b_gpu` + `6td3b` target entry, all ALONGSIDE the originals** (`RETRAIN_RUNBOOK` §0b, 6TD3 UNPARKED; both oracles stay in the tree so old numbers stay reproducible). Existing 6TD3 libraries do NOT survive re-gating (~0 pass), so this needs the full re-train→re-sample→re-enumerate chain, not a re-gate. **Run-time cost ≈ 0:** same docking pass, different column. Batch scaling (job 74701, 4×A100): 0.828/0.644/**0.517** s/mol at 100/200/**400** — 400 works, so [036]'s 200 ceiling was conservative; `tier1_score` is only **2.3%** at batch 400 (near-fixed ~4 s model load) so **KEEP Tier 1** and have every run emit both oracles' signals. `pose_convert` is now 19.3% and is the real throughput target. **NOT shown:** `cnnsc_t2` is a POSE-QUALITY classifier, not affinity — 'does not dock confidently' ≠ 'does not bind'; and finding one signal our molecules do not game is not proof the new one cannot be gamed (re-run this comparison after re-training — it is now the standing check). One generator, one seed. Job 74833. |
| [073](../Logs/073_parallelizable-groups-in-hub-batching.md) | 2026-08-26 | SCENT / sEH + DRD2 — are the molecules we deliver ones a chemist could make SIDE BY SIDE? **(CURIOSITY — not a publication claim)** | **Hub-batching creates genuine parallel ARRAYS and best-candidate creates none; grouping by reaction alone is the chemistry library, not our selection.** Each selected molecule joined to the single reaction that made it (`enum_children.json` `children[].reaction`, 100% populated, exactly 1 step, `input == hub_input` on all 702,574 seed-43 children) and grouped two ways. **Class A (parent + reaction class = one substrate, N partners):** at R=100 hub-batching gives arrays of **26/27/16/21**, with **0.77-0.89** of the library in arrays >=4 and **0.36-0.68** in arrays >=10; best-candidate on IDENTICAL chemistry/gate/tau gives largest **1-2** and **0.00** at >=4. At EQUAL library size (300 modes, so size explains nothing) best-candidate needs **279/287/299/300 arrays for 300 molecules** -- pure singletons -- vs 54/58/83/85. **Class B (reaction class alone) is NOT ours:** at equal library size the arms are indistinguishable (largest 65-117 vs 57-114; >=10 coverage 0.88-0.98 vs 0.87-0.94) because a dozen reaction classes dominate the library regardless of chooser (cf. [071]). Decomposition is the legible number: HB's 28-molecule Buchwald group = **2 arrays** (26+2); BC's 8-molecule amide group = **8 arrays of 1**. **TWO CAVEATS MEASURED.** (i) **Molecules overstate WELLS** where the substrate is polyfunctional -- one template fires at either of two amines, so 78 molecules = **66 wells** on drd2_s44 (12 wells give 2 separable regioisomers); wells = distinct `(parent, reagent set)`, and keying on the reagent ALONE (a bug caught and fixed here) made BC's Class B read ~5x more concentrated than it is (114 mol as 23 wells). (ii) **A third 'parallelization' filter (>=N reward-passing siblings per (hub, reaction)) is nearly FREE but optimises the wrong thing** -- seh_s43 at N=**800** costs **0 modes** at R=100 (+3% reactions over the full curve) and consolidates 14 arrays into **5** with 0.90 in arrays >=10 (3 plates off 2 substrates); drd2_s44 is free to N=400 and breaks at 800 (196 modes, **pool-exhausted -- flagged, not a cost win**). It guarantees siblings AVAILABLE, not DELIVERED: arrays of 2-3 survive the >=10 filter because the **diversity filter, not availability, bounds array width**. Incidental: seh_s44's whole 80-molecule R=100 library sits on **ONE** substrate -- the late-stage-diversification limit, and the worst case for [071]'s correlated-failure finding. **SCOPE: 1 generator, 2 seeds/target** -- the only cells holding recorded routes ([070]: 5 of 36); `scent_6td3` s43 excluded despite being route-complete because its gate is the one [072] showed we exploit. Reaction names are a reading aid, never the join key; no chemist has checked whether the arrays are runnable (several substrates are polyfunctional -- a selectivity question this cannot answer). Frozen enumeration (md5-verified == live), loader parity asserted vs `run_campaign`, 0/300 unjoined and 0/300 ambiguous per cell. New **A THIRD FILTER, IN TWO VERSIONS -- and only one works.** Gating on >=10 reward-passing siblings (AVAILABILITY) does nothing at N=10 (the walked hubs have hundreds per class) and only bites bluntly at N=200-800: seh_s43 tolerates **800** (0 modes lost, 14 arrays -> **5**, 0.90 in >=10) while drd2_s44 breaks between 400 and 800 (196 modes, **pool-exhausted -- flagged, not a cost win**). Gating instead on **>=10 DELIVERABLE MODES** -- a micro greedy selection (same bar, same tau, FRESH selector) run inside each group -- does bite and is nearly free: **0-3 modes lost** of 78-80 at R=100 and **+0.6-7.1% reactions** for 300 modes, in exchange for arrays consolidating **10-15 -> 3-8**, `>=4` coverage **0.99-1.00 in every cell**, and `>=10` coverage roughly DOUBLING (0.52->0.94/1.00, 0.68->0.89/0.91, 0.36->0.61/0.68, 0.42->1.00). It rejects most groups it sees (173/190, 136/154, 702/730, 779/812) but keeps the ones already carrying the library. **THE PREDICTED COLLAPSE IS REAL, 9-39%, AND NOT AN ORDERING ARTIFACT:** local capacity vs delivered, on CLOSED groups only, retains **87/91/80/61%**; a group gated in at >=10 still delivers >=10 for **13-14 of 16** groups on sEH but only **14-17 of 27-31** on DRD2. Running the arm with groups offered CONTIGUOUSLY instead of interleaved by reward changes retention by **0-2 pp** => the loss is genuine cross-group/cross-hub overlap, **not** interleaving (clean negative result; the residual is a GATE problem -- it counts diversity in isolation -- not an ORDER problem). **TRUNCATION TRAP CAUGHT:** a budget stops the campaign mid-walk, and `HubBatchingStrategy` leaves exactly ONE hub partially consumed (it offers every child of a hub before moving on, breaking both loops only on the budget), so groups on that hub are flagged `hub_truncated` and EXCLUDED -- without that the seh_s43 loss reads **25% instead of 13%**. New `experiments/lsd_hubs/campaign/parallel_groups.py`; emits the per-molecule library table the committed curves never carried (they record `source_hub` but not SMILES). ~33 s/cell, pure CPU, login node. Not committed (branch Hub-Analysis). |
| [074](../Logs/074_synformer-fork-safety-four-hazards.md) | 2026-08-27 | SynFormer — why nine training runs died at hour ten, and what it took to make the tenth survive | **The runs were never slow: workers could not be spawned, and the coordinating process then waited forever. FOUR separate defects, all in OUR harness, each individually sufficient: (1) `from gflownet.models import bengio2021flow` at MODULE scope opens 6 `/dev/nvidia*` descriptors at import, poisoning process spawning for DRD2 (an sklearn pickle) and ClpP (a socket path) which never touch that code; (2) loading the sEH model takes the parent from 64 to 128-191 threads, so a worker hangs at exactly 2.9 GiB in `futex_do_wait`; (3) `torch.cuda.device_count` is lru-cached, so a 0 cached while the GPU was hidden survives the restore; (4) `torch.cuda.is_available()` inside `_free_gpu_cache` — reached from `_dock` every docking batch — opens those same 6 descriptors, breaking ClpP and ONLY ClpP. Note `torch.cuda.is_initialized()` reads False throughout all of it, so torch's own flag never reveals the condition; the `/proc/<pid>/fd` count does, and a probe must never call `is_available()`/`device_count()` to measure this because those calls CREATE it. **The OOM was a consequence, not the cause**: the authors discard workers every generation (worker memory returns to 0.0-0.1 GiB, and 893/893 rows keep their recipes), we kept ours alive, and the mechanism that would have bounded the leak is what the defects broke. DRD2 and ClpP now VERIFIED end-to-end (5 teardowns, budget reached, recipes written; ClpP 40.1->22.0 GiB across a teardown with docking live). sEH is STRUCTURALLY blocked — job 75080 with SynFormer removed shows spawn-before-load OK and spawn-after-load FAIL with the parent at fds=0 threads=33, so those metrics are necessary but NOT sufficient and three successive guards were reverted. Bonus fairness result: running the authors' loop unmodified, THEIR `reproduce` crashes stochastically (`IndexError` from `crossover_non_ring`'s `RunReactants(...)[0]`, catching only `ValueError`), so our widened catch is a required bug fix, not a preference. NOT verified: smokes ran 120-400 molecule budgets against production's 10,000 and the original failure took 10 h to appear — 'safe to launch', not 'will complete'.** |
| [075](../Logs/075_s3gfn-selection-arms-and-one-step-molecules.md) | 2026-08-28 | S3-GFN / sEH+DRD2+ClpP — greedy vs SPARROW on ONE retrosynthesis at 100 reactions, and an audit of a suspicious result | **The 100-modes-for-100-reactions cell is real and it is a finding about the GENERATOR: 98 of the 100 selected molecules are ONE reaction from purchasable stock (144/144 starting materials genuinely in catalogue), so the solver was handed a shopping list rather than out-planning anyone.** One-step share is 39-80% for S3-GFN vs 9-34% REINVENT and 0-16% Saturn — a generator effect, not a target effect (target medians only 14-34%). Enlarging S3-GFN's sEH pool from a FROZEN checkpoint (no retraining) took seed 43 from 65 above-bar molecules (28 modes) to 649 (270 modes) and changed its delivered count from 25 to 100, so every earlier small-pool comparison measured the pool and not the pipeline; cost 10.3k/41.3k/10.3k eval-phase scoring calls, up to 4x the training budget, and must be quoted alongside. Also: `s3gfn_drd2_seed42` routes 0/500 — 79% of that pool is amides of an amine absent from our catalogue (stock coverage, not a pipeline bug). |
| [076](../Logs/076_fraggfn-normalized-into-the-competitor-block.md) | 2026-08-28 | FragGFN — moving a generator between two benchmarks that were never normalized the same way | **FragGFN reclassified out of the 16-cell reaction-GFlowNet matrix into the COMPETITOR block: it is a fragment-based GFlowNet with no reaction model, so it is S3-GFN's sibling, not RGFN's. The move forced a retrain because the two halves normalize on DIFFERENT QUANTITIES — competitors on ~10,000 ORACLE CALLS (each paper's own default), the matrix on 5,000 TRAINING STEPS — and a step scores a whole batch. FragGFN was running 5,000 x 64 = **320,000 reward evals, 32x** every other competitor, the same class of error the 2026-08-20 audit caught in REINVENT at 12.4x where it demonstrably distorted results. All nine cells now record EXACTLY 10,048 (157 x 64), matching REINVENT and S3-GFN. Also added the per-molecule trace it had none of (verified exact: a 12-step run = 768 rows), pinned `num_from_policy` so the budget cannot drift with an upstream default, and made `max_nodes` uniform at 6 — reclassified from 'paper-faithful' to a DISCLOSED DEVIATION, since upstream sets 9 (seh_frag.py:145) and 9 collapses DRD2 to ~0.03 (entry 046) while 6 recovers the RGFN paper's 0.908; seed 42 had been running 9 while 43/44 ran 6, so every PRE-retrain FragGFN three-seed band mixed two caps. **Six of nine cells already exceed the 500-mode Stage-2 target from their training history alone** (sEH 982/1283/1283, ClpP 668/616/572) at zero extra oracle cost; only DRD2 (376/426/435) needs rounds. Measured costs: surrogate cell 3 min 46 s, docking cell 2 h 07 at **0.65 s/molecule** — 8x faster than Saturn's 5.1, because batched requests plus max_nodes=6 make small molecules. TWO OF MY OWN MEASUREMENT ERRORS corrected in-flight: the overage was first reported as 64x (I read the frozen proxy's INFERENCE batch, 128, instead of the training batch, 64 — which was already the authors' value), and the docking cells were first sized at 17 h by borrowing Saturn's rate. **STILL OPEN, and it runs in OUR favour:** RGFN, SCENT and RxnFlow remain on the matrix's 5,000-step convention, unmeasured — inferred from config files only.** |
| [077](../Logs/077_synthetic-depth-constraint-and-catalogue-ladder.md) | 2026-09-04 | sEH + DRD2 + ClpP — what the reaction budget measures once trivially-makeable molecules are excluded | **DIAGNOSTIC ONLY — every number uses SUPERSEDED gates and none belongs in a figure.** Following [075] (the competitor hits the metric's FLOOR because 98/100 of its molecules are one reaction from a 17.4M-compound catalogue), we forbade shallow molecules on BOTH sides and swept min synthetic depth 1-5. **Three regimes, not two:** on sEH at gate 5.68 (properly gate-matched) ours holds **96/96/95 -> 94/93/94** molecules at R=100 going from depth >=1 to >=2 across three seeds while the competitor falls **98/100 -> 72/86**; at >=4 we are 58/59/54 vs 53/53; and at **>=5 WE DELIVER ZERO on every seed** while the competitor still returns 38/24 — our reaction cap of 4 empties the pool, so the regime where our argument should be strongest is one we cannot reach. Reactions/molecule over the sweep: ours 1.07->1.48, theirs 1.00->2.63. **THE SIZE CONTROL IS WHAT MAKES THIS INTERPRETABLE:** halving the competitor's pool costs only 1-6% (98->92/94/94, 100->98/99/98 over top-N and two random draws) while depth-filtering to the IDENTICAL size costs 14-27% — depth effect is 5-15x the size effect, and the size-matched arms stay pinned at 1.01-1.07 rxn/mode, i.e. on the floor. Best-candidate is flat at 27-28 modes / ~3.2-3.4 rxn/mode at EVERY depth (internal control: the filter is not starving pools indiscriminately). **LOAD-BEARING CAVEAT — the depth axis is NOT the same quantity on the two sides:** ours counts reactions from our 418 blocks, theirs from 17.4M ZINC, so the same molecule scores shallower on their axis and the constraint bites them harder at every setting. Mechanism measured: 73.8% of our enumerated children attach a promoted fragment that must itself be built, so even a bought hub's child usually reaches depth >=2 — min-depth 2 removes 0.2% of our pool against 36% of theirs. **Per-method curves are meaningful; NO CROSSOVER MAY BE QUOTED** until both are routed against one catalogue (~1,000-2,000 molecules/seed at 11.1 s/mol measured, <1 h for three seeds if parallelised). **A RESULT FROM EARLIER THE SAME SESSION IS RETRACTED:** a first pass on the `_big` pools (2026-08-28, gate 7.0) read 67/62/70 vs 80 = 1.19-1.29x with ClpP and DRD2 flipping too; those pools are superseded by Stage 2 (2026-08-29, 500 tau-distinct modes at current gates) where the same constraint gives 72/86 vs 93/94. The `_big` numbers must not be quoted and are retained on disk with `gate_provenance` set so the error is not repeated. DRD2 s42 is **pool-exhausted at every rung >=2** (28/16/9/7 delivered of 28/16/9/7 available, 93/70/50/43 reactions of 100) because only **50 of 500** route at all — [075]'s unroutable seed-42 finding reproduced at the current protocol. Reagent structure: ClpP unfiltered is 100 molecules from **165 purchases, 135 used exactly once**, max reuse 4 — no sharing at all — against our 80 molecules off **18 built hubs**. Do NOT use the artifacts' `n_starting_materials` field (objective is weights=[1,0,0,0,0], purchases are free, so the solver sets ~43% of compound nodes regardless). New `depth_pruned_frontier.py` (competitor side, re-solves rather than trims per CLAUDE.md) + `--min-synth-depth` in `run_campaign.py` (default off, both pools, `glue/` untouched). **Catalogue ladder built and IN FLIGHT** (job 75660): five stocks (ZINC 17.4M / ZINCFrag 178,598 / our 456 keys / plus random-ZINC twins matched key-for-key as the size-vs-chemistry control), 100 molecules/cell, plain AiZynth not MultiAiZ. Its ZINC rung reads 72% solved against MultiAiZ's independent 70.2% routed — instrument validated. **A 3-molecule smoke caught two defects that would have produced a silently all-zero ladder:** `search_stats` has NO `is_solved` key, and `routes.reaction_trees` includes UNSOLVED partial trees whose reaction count is not a route length; both fixed to `ReactionTree.is_solved`. Snapshots carry `smiles_to_min_num_reactions` but not `smiles_to_route`, so nested costs use the same non-nested fallback every prior number for these cells used. Not committed (branch Hub-Analysis). **UPDATED 2026-09-06, TWO CLAIMS IN THIS ROW RETRACTED.** (1) The depth-5 collapse ("we deliver ZERO") was an AXIS ARTIFACT: depth was counted from our 418 blocks on our side and from ZINC's 17.4M on theirs. Re-measured with both sides on ZINC (job 75749), hub-batching delivers **73-86 molecules at depth>=5** and the advantage GROWS with depth — 1.28x at >=2 to **1.92-2.26x at >=5** (ours drop/deep 94/96, 92/94, 86/91, 82/89, 73/86 vs theirs 98, 72, 60⚠capped, 53, 38; best-candidate flat at 26-28). A molecule 4 reactions from our blocks is often FURTHER from ZINC, so our own axis made our molecules look shallow and emptied the pool exactly where we win most — the mismatched-axis figure would have shown us collapsing in our best regime. All five `deep` rungs CONVERGED; all five `drop` rungs hit the 40-round cap with 2/3/10/17/42 unjudged, so `drop` is an UPPER bound and the band is widest at depth 5 because 39% of our molecules are unroutable by plain AiZynth. 11,167 molecules routed. (2) The ladder's finding is **CURATION, NOT SIZE**: a random 178,598-key slice of ZINC routes **0%** where curated ZINCFrag of identical size routes 51-52% (ZINC 17.4M: 71/70%; our 456 keys: 0%; random 456: 0%). This retires the "40,000x" framing. Median depth 2.0 at both ZINC and ZINCFrag, so shrinking the catalogue removes molecules wholesale rather than lengthening routes. CAVEATS: `zinc` and `zincfrag` are NOT nested in our implementation (DRD2 inverts, 16% ZINC vs 39% ZINCFrag), and 0% from our blocks reflects USPTO-vs-our-112-templates incompatibility ([048] Exp A), not chemical unreachability. **ALSO FOUND: a pre-existing NaN gate hole in `build_s3gfn_pools.load_ranked`** — "skip if it fails" phrasing means NaN falls THROUGH the gate, so unscored molecules entered pools; **20 of 176 pools contaminated**, worst `s3gfn_clpp_seed42/43/44_stage2_N500` at 408/422/403 of 500 (~82% unscored). Every other gate site phrases the test positively and is safe, so selection results and mode counts stand — but pool sizes were overstated, MultiAiZ effort was wasted, and **pool-exhausted flags were misattributed**. Fixed by the co-agent as `3a66acf`. Underneath it: ClpP stage-2 sources are ~48-50% NaN at **exactly 8,000 rows per seed** (= 2 x the 4,000 `--round-size`), i.e. two whole scoring rounds returned nothing, which if the docker never started means the Stage-2 surcharge is overstated by 8,000 per ClpP cell — handed to the Standard Pipeline session. **New c-mode sweep** (catalogue-distinct modes, no retrosynthesis): ours 95/78/51 modes at tau 0.5/0.4/0.3 vs theirs 49 at 0.3 — **the methods CONVERGE at tau=0.3 (51 vs 49, their rxn/mode marginally better at 2.04 vs 2.16)** because mutual dissimilarity, not the reaction budget, becomes binding; two pools differing by 4 orders of magnitude (391,876 children vs 90) land within 4%. tau=0.3 is pool-limited AND draw-sensitive (yield swings 2.7x between draws from one frozen checkpoint), so it carries no conclusion alone. **Two further claims of mine tested and withdrawn**: the block test is NOT a fixed bar helped linearly by sampling, and the extra draws are NOT increasingly catalogue-like — median block-sim is FLAT across reward deciles (0.425->0.444) and the two draws differ only in the far-left tail. |
| [078](../Logs/078_arm-a-viability-pilot-library-not-budget.md) | 2026-09-08 | SCENT + RGFN / sEH — does a reaction-GFN trained to the matched 10,000-call budget still carry a usable flow field for hub-batching? **(GATES THE 108-CELL RE-RUN)** | **Arm A breaks SCENT specifically, NOT hub-batching in general -- and the four-cell table at one gate says so: RGFN is UNHARMED by the 32x cut (57 -> 65 modes, 2.28x -> 2.60x) while SCENT is HALVED (96 -> 39, 3.43x -> 1.56x). SCENT at arm A costs 1.918 rxn/mode against RGFN's 1.965 -- within 2.5%, i.e. stripped of its library it lands on the library-less cost profile.** Arm A leaves the field just as separated as the 320,000-call one (top10-above-median **4.28 nats vs 2.90**) and TEN TIMES better evidenced (**101** observed children per walked hub vs 10) -- so it is not noise. The flow field itself survives the cut on both generators. **CAUSE, isolated by a controlled 2x2:** SCENT promotes fragments on an ITERATION schedule (`every_n_iterations=1000` x 10), so the first promotion sits at iteration 1,000 = **64,000 calls, 6.4x the whole arm-A budget** -- `additional_fragments/` is never created, the checkpoint is **28 MB against v1's 205 MB**, `free_frag`/`prebuild-k` are no-ops and the cell fails `check_route_readiness` (no recipes -> no route dataset). RGFN, which never has a library, hubs at depth 3 at **both** budgets ({1:1,2:5,3:34} vs {1:2,2:4,3:34}); SCENT flips **1 -> 3**. So the library, not training length, decides where a flow field hubs -- no single-generator comparison could show that. **DEPTH-0 SHARE, first measurement and unrecoverable for v1:** 2 of 200 hubs (1%) deliver **36.5%** of the R=100 library, because a bought hub prices at **1.000 rxn/mode, the floor** (1.000/1.042/1.154 by depth 0/1/2) -- counting HUBS understates reliance ~36x, so quote the delivered-MODE share. Excluding buy-and-couple molecules costs **96->94 modes, 2.1%**. A LOWER BOUND and a SCENT number (`--pool all` holds 11 depth-0 hubs, not 2; the library-less generators already hub one step from terminal). **GRID TEST over 35 cells:** library-less cells are **23/23 concentrated** (median mode share ~85%) at cap-1 or cap-2; SCENT only **6/12** (~51%) -- so the real statement is *without a library the field COLLAPSES onto one depth, with one it SPREADS*, and SCENT's apparent target-dependence is largely spread distributions read by their largest bin. **TRACE COUNTER VINDICATED:** RGFN's arm-A checkpoint crossed at iteration **83**, not the 100 that arithmetic predicts, because it scores 100 forward **+ 20 replay** = 120.6/iter -- step-count placement would have overshot the budget ~20% in our favour. **THREE DEFECTS:** `pick_hubs` overwrote its own sidecars (**the cause of the standing 'hub_scores.csv is often stale' warning** -- it does not age, the next run clobbers it; fixed in `6599590`); `run_campaign`'s `hub_pool`/`min_hub_depth` are null on any real cell because `pick_hubs` is a separate stage; and RGFN's `timing.json` `score` reads 52 ms for 16,919 molecules (3 us each) -- not credible, do not quote. **UNEXPLAINED, three mechanisms ruled out:** dose-response (all 12 SCENT cells fired exactly 4 promotions), reward-by-depth (top-decile hub depth 2/3/2/3, no surrogate/docking split), and a policy-term effect (`logR` dominates the P_B/P_F terms by an order of magnitude). **Consequence: internal matrix at arm B, external head-to-head at arm A.** Caveat: arm A enumerated 64 hubs capped at 4,000 children vs v1's 200 uncapped -- but the same handicap hit BOTH generators and only SCENT collapsed (RGFN improved under it), so it caveats magnitudes, not the finding. |
| [077](../Logs/077_fraggfn-stage2-free-harvest.md) | 2026-08-28 | FragGFN / sEH+DRD2+ClpP — how much of the 500-mode Stage-2 pool was ALREADY PAID FOR by training? | **Six of nine cells reach the full 500-mode pool from the training history alone, at ZERO extra scoring calls; the entire Stage-2 surcharge is 5,992 calls and all of it is DRD2.** `trace.csv` records every molecule the oracle scored during training, so harvesting it before sampling is nearly free: sEH (990/1,301/1,303 above the 5.68 gate) and ClpP (673/618/572 above −9.1) each hit 500 modes with no sampling at all — on ClpP that is ~28 GPU-h per cell not spent. DRD2 is the exception at 376/427/435 above 0.345, needing ONE 4,000-molecule round per seed (~2,000 new distinct, ~20% on top of the 10,048-call training budget, 1.3–1.4 min). **The DRD2 shortfall is a shortage of gate-clearing chemistry, not redundancy** — gated and modes are nearly identical (376→376, 427→426, 435→435), so the diversity filter removes essentially nothing; sEH is the opposite, 990–1,303 gated collapsing to 500 only because the cap stops the count. **TRAP, and it silently changes every number:** the 5%-FPR gates were UNCOMMITTED in the shared checkout, so a worktree branched off `Hub-Analysis` HEAD resolves the OLD 5.0/0.5/−8.0 through `targets.py` with no warning — a first pass of this analysis did exactly that and read 8 of 9 cells free instead of 6, with sEH's gated set 2,146 instead of 990. Production was unaffected because the launcher `cd`s to the shared checkout. **SECOND TRAP, in the launcher:** the shared `${GEN}_${TGT}_fixed.yaml` convention resolves for fraggfn to a file that EXISTS and is WRONG (`fraggfn_seh_fixed.yaml`, the old 5,000-step build), and the runner's resume guard is `remaining = n_train_steps - loop._it`, so it would have read 157 < 5000 and silently re-trained 4,843 steps inside a SAMPLING stage — corrupting the oracle-call accounting in FragGFN's favour, past a `[ -s $CFG ]` guard that cannot catch a wrong-but-present file. **A PREDICTION MADE HERE WAS WRONG:** `clpp/44`'s mid-training gated rate of 4.25% (vs siblings' 7.2%/6.6%) projected ~400 modes and a first real docking round; its finished trace reads 6.2% and 500 modes free — a gate-clearing rate measured mid-training is not an estimate of the final rate. Consequently the new Stage-2 docking-server block (OpenCL gate once per job, server per cell) is verified only in start-and-teardown form and **remains unexercised under load**. Jobs 75154/75155/75158/75159. |
| [078](../Logs/078_stage3-routability-is-the-bottleneck.md) | 2026-08-31 | All entrants / sEH+DRD2+ClpP — after Stage 2 equalises the pools at ~500 modes, how many molecules can a planner actually MAKE? | **ROUTABILITY, not diversity or reward, bounds this benchmark — and for FragGFN the mechanism is measured, not inferred.** Every cell enters Stage 3 with ~500 distinct modes; FragGFN loses 90-97% of them at retrosynthesis on every target (routed 35/500 sEH, 32 and 12/500 DRD2, 52/500 ClpP) against S3-GFN's 351-394/500 on sEH. The survivors are the WRONG molecules: pooled over 119 routed vs 1,381 unrouted, the routable ones are flatter and greasier (biaryl 3 vs 1, aromatic rings 5 vs 3, Fsp3 0.16 vs 0.33, logP 7.24 vs 5.88), i.e. the sp3/heteroatom-rich drug-like majority is what fails. **The mechanism, read off the routes themselves: 46%/56%/19% of FragGFN's route steps form an aryl-aryl bond vs 2% for S3-GFN** — a fragment-joining generator makes bonds reaction templates do not make, EXCEPT where the joint happens to be a cross-coupling. **BUT DRD2 IS CONFOUNDED AND THE ENTRY SAYS SO:** S3-GFN itself collapses to 10.0% there, so the gap narrows from 10-14x (sEH) to 1.6-4x, and [075] already traced DRD2's routing failure to an amine absent from our ZINC stock — on that target the binding constraint is OURS. **A DEFECT CAUGHT BEFORE PUBLICATION:** greedy_frontier.csv records n_modes (REQUESTED) and n_targets_priced (DELIVERED) and divides rxn_per_mode by the request; these are equal on every multiaiz cell so the distinction was invisible, but on SynFormer's native routes they diverge 89/100, 112/125, 133/150 — '100 modes at 198 reactions' delivers 89. Cause ESTABLISHED not guessed: not stereo collapse (1/500), not coverage (network_stats m=100 reads n_targets 100, n_routeless_targets 0); MultiAiZ offers MANY routes per molecule (1,422 reaction nodes/217 shared) vs native's ONE (232/55), so under the `count` objective the solver declines expensive targets it cannot make cheaper. QUOTE n_targets_priced. Also: discovery measured at 6-12 h/cell not the 3.67 h used for sizing (five chains timed out at 20 h with one cell each; FragGFN is both least routable AND slowest, 89 s/target); naive-vs-pruned pool overlap is a free redundancy measure (FragGFN DRD2 100% = duplicate run, Saturn 4-19% = genuinely different, matching its mode collapse); a cell under 25 modes produced NO frontier row until the ladder was lowered to 5; and s3gfn_drd2_s42 went from 0/500 routed ([075], budget-faithful pool) to 50/500 from the Stage-2 pool, so upsampling RECOVERED a dead cell. **SCOPE: 10 of 76 route runs, mostly seed 42; REINVENT/Saturn/TANGO are 0/58** — the competitor comparison this campaign exists for has not started, blocked on fairshare (EffectvUsage 0.279 vs NormShares 0.023) after five of nine nodes were reserved. Jobs 75169-75300. |
| [079](../Logs/079_reprice-capped-rows-and-coarse-ladder.md) | 2026-09-06 | All entrants / sEH+DRD2+ClpP — at the 100-reaction readout, is the number a property of the GENERATOR or of our own pricing tools? | **Two measurement defects, both in our tooling, one of them holding up a headline.** (1) **14 of 87 SPARROW rows at R=100 were `TimeLimit`** — lower bounds our own convention forbids quoting as optima — and on sEH they fall ENTIRELY on the competitor: pruned pool reads S3-GFN 98/100/100 `Optimal` (~1.6 s) against REINVENT 80/70/92 `TimeLimit` (1802 s), while the FULLY CERTIFIED naive pool points the other way (S3-GFN 56/38/49 vs REINVENT 61/47/83). The pool we lead on is exactly the pool where the competitor's solver was truncated, and our pruned rows sit at the 1.0 rxn/mode ARITHMETIC CEILING, so the only real question was whether REINVENT reaches it too. **The fix was not compute but precision:** relaxing CBC's gap from SPARROW's hardcoded 1e-7 to 1e-2 turned seed 44 from `TimeLimit` at 1801.71 s into `Optimal` at **5.65 s with the IDENTICAL 92 modes** — the cap was hiding a correct number, not a wrong one. Certification is now at a 1% gap vs 1e-7 for the older rows (±1 mode on 92; state it when quoting). This also REFINES the note in `--gap-rel`'s own help that gap relaxation 'did NOT achieve convergence alone' — here it did, and the 6 h cap was never approached. (2) **The greedy arm is priced on the MODE axis but read on the REACTION axis**, so it is quantised by rung spacing: **59 of 96 cells were still coarse (25/50/75/…), the median one quoted from a rung leaving 33 of 100 reactions UNSPENT** (max 52; 26 cells >30), and **10 cells had no readable R=100 number at all** because 25 modes already costs >100 reactions. Re-pricing dense moved the first cell 25→**30 modes** at the same budget, with every shared rung reproducing exactly (25→77 rxn both ways) — a strict refinement that RAISES every entrant including ours, i.e. a fairness fix, not a favourable one. **Neither fix re-ran any chemistry:** routes were cached and only the selection maths was redone, deliberately BYPASSING the chain, which would have re-run ~3.7 h/cell of discovery, clobbered the ten-budget CSV with a one-row file, AND rebuilt pools with the since-added NaN gate so they would no longer match their own cached routes. Verified before touching anything that all 96 campaign greedy results POSTDATE the last greedy fix (c802026), so the re-price changes the ladder and nothing else. **THE LADDER HAD ERASED A RESULT, not merely depressed one.** The 58-cell dense pass moved 18 cells (450→620 modes summed), recovered 1 that had NO readable R=100 number, and changed none of the other 6 — but UNEVENLY (25→30 for some, 25→40 for others), so it was distorting cross-generator comparison. On **ClpP naive it had hidden a clean 3-seed result: S3-GFN 40/40/40 vs REINVENT 30/30/30, disjoint, where BOTH previously read 25/25/25 (a tie)**. SynFormer was worst — understated up to **1.8×** (25@52 → 45@93) because `submit_native_routes.sh`'s own ladder skips 25→50. **A SECOND DISAGREEMENT AXIS, missed on the first pass:** the leader depends on the SELECTOR as well as the pool. Greedy chases diversity, SPARROW-Batching optimises SHARED INTERMEDIATES, and the greedy→SB uplift is a stable per-generator property — REINVENT **1.86× naive / 2.57× pruned** vs S3-GFN **1.01× / 1.65×**, consistent on both pools and all three targets — which REVERSES the naive-pool ranking between selectors (greedy: S3-GFN 40/50/75 vs REINVENT 30/30/30; SB: REINVENT 61/47/83 vs S3-GFN 56/38/49). So the benchmark is a **2×2** with a measured mechanism per axis (pool = reward-ranking redundancy, selector = route sharing). **THE SELECTOR IS THE DOMINANT AXIS:** completing the 2×2 with ClpP, greedy favours S3-GFN **4 of 4** pool×target combos (disjoint 3-seed bands in all four) while SB favours REINVENT **3 of 4** — all but sEH/pruned, exactly where S3-GFN sits at the 1.0 rxn/mode CEILING. So the arm CLAUDE.md designates to carry the headline (SB, the competitor's own optimiser and the stronger arm) is the arm the COMPETITOR usually wins on; ClpP's REINVENT SB rows are still capped but a cap is a LOWER bound, so the direction is safe and only the margin is uncertain. An earlier draft of the entry said '3 of 4 quadrants favour S3-GFN' — read off sEH alone, and it does not generalise. Saturn/TANGO uplift <1.0× is NOT a defect — on a collapsed pool SB pursues its own objective and does not chase modes. **TWO CONFOUNDS CLOSED BY MEASUREMENT:** all 96 campaign greedy results postdate the last greedy-arm change (c802026) so the re-price moves the ladder alone; and 75771/75772 re-ran SynFormer greedy on the SAME coarse ladder against NaN-fixed REBUILT pools — all 6 pruned cells came back IDENTICAL, so the pool rebuild contributes nothing to any delta. **TWO LAUNCHER BUGS CAUGHT BY A REFUSAL, not by a wrong number:** `${TAG_SUFFIX:-_stage2}` rewrote an explicitly-EMPTY suffix back to `_stage2` (route-carrying generators have no Stage 2), and `FR_ROOT` lacked the `experiments/` segment — the script refuses on a cache miss instead of rediscovering, so both surfaced as an 8 s rc=1 naming the bad paths rather than hours spent pricing the wrong pool. Residual: 77/95 cells resolved, 18 still coarse WHERE THEY LAND (12 from the dense ladder's own 30→40→50→60→75→100 spacing), re-priced on a finer ladder; the ladder is still 25-spaced ABOVE 100, where no cell yet lands. Jobs 75788–75807. |

---

## The systems

| Name | PDB | E3 | Neosubstrate | Glue | Warhead | Oracle status |
|---|---|---|---|---|---|---|
| 5HXB | 5HXB | CRBN | GSPT1 | CC-885 | glutarimide | ⚠️ Ceiling — even with warhead-anchored docking, the neosubstrate differential doesn't discriminate real glues from random ones |
| 6TD3 | 6TD3 | DDB1 (adaptor) | cyclin K (CDK12-bound) | CR8 | purine (binds CDK12, *not* the E3) | ✅ Validated oracle |

> Note on 6TD3: the protein *recruited* in the differential is the **E3 adaptor DDB1**, not the degraded neosubstrate (cyclin K). This is the mirror-image flip described above and the reason "neosubstrate differential" is a slight misnomer for this system.

---

## Key terminology

- **RGFN**: Our generative model — a GFlowNet that builds molecules from reactions and **samples proportional to reward** (not reward-maximizing). Needs an oracle/proxy reward to learn what "good" looks like. See `[koziarski2024rgfn]`, `[bengio2021gflownet]`.
- **Oracle (`O`)**: The expensive, trusted scoring function. Currently docking on the ternary complex; MD stability is a candidate complementary/replacement oracle. In the active-learning loop it is queried only on the per-round query batch.
- **Proxy (`M`)**: The fast, learned in-loop reward, fit on oracle-labeled data and refit each round. RGFN trains against `M(x)^β`, never against `O` directly. See `[bengio2021gflownet]` §4.3 / Alg. 1.
- **Active-learning loop / multi-round**: The training procedure — fit proxy, train RGFN against it, sample a batch, label the batch with the oracle, refit the proxy, repeat for `N` rounds. `[bengio2021gflownet]` Alg. 1 (A.5).
- **β (inverse temperature)**: Controls how peaked the target reward is. Higher β concentrates sampling on high-reward modes.
- **Molecular glue**: Small molecule that induces proximity of two proteins (E3 ligase + neosubstrate), leading to ubiquitination and degradation of the neosubstrate.
- **E3 ligase**: The degradation machinery. We study CRBN (part of the CRL4 complex) and DDB1 (adaptor for CDK12).
- **Neosubstrate**: The protein being degraded. GSPT1 (CRBN system); cyclin K, presented by CDK12 (6TD3 system).
- **Warhead**: The part of the glue that anchors into a fixed pocket. That pocket is on the **E3** for CRBN (glutarimide → CRBN tri-Trp cage) but on the **target kinase** for 6TD3 (purine → CDK12 ATP pocket) — *not* the E3. This flip is the mirror-image point above.
- **Neosubstrate differential**: Tier2 − Tier1 score for the same docked pose. Isolates the arm's contribution to recruiting the second protein (the neosubstrate GSPT1 for CRBN; the E3 adaptor DDB1 for 6TD3). Our primary discrimination metric and the project's novel contribution. The validated signal uses the **Vina** score (lower = better binding, so the differential is lower-is-better; entries 006/007); gnina's CNN score is used only to pick the docked pose, not as the differential.
- **Tier 1 / Tier 2**: Tier 1 = the **warhead-anchoring protein only**. Tier 2 = that protein **plus the recruited partner**. Same pose scored against both. (CRBN: Tier 1 = CRBN, Tier 2 = CRBN+GSPT1. 6TD3: Tier 1 = CDK12, Tier 2 = CDK12+DDB1.)
- **Decoys**: Realistic fake glues — correct warhead, random drug-like arm. If decoys score like known glues, the oracle only reads warhead binding and is useless for RGFN.
- **gnina**: Our docking engine (v1.3.2, CNN-rescored). Launched from `/scratch/markymoo/gnina/run_gnina.sh`.
- **Balam**: SciNet GPU cluster (4× A100 per debug_full_node, 64 cores, 1 h max). Outputs go to `$SCRATCH` (`/scratch/markymoo/`), not `$HOME`.
- **SPARROW-Verifier (SV)**: SPARROW as an **independent auditor** — given an already-chosen library and its recipes, find the cheapest way to make *all* of it (`constrain_all_targets=True`, reward weight 0). Used to show our own cost accounting is not self-serving; we *expect* agreement.
- **SPARROW-Batching (SB)**: SPARROW as a **competitor** — given a large pool and a reaction budget, choose *which* molecules to make (`constrain_all_targets=False`, reward objective, `--max-rxns`). A genuine rival to hub-batching. Has **no diversity term**, so the distinctness of its picks is always measured, never assumed. Always name the pool with it (`BC-SB`, `S3GFN + SB`).
- **BC-SB (Best-Candidate SPARROW-Batching)**: SB over the N highest-reward *distinct* candidates the generator sampled — no enumeration, no flow. The "naive chemist" competitor.
- **BC-Enum-SB (Best-Candidate Full-Enumeration SPARROW-Batching)**: SB over the full enumeration of 64 hubs picked by walking candidates best-reward-first. The strongest competitor that never uses flow.
- **Mode**: one distinct "kind" of molecule — clears the reward gate *and* is Tanimoto-dissimilar (Morgan r=3/2048, ≤ 0.5) from every mode already counted. 100 modes = 100 genuinely different shots on goal, not 100 near-copies.
- **Hub**: a partly-built intermediate many finished molecules can share. Build it once, branch off it.
- **Native route vs from-scratch**: *native* = the recipe our generator already used (free). *From-scratch* = a planner re-deriving a recipe for a finished molecule. Which one you price with can reverse a head-to-head result, because re-derivation partly measures whose chemistry the planner's catalogue happens to cover (entry `047`).
- **count-once**: our cost model — each molecule's assembly couplings plus each distinct promoted fragment built **once**. Only defined over a reaction DAG, so it **cannot score route-less generators** — which is why cross-method comparisons must use SPARROW.

---

## Where things live

- **Experiment logs**: `Logs/` (indexed above).
- **Key publications** (method + biology): `Logs/references/` — orientation sheet + PDFs; ground method/biology claims here.
- **Repo layout, code, datasets, and result/scratch locations**: see `docs/ARCHITECTURE.md` (the single source for where things live in the repo) — kept there so locations aren't duplicated across docs.
